# Bristlenose — Where I Left Off

Last updated: 22 Sep 2026. _This file is a capture inbox + session context, not a changelog — `git log` + `CHANGELOG.md` are the unabridged record._

**⏳ PENDING, needs a nudge whenever `main` moves — PR #143 (frontend deps) is verified green and cannot land on its own.** All six required checks pass on it; **mypy is NOT a required check**, so its red is irrelevant (declared ratchet soft gate, red on `main` too). The blocker is `strict: True` branch protection plus a busy `main`: **GitHub's auto-merge does not update a branch**, so every push to `main` flips the PR to `BEHIND` and auto-merge silently declines to fire — armed, looking in-progress, structurally stuck. `main` took eight commits in half an hour today, so this will recur.

    gh pr update-branch 143     # whenever `gh pr view 143 --json mergeStateStatus` says BEHIND

Auto-merge is armed (squash, delete branch), so the update is the only action needed; it lands itself once CI passes without `main` moving underneath. **No split was needed** — the one reason to split was #143's +24 kB "bundle regression", which was the old `size-limit` config measuring chunk *filenames* that Rolldown renames between vite minors (`f020e234`). Verified locally on the PR's own lock: `npm ci` clean, build clean, lint + typecheck clean, 1726 tests, and the bundle gate at **199.6 kB of 220 kB — more headroom than `main`'s 204.6 kB**. Its ratchet red was cleared by raising `pytest_skip_sites` 23 → 24 (`9002ee14`); **that raise is on the time-audit work's skip** (`test_media_time_meta.py`'s corpus-availability `skipif`), taken because a ceiling holding unrelated correct work hostage is how a gate gets switched off — worth a glance from whoever owns that test.

**22 Sep 2026 (evening) — one preference was driving three unrelated languages, and the worst of them told Whisper every recording was English.** The UI language belongs to the reader, the spoken language to the recording, the generated output to the deliverable's audience; all three resolved from the picker's `language` key. `whisper_language` defaulted to `"en"`, which is a **pin, not a default** (`"auto"` is the sentinel), so every CLI user and every English-UI desktop user had non-English audio decoded under an English language token — not translated, not mislabelled, decoded wrong. Whisper speaks 100 languages, we render 22, and the 78 in between are the people it hurt: a UK study with Polish, Nepali or Bengali participants among them. Fixed across two commits (`cca68462`, `9ba2bacf`): default `auto`, desktop injects nothing, `--whisper-language` on `run`/`transcribe`, the detected label captured from both backends and carried to the transcript header (`extra_headers` was already on the formatter, so that half was free).
- **Four axis-1 defects fixed in the same pass.** The matcher was latching — `preferredLocalizations(from:forPreferences: nil)` resolves the language list on the **first call in the process** and caches it for the lifetime, and AppKit primes it during `NSApplicationMain` before any of our code runs; five review lanes found this independently and three measured it. The AppKit write-back **reverted** a language set in System Settings ▸ Apps rather than merely ignoring it, so the researcher's choice vanished from the System Settings pane itself. The picker displayed **English while the app ran in German** (`@AppStorage` default is a Swift-side fallback, not a registered default) — seeded from `I18n.resolvedLocale` now, as `@State` and deliberately **not** a seeded key, because the key's *absence* is what makes the app follow the system. And two readers plus the `?locale=` param read absence as English.
- **Decisions taken and recorded in `docs/design-locale-negotiation.md` § Decisions and fixes** — including the four **declined**, because a declined option with no written reason is one somebody re-derives: no "System Default" picker row (the population wanting a way back has 2+ preferred languages, which is exactly who macOS shows the per-app row to), `UIPrefersShowingLanguageSettings` still unset, Swiss German falling to English accepted (`gsw` is not a German variant to Apple's matcher), UI-vs-output-language decoupling left alone.
- **Owed: a CHANGELOG line at the next bump, under Fixed — not Improved.** "Non-English recordings were transcribed as if they were English" is a defect with a user-visible consequence, not a default being tuned. It also changes behaviour for existing English-UI users (pinned → auto-detect).
- **Owed: the website's `docs-src/cli.md` in the deploy repo** for `--whisper-language`. README needed nothing — it points at `man bristlenose` rather than enumerating flags. **`.env.example` did**, and it is a fourth user-facing surface the "three places, no fourth" rule in root `CLAUDE.md` does not name.
- **Owed: `desktop/CLAUDE.md` ~line 644 carries the wrong loctable alias** (`pt-BR`→`pt`; it is `pt_BR`). Left unfixed because that file had another session's uncommitted work in it and a pathspec commit would have carried their changes under my message. `.claude/agents/i18n-review.md` is fixed (`d3ef7e52`) with the measurement: `pt` in 1,706 loctables, `pt_BR` in **8,028**, **both in zero** — so the documented alias sends every Apple cross-check to a table that exists but is the minority spelling, and the miss reads as a completed audit.
- **Still open, inherited rather than rediscovered:** the System Settings round-trip has only ever been **simulated** (reading the real sandboxed container needs Full Disk Access) — build, set a language there, relaunch, and relaunch *again* to prove the revert is gone; the `.lproj` set is still an ungated 23rd-language registration site; and **English is the *unsteered* case for generated output**, not steered-to-English, so a non-English corpus under an English UI gets provider-dependent theme language (Claude reliably English, ChatGPT and Gemini drift). **That is a decision, not an unfixed thing** (22 Sep 2026) — English sends no instruction so its prompt stays byte-identical, which is what keeps cohort baselines comparable for the majority and makes the change a no-op for them. And the stated cost is wrong: the steer is appended at the **call site**, not in the prompt `.md`, so nothing forks the prompt hash — that seam was chosen precisely to avoid churning `cohort-baselines.json`. Reopening it is one line and free of hash churn; the question is whether an English-UI researcher with a non-English corpus should get steered output, and the answer so far is no.

**22 Sep 2026 (late) — the edit pencils named their target in English to every screen reader, and a whole class had no gate.** `QuoteGroup.tsx` had zero i18n call sites and built two accessible names by interpolation, so a VoiceOver user in any of the other 20 languages heard English on the controls that rename a section or a theme. Failure class 2 — invisible to `check-locales.py` **by construction**, because nothing was missing *from* `en`. Fixed as four whole-sentence keys, not one parameterised key with the noun interpolated: a frame cannot inflect, and pl wants genitive *sekcji* against a nominative *Sekcje*, ru *раздела*, tr the possessive-accusative *başlığını*. Recorded as row 55 in `docs/i18n-defects.md`, with why it does **not** contradict row 33's decision that 100 SF Symbol labels stay English (there the icon is decoration on top of the project name; here the label is the control's only name).
- **Owed: five components still carry the same defect** — `EditableText`, `EyeToggle`, `ModalNav`, `TagGroupCard`, `TagRow`, ~7 strings so ~147 values, two of them the English-frame shape. **Not urgent and cannot rot:** `tests/test_aria_label_enrolment.py` holds them as a ratchet — the set may not grow, and a fixed file must be struck from it or the gate reddens. Listed last in that register's Undone for the same reason.
- **The gate is the durable half.** An `aria-label` may carry no human-readable literal unless it came from `t()` — a rule that *derives* the legitimate pass-throughs (`{book.title}`, `{ariaLabel}`) rather than allow-listing them, and reads the brace-balanced expression rather than the line, because the fix is a multi-line ternary a line-oriented matcher would skip without inspecting either arm. Mutation-proved.
- **Docs trued in the same pass:** `docs/design-i18n-wiring.md` archived (a March plan whose premise died six days after it was written; its §10 was the *only* thing asking anyone to wire `enums:speakerRole.*`, and was wrong to), which unblocked the 63-value deletion `docs/design-people.md` was holding on *"reconcile the wiring doc first"*. `participant` stays — it has three live Swift callers, so the block was never the "dead" four that paragraph assumed.
- **Settled while this was being written, and worth knowing before you "fix" it back: zh-Hant's ellipsis is `⋯` U+22EF, not `…` U+2026.** A concurrent session swept 22 values and carved zh-Hant out of `test_the_in_app_cta_keeps_its_ellipsis_in_every_locale` (`38cabf02`), on measured evidence — Apple's zh_TW 982:5 and zh_HK 984:5 for U+22EF, against Simplified, ja and ko which use U+2026 exclusively. The URL placeholder `drive.google.com/…/clips` keeps U+2026 deliberately. Recorded because the test *was* briefly red in between, and the reflex on seeing it is to change the locale rather than the assertion, which is backwards.

**22 Sep 2026 (afternoon) — every authored framework codebook now says what its author said, and a gate stops pointers to tags that do not exist.** Norman audited against the 2013 book: v1 had padded every group to four tags and invented where the book ran out (`system model`, `logical layout`, `exploration`, `first-time use`, `learned behaviour`, the affordance/perceived-affordance pair). Norman is at **2.5**, 28 tags each with a page; Garrett, Nielsen, Yablonski and Morville at **2.0** from the same audit method (Garrett and Morville had the identical 4-per-group symmetry; Morville's Valuable facet had been inverted from sponsor-side to user-side; Yablonski's bio placed him at a company he never worked for). Each has a decision register (`docs/design-codebook-*.md`), a golden fixture, `renamed_from:` on every rename so `codebook_sync` migrates installed rows, and the v1 archived. `tests/test_codebook_registers.py` refuses a settled decision without a witness, a fixture tag that does not exist, and a `not_this` pointing at another codebook's tag that does not exist — eighteen such pointers on its first run. Useful tags that were not the author's went to UXR (2.2: `exploration`, `first-time use`, `lost my place`). Two CC-licensed think-aloud corpora committed under `tests/fixtures/codebook-corpora/`. Plato untouched, by decision.
- **What the codebooks need next is real, task-driven speech.** One public Zipcar demo in 40-word caption chunks (identical file agrees with itself on 57 of 75 tags) is dancing on pinheads; the golden sets echo the wording and are floors. `design-codebook-norman.md` § What the next iteration needs lists what to collect first — our own moderated sessions through the real pipeline, above all.
- **The shared prompt's "always return a tag" is the one defect no codebook wording can fix** (Norman register N-18): out-of-scope quotes take a placeholder tag at 0.10 that differs by corpus and feeds Signals at that weight with no floor. Fix belongs in the autocode prompt (an abstain value) or a Signals floor.
- **The website's `codebook-frameworks.md` in the deploy repo now carries the old counts and names for all five codebooks.**
- **CHANGELOG at the next bump, under New: minor.**

**22 Sep 2026 — FIXED (`78a63dd1`): bulk hide was quadratic, its amplification was load-bearing, and ⌘A made it a hang.** The mid-collapse record moved into the store and `hideQuotes` now owns the whole gesture: mark, wait out the collapse, commit once. Three quotes cost one write where they cost nine; a selection spanning two groups collapses in both, which it did not before. Three tests, all mutation-proved. **Star fixed too (`87de20d7`)** — and it was worse than the keyboard/click disagreement: a *third* rule, `starActionIsUnstar`, is pushed over the bridge and labels the native Quotes menu Star or Unstar. A mixed selection with focus on the starred quote showed "Star" and unstarred both. One rule now, read by both paths, with the AppLayout comment that already claimed they agreed finally true. **Restore-all fixed too (`e17ce041`)** — `unhideQuotes` joins `hideQuotes` and `setStarred`, and deleting the now-callerless `toggleHide` re-homed its two contracts onto them. The fly-down cascade was never implicated: it reads a pending set in an effect keyed on a version counter, so it already expected a batch. **One live instance of the same shape remains, found at close-out and NOT fixed:** the `r` key (quick-apply last tag) loops `addTag` per target (`useKeyboardShortcuts.ts` `handleQuickApply`), and `addTag` sends a full-map `putTags` each call — so tagging a selection of n quotes sends n racing replacements of the whole tag map. Same defect, different map; it needs a bulk `addTagToQuotes` beside `hideQuotes`/`unhideQuotes`/`setStarred`. Note also that `addTag` calls `announce()` *inside* the `setState` updater, which React may invoke twice; pre-existing and separate. **A claimed defect here was FALSE and is retracted:** `e17ce041`'s message says `QuoteGroup` passes `data-testid` to a `Counter` whose prop is `testId`, so its test ids render `undefined`. They do not. `CounterProps` declares `"data-testid"?: string` and the component renames it on destructure (`"data-testid": testId`) — measured: the badge rendered through `QuoteGroup` carries `data-testid="bn-counter-section-0"`, and `Counter.test.tsx` has sixteen green assertions reading those ids with `getByTestId`. I read the destructured local name and the call site and skipped the interface between them, which is this session's own trap in miniature — a claim about two things agreeing, made without reading the thing that makes them agree. The commit message is immutable and carries it; this is the correction. Original diagnosis follows. Supersedes the 21 Sep capture, which was right about the multiplication and **wrong about the announcements**. Pressing `h` on a selection loops the selection in `handleHide` (`frontend/src/hooks/useKeyboardShortcuts.ts:183`); each `hideQuote(id)` reaches the per-card handler registered at `QuoteGroup.tsx:454`, and `handleToggleHide` (`QuoteGroup.tsx:352`) re-expands the whole selection again. N selected quotes therefore schedule N² timers, and at +300 ms each fires `toggleHide` → `setState` → a full-map `putHidden`. **Not** N² announcements: `announce()` coalesces on a rAF (`utils/announce.ts:29`) and every timer lands in the same tick, so the burst collapses to one — the 21 Sep line claiming otherwise was wrong.

- **MEASURED 22 Sep 2026, not inferred: 3 selected quotes → 9 writes, 5 → 25.** `frontend/src/islands/QuoteGroupBulkHide.test.tsx.parked` mounts the real `QuoteGroup` inside the real provider tree and counts `putHidden`; it is **red on shipped code and deliberately uncommitted**, because a red test on `main` would break concurrent sessions and CI for a defect they did not introduce. It carries a `.parked` suffix for a second reason: vitest's config sets no `include`, so the default glob collects an **untracked** file too — leaving it named `.test.tsx` would have failed another session's suite without it ever being committed. Restore with `mv <file>.parked <file>` when the fix lands. Its sibling case — one focused quote, no selection — **passes**, so the failure is specific rather than a broken harness. Land it with the fix, not before.
- **⌘A raises this from waste to a hang, and it is a natural gesture.** ⌘A selects every visible quote (`useKeyboardShortcuts.ts:437`); hide-all-then-unhide-a-few is a real triage move. At a few hundred quotes that is tens of thousands of timers, `setState` calls and fetches in one burst. The growth law is measured; the consequence at a few hundred quotes is extrapolated from it, and the one-minute confirmation is select-all, press `h`, watch the network panel.
- **The amplification is load-bearing, so the one-line fix is a visual regression.** A selection spans groups, but `hidingIds` is **per-`QuoteGroup` local state**, so a group only collapses the ids it was told about. The click path tells one group and the other groups' quotes vanish with no animation; the keyboard path accidentally tells *every* group the full set, precisely because it calls the handler once per id. Dropping the outer loop fixes the arithmetic and breaks cross-group collapse. The bug and the feature are the same line.
- **Star shares the write amplification and disagrees with itself on direction.** `handleStar` (`useKeyboardShortcuts.ts:149`) takes direction from the focused quote; `handleToggleStar` (`QuoteGroup.tsx:333`) takes it from the clicked quote, with a different fallback — same user-visible action, two rules. Both issue N full-map PUTs, as does `handleUnhideAll` (`QuoteGroup.tsx:726`). So the systemic shape is **a per-item write API with a whole-map payload**: every bulk gesture costs O(items) unordered fire-and-forget PUTs that last-arrival-wins on the server, the same family as the framework-states replacement incident in root `CLAUDE.md`.
- **Nothing tests any of it.** No `QuoteGroup.test.tsx` exists, and none of the 57 tests in `useKeyboardShortcuts.test.ts` mentions hide. Step one is a failing test, not a patch.

**22 Sep 2026 — FIXED: the pre-run cost forecast returned `None` for every user, on every model, for five months — and four separate defects each did it alone.** `estimate_pipeline_cost` is the line that answers *what will this study cost me on my own API key* before a researcher spends their own money, so this is a bring-your-own-key launch blocker rather than a missing nicety. **The symptom is the worst kind: an absent line.** The CLI prints the estimate only when it is not `None`, so there was no error to grep and nothing looked broken.

- **(1) `cohort-baselines.json` shipped as the empty Slice A placeholder** (`{"cohorts": []}`, 2026-04-28 → today) and is the *only* step of the resolution order a new user can reach — they have no local `llm-calls.jsonl` by definition. Now 15 rows generated from the 228 usable pipeline rows in the maintainer corpus by the new `scripts/regenerate-cohort-baselines.py`, the script `design-llm-call-telemetry.md` §Implementation reserved a name for in April. It emits aggregates only and **refuses to write** if a session id, run id, timestamp or prompt sha reaches the output — proved to bite, including on a value nested below a key-level scan.
- **(2) `_LOCAL_N_THRESHOLD = 3` was applied to per-run stages.** `s10`/`s11` issue exactly one call per run *by construction*, so three samples meant three runs of the same project — and an under-sampled bucket `return None`ed the **whole** forecast, so FOSSDA's 39 well-sampled per-session rows were discarded because two stages had the only sample count they will ever have. Threshold is now per-cardinality, and a thin bucket is *skipped and topped up from the baseline* rather than vetoing, so the two sources compose.
- **(3) Baseline lookup was exact-match on `(family, major)`.** `claude-sonnet-5` found nothing while `claude-sonnet-4` rows sat in the same file; worse, **three of the four cloud defaults normalise to a family with no rows at all** (`gpt-5.6-terra` → `("gpt-5.6-terra","0")`, `gemini-3.8-flash` → `("gemini-flash","3")`). Lookup now widens exact → same-family-nearest-major → pooled `("*","*")`, one row per stage so nothing double-bills.
- **(4) `cost_usd_actual_estimate` and `cost_usd_predicted` were declared on `LLMCallEvent` in Slice A and written by nothing**, so no row in any `llm-calls.jsonl` in the tree carried a cost. Both now derived centrally in `record_call`, which covers every call site including serve-mode. Response model first, request model as fallback — **every** OpenAI row in the corpus answers `gpt-4o` with the unpriced `gpt-4o-2024-08-06`, so pricing the response model alone would have left the whole provider costless. Predicted reads the lru-cached baselines only, never the JSONL, so the residual `actual − predicted` measures shipped calibration without file I/O per call.
- **Backtested, not merely un-`None`d: 12 complete runs, median forecast/actual 0.95×, 11 of 12 within 2×.** The outlier is FOSSDA at 0.26× — long-form oral history, sessions far above the corpus median — which its own local history corrects to 0.78× on a second run. **The ~3.8 c/session figure in circulation is the median across all 16 runs mixing gpt-4o and Sonnet**; Sonnet-only is 4.96 c and FOSSDA is 14.06 c. That 7× spread is session *length*, not model, so a session-count-only forecast will under-read for long-form work — inherent to the current model, not a defect, and worth knowing before quoting a number.
- **The suite could not see any of it, and one test was pinning the defect.** Every fixture in `TestEstimatePipelineCost` stubbed `_load_baselines` with synthetic cohorts over one or two stages — never the real file, never the per-session + per-run mix a real log has. `test_returns_none_when_no_data_anywhere` read the *real* baselines and asserted `None`, so it went red the moment the file was populated: the fix reads as the regression. New `TestShippedBaselinesServeNewUsers` reads the shipped file and a `run_dir` asserted empty, parameterised from `PRICING` itself so a model added to the rate table without baseline coverage fails there. **All four fixes mutation-proved** — reverted each to its pre-fix form and watched precisely the intended tests go red.
- **OPEN — the standing obligation has no gate.** `design-llm-call-telemetry.md` claims a CI check fails when `prompts.lock` and `cohort-baselines.json` drift; **it does not exist**, and that absence is why the file sat empty across many prompt versions with nothing saying so. Re-running the generator after a prompt reword is a habit, not a rule. Both design docs are trued to say so. Related: the corpus currently blends prompt v0.1.0/v0.1.1, so each row records `prompt_versions_contributing` rather than claiming a single version over a blended median.
- **Attribution note:** the four `bristlenose/llm/` files were swept into a concurrent session's `73e6881d` ("miro failures carry a reason…"), a subject unrelated to this work. Content verified intact on `main`; not amended, per the concurrency rule in root `CLAUDE.md`.

**21 Sep 2026 — `/true-the-docs --claude-pointers`, wave 3b: the orientation layer trued against 0.30.0, and one dead link on a shipped surface.** 151 pointers, ~85 docs classified by three parallel agents, six commits. **The finding that mattered is not a doc.** `Dashboard.tsx:230` still emitted `target: "analysis:section-x-sentiment"` after `c93d9c92` renamed the `TAB_ROUTES` key to `signals`, so the AI-tags stat card took the `?? "/report/"` fallback and put you back on the dashboard you clicked from — working at v0.29.1, shipped broken in 0.30.0 that morning. Fixed with two tests watched red against the shipped string; the general one asserts every stat card reaches the lens its target names, which nothing anywhere had asserted. The static byproduct's identical copy is **correct** and deliberately untouched.

- **The dominant doc failure was the same as wave 1b's, at a different altitude: the summary was wrong while the body was right.** `design-codebook-focus.md` said "Status: proposed. Not a decision" for the whole nine days the cursor has been shipping. `sidecar-transcription-crash.md` said "two defects, neither yet fixed" with one fixed. `design-playwright-testing.md` listed Layer 4 under Future work two months after its own section marked it shipped. `design-ci.md` asked "Why 3.10–3.13?" against a five-cell matrix. Nine docs in one commit.
- **Two orientation-layer defects were actively dangerous.** `docs/ARCHITECTURE.md` told a releaser that `bump-version.py` "creates the tag" — it has not since 14 Aug, and following that line tags pre-bump HEAD, the exact failure the removal ended. And root `CLAUDE.md`'s CLI list offered `transcribe-only` and `render`, neither of which is a command, while its own Gotchas six hundred lines down says plainly that `transcribe-only` exits with "No such command".
- **`serve_report_html()` does not exist**, and three CLAUDE.md files plus `design-react-islands.md` described it as live. It survived greps because the sealed Jinja2 renderer still *writes* the markers it names and nothing reads them. The doc is bannered; **the move to `docs/archive/` is owed** (it wants a pointer sweep).
- **Agents reported six broken paths; one was real.** Three were prose *about* deleted files doing its job, two were a proposal naming files it would create. Worth carrying into the next pass's brief.
- **Owed, in the session's plan note kept outside the public tree**, twelve items — heaviest are `design-export-quotes.md` (Archetype D, "Status: Not started" over routes that shipped in March), `file-map.md`'s JS count (the *fourth* copy of a number CLAUDE.md already calls wrong in three places), a ~60-occurrence lens-name sweep across nine docs, and **`ct()` now has zero production callers** since `baa1aa0e` deleted its one call site. Also **five Codes-menu commands still inert on all nine channels** — a live product call, not a doc gap.
- **One 🔴 for the maintainer's private continuity register:** the PII model-pack HTTPS host has no row. `bristlenose.app/models/` is the Developer-ID acquisition endpoint, driven by `PII_PACK_URL` and probed by the release-ready gate; the DreamHost row covers `/dmg/` retention explicitly and is silent on `/models/`. Same "nothing else reaps it" hazard, unrecorded. Two stale clauses in the Fedora row too.

**20 Sep 2026 — the codebook/autocode doc cluster trued (12 docs), and a graduation gate found breached on public channels and closed.** `/true-the-docs --topic codebook`, wave 1b. Classification **B:1 C:6 D:2 P:3**. The brief predicted Archetype-D-heavy (v1 lens deleted in 0.29.0, so docs describing it are historical) and that was **wrong in the interesting direction** — only two were wholly obsolete. The dominant failure was six docs whose **"Future:" / "Not implemented" / "Goal:" section headings** described shipped features: `design-autocode-threshold-review.md` opens with a `**Goal:**` for work five months live, and `design-codebook-ecosystem.md` had a `## Future: tag display modes — Not implemented` over the shipped eye toggle. That tell is now a trap in the skill; the sibling lesson (inverting a default can orphan a test's real contract) is a CLAUDE.md gotcha. Archived: `design-codebook.md` and `design-codebook-island.md` (the latter's move had been deliberately deferred in August pending a pointer sweep — now a scheduled lane). Seven stale code comments naming the deleted `CodebookPanel` were trued in the same pass, one of which counted three consumers where twelve importers exist.

- **Done, and user-facing at the next bump: `experimental_codebook_lab` is now default-`False`.** It had been default-on through **0.29.0 and 0.29.1** on PyPI, Homebrew, Snap and Copr, so an English-only inline-CSS experiment page has been mounting on every public install for three weeks — exactly what the gate in `docs/design-dynamic-codebook-builder.md` existed to prevent. `BRISTLENOSE_EXPERIMENTAL_CODEBOOK_LAB=1` now opts **in** (the switch inverted). **Gating is on the flag alone, not `--dev`** — so `serve --dev` no longer serves the lab either; a developer who wants it sets the env var. Joins the "owed a CHANGELOG line" list above.
- **Three dead-code findings — (1) closed 22 Sep 2026, (2) and (3) still open.** ~~(1) **`frontend/src/utils/codebookDot.ts` has no production consumer**~~ ✅ deleted, with `.codebook-dot{,-on,-off}` in `theme/organisms/sidebar.css` — the sibling half, which nothing emitted from the SPA, the frozen vanilla JS or the static templates. It was the helper that was dead, not a surface that stopped rendering: the v2 sidebar renders a real platform switch and always did. Its own test was indeed the only thing asserting it, and the three-state rule it pinned had no live subject left to protect. (2) **`frontend/src/shims/navigation.ts:29`** maps `codebookV2: "/report/codebook-v2/"` — a route `router.tsx` no longer registers, so it falls through to a redirect. (3) **`frontend/src/components/LensSubtitleSync.tsx:86`** branches on `tab === "codebookV2"`, unreachable since Swift retired the enum case.
- ~~**Orphan key for the i18n lane:** `codebook.codebookLab` has **zero call sites** anywhere (the entry point went with the v1 lens) while all 21 locales still carry the string.~~ ✅ swept 22 Sep 2026 (`162d86bd`) — it went with the `common.codebook.*` block, one of 1,020 dead values. Closed incidentally, which is why `tests/test_locale_key_readers.py` now exists: the gate lists what is *excused*, so an unlisted orphan is caught rather than waiting for someone to notice it in a capture inbox.
- ~~**Orphan refs recorded for the `--claude-pointers` sweep (wave 3b)**~~ ✅ **done 21 Sep 2026.** All seven live refs repointed at `docs/archive/`; both codebook pointers (root `CLAUDE.md`, `bristlenose/server/CLAUDE.md`) now name `design-codebook-v2.md` and `design-codebook-state-model.md`, the two docs that own the lens. The three refs in `docs/prompts/milestone-5-codebook-island.md` are **left alone by decision** — a Feb 2026 milestone prompt correctly names the doc as it was then, and editing a historical prompt to keep its links current is the trap the skill warns about.
- **Cross-doc count drift, not mine to fix:** `bristlenose/server/CLAUDE.md` says `routes/autocode.py` has **7** endpoints in two places; there are **8** (the cancel route). `docs/design-autocode.md` is corrected; the sibling CLAUDE.md is wave 3b's.

**12 Sep 2026 — the time audit: timecodes, durations and start times across Python, TypeScript and Swift — diagnosed, tiers 0–1 fixed, the container capture shipped, the rest parked with reasons.** `gpt-5.6-terra` returned 63% of quote timecodes exactly 60× too large because the prompt rendered `MM:SS` against a schema asking for `HH:MM:SS`; the same signature turned up in stage 8 under Claude (28.6%) and unpadded Gemini (5.3%), and terra s08 read 0/0/0/11 across four passes — a format mismatch, not a model. Fixed at source (`format_timecode_prompt`, `b89119b2`) with one repair-on-signature guard shared by s08 and s09 (`a7d455d2`, `561c392a`; s09 clamps, s08 drops), and ten projects' cached boundaries repaired in place behind pre-repair snapshots. The audit that followed (`docs/time-defects.md`, twelve findings, every claim labelled measured/inferred) closed everything but H9, H10 and H12 the same day (`8cde1232`, `d6da6ebf`): one parser (`utils/timecodes.parse_timecode` — fullmatch, ≤99 h, unpadded refused on a 344-file measurement), one duration shape, one `# Date:` reader that converts an offset instead of relabelling it, `local_now()` at all four render sites, `parse_iso_lenient` for `Z` and `+0100`. `f903ddca` reads the container: `InputFile.container_meta` (`MediaTimeMeta`, eight fields) from the one ffprobe call ingest already makes, GPS excluded by allowlist. Docs trued in `79b215f9`.

- **Parked — each a judgement call, recorded in `docs/time-defects.md` § 7 and `docs/design-timezones.md` § 5:** **`author`** — measured to carry a real name in the corpus; keep/drop/redact is governance, so it is held in memory only, bounded to 256 chars, until decided. **Tier 2 (§ 5.2–5.8)** — the DB column drops the offset (`server/models.py:220`; `timezone=True` or `AwareDatetime`), so nothing *persists* what § 5.1 captures; the resolver needs a precedence rule (container › filename › birthtime − duration) and a way to report the measured four-hour disagreement; `bristlenose status` shows none of it. **H10 / H12** — `finder_date` has three implementations and zero pinned cases; the zero-duration fork is catalogued in the fixture's `divergences` but no test reads that array. **Tests** — `tests/test_time_tier0.py` and `tests/test_media_time_meta.py` are named for the pass that wrote them; fold into topic files when next touched. **Dead siblings** — `format_timecode_ms` has zero callers; `probe_time_meta` / `probe_duration` are zero-caller wrappers around `probe_media` — the H1 shape again, a helper nobody imports is where the next copy grows. **`SECURITY.md`** — `Cause.message` stays English on the wire by decision and the page does not say so.
- **Owed (user-facing): a CHANGELOG line at the next bump.** Quote timecodes are no longer 60× under terra; the static report's total reads `18h 23m`, not `18 h 23 min`; Miro clip labels past 60 minutes are right; "Generated:" and "Today at" are back on the local wall clock; a transcript `# Date:` carrying an offset now converts. Joins the 22 Aug line below.
- **Not mine, noted:** the two struck items under "What is not done" in `docs/design-llm-live-checking.md` stay in place by the other session's choice; five of its commits sat in this end-session's Scan A window and are deferred for the maintainer (audit log).

**12 Sep 2026 — a task to make the lifecycle SIGINT/SIGTERM cancel tests event-driven turned out to be already shipped; no action, recorded as a re-derivation near-miss.** The brief described the flaky pair (`test_subprocess_sigint_writes_run_cancelled` + SIGTERM sibling) as still parking on a fixed `time.sleep(60)` under a ~240s deadline, asking for the readiness-sentinel rewrite. That fix is on `main`: `time.sleep(60)` → `signal.pause()` in `6f3e3b3a` (20 May), and the `_paused_body`/`_wait_for_ready` sentinel + startup-window guard in `07d6e8c1`/`824655d6` (9 Aug). At HEAD both tests already write a sentinel just before `signal.pause()`, gate on it before signalling, poll the events file via `_wait_for_event`, and carry a **60s** deadline — the only `240` left is a docstring recording the climb-down. The brief was written against the superseded 240s tree (`58041d38`). **Verified rather than re-derived:** 52/52 pass in ~1.3s across 3 runs; the 4 subprocess signal tests pass across 5 runs in ~0.9s; CI shows **40/40** test-matrix cells green across the last four `main` runs (2026-09-12), the historically-flaky `test (3.12, ubuntu-latest)` cell included. Today's three red `main` runs failed on **mypy** and **ratchet** only, both since resolved. No code change, no commit, no bump. The lesson is the one the top-of-`git-log` gotcha just added: a task to *build or fix* something is a claim about the tree, and the tree is one grep away — `grep -n "time.sleep(60)\|240"` answered it in a line.

**12 Sep 2026 — a whole interview was lost from a report four months ago, silently, and the fix for it is half-landed.** FOSSDA s3 produced **zero** quotes while the other nine sessions produced 11–80. Cause: three consecutive 600 s API timeouts (`elapsed_ms=1801455` in `llm-calls.jsonl`; the retry intervals land exactly on 600/1200 s), not truncation — so `_extract_with_split` never fired, because it catches `TruncatedResponseError` only. **Reproduced 7 Sep on `claude-sonnet-4-6`**, same session, first attempt timing out at exactly 600 s; at today's `_CLOUD_MAX_RETRIES = 6` it would hang **70 minutes** before failing. Split in two it succeeds — **522 s + 293 s, 91 quotes after dedupe** (density 1.23/1k chars, right in the corpus band) — so s3 would have been the second-richest interview in the study. Measured throughput 9.10 s/1k chars independently predicts 671 s for the whole session against a 600 s wall.

- **Landed (`6277eabe`):** s09 was marking *failed* sessions complete in the manifest; s08 had excluded them since May and nothing compared the two sites. Both now route through one `_record_session_outcomes`. Tests pin the rule and both wirings, watched red against the pre-fix tree.
- **Landed (second commit, 12 Sep):** the half that makes the retry actually happen. A partially-failed stage was still marked `COMPLETE`, so the next run took the full-cache read and never consulted the per-session records — the first fix was correct and **inert** for a day, and was reported as working. Now: `mark_session_failed` records `StageStatus.FAILED` instead of omitting the session (absence is indistinguishable from not-yet-attempted, so `_derive_stage_status` returned `COMPLETE` from `{COMPLETE, COMPLETE}`), and `mark_stage_complete` derives from its own session map rather than forcing `COMPLETE`. Two-run probe: run 2 re-extracts **only** the failed session, reporting `attempted=3 succeeded=2 failed=1`. Pinned by `test_a_failed_session_is_retried_on_the_next_run`, watched red against **each half reverted independently**.
- **Owed:** the same fix at two sibling sites. **s05** (`pipeline.py:969-972`) — `s05_transcribe.py:198` writes `results[sid] = []` on failure so the failed session *is* a key in `_fresh_segments`, with `_fresh_transcript_outcome` in scope and unconsulted. **Verified 12 Sep that the helper applies cleanly**: silent recordings are *not* in `_fresh_transcript_outcome.failed` — they are computed separately at the rollup from `session_segments` emptiness and added to `_summary.transcripts.failed` only — so applying the helper marks genuine failures FAILED and leaves silent recordings COMPLETE, which is correct (Whisper succeeded; re-running will not help). Held back only because a transcription-resume change needs its own two-run probe, and shipping one without it would repeat the mistake this entry is about. **s05b** (`:1271-1276`) is not a drop-in — `identify_speaker_roles_llm` reports through an `errors` list, not a `StageOutcome`, so there is no failed set to filter on without changing the stage signature.
- **Swift-visible consequence of the 12 Sep fix — wants a look, and I could not run the Swift suite.** A partially-failed stage's manifest status is now `"partial"` where it used to be `"complete"`, and `PipelineRunner.swift` reads that raw string in two places: `stagesCompleteFromStages` (:928) counts only `"complete"`, so such a stage no longer appears in the `.partial`/`.stopped` progress list — correct, it is not complete; and the back-compat `parseManifest` path (:1024) does `if status != "complete" { return .idle }`, so a project **with no events log** whose stage is now `"partial"` reads as never-analysed where it previously read as analysed-with-a-silently-missing-session. That path only runs for projects predating the events log (pre-Apr 2026), and `.idle` is the more honest of the two answers, so this is recorded rather than treated as a regression — but it is a wire-visible change to a mirror nothing gates (see `feedback_swift_suite_is_ungated`). **`test-swift.sh` run 12 Sep — green, 1442 passed, 0 failed.** Note what that does and does not say: nothing in the Swift suite constructs a manifest with a `"partial"` stage, so this is *no regression detected*, not *the scenario is covered* — the back-compat `parseManifest` path needs a project with no events log and is unasserted on both sides.

- **Owed:** split-on-timeout. The judgement call is the trigger — splitting on *any* timeout risks doubling spend on a transient network fault; splitting only above a size threshold needs a threshold that is model- and load-dependent. Note `s09`'s depth-budget comment ("each timeout-bounded") was written for truncation, where a failed call returns fast; a timeout-driven split at depth 3 has a very different wall-clock worst case.
- **Landed:** the guard test's fixture widened from `[]` quotes to real ones. The degenerate value made `mark_stage_complete` refuse on its own empty-content guard, so the stage never reached `COMPLETE` and the short-circuit was unreachable from the test that existed to catch it. **A fixture's zero values can disable the path under test** — now a CLAUDE.md gotcha.
- **Owed (user-facing):** a CHANGELOG entry at next release — "a session that failed analysis is no longer skipped forever on a re-run" is a real behaviour change, and it should not ship as an internal note.
- **Not mine, flagged:** the ratchet is **red on `main`** — `pytest_skip_sites: 24 > ceiling 23`, from `f903ddca`'s corpus-gated skip at `tests/test_media_time_meta.py:81`. Either the skip is justified (same shape as the other corpus-gated ones) and the ceiling moves in a commit that says why, or it does not. Raising a ceiling is a deliberate edit; `--tighten` will not do it.
- Docs trued in `97e730c0`: `design-pipeline-resilience.md` (owning doc — the invariant, spec-vs-as-built), `design-analysis-lifecycle.md` (new **outcome 3c** — stated honestly, then laundered into a success by the next run; 3b's tell provably cannot catch it), `testing/gaps.md` (**G10**, the cross-record gate form).

- **The published sdist ships the whole tree** — measured on 0.29.1 (5 Sep 2026): `desktop/` 438 files, `docs/` 503, `scripts/` 42, `e2e/` 8, none of them the package. Inert (no entry point), but `scripts/release-board.py` now binds a socket and a package reviewer will grep for `http.server`. One `exclude` line in `[tool.hatch.build.targets.sdist]` — **after** checking `rpm/` and `.copr/` read nothing from those trees, since Copr fetches Source0 from PyPI. Surfaced by the release-board security review; the other board judgement calls are in the private review log.

**5 Sep 2026 — the August dependency review, three model defaults moved with live proof, and a check that makes the green lights honest.** The review ran 3–5 Sep (Entry 7 in `docs/dependency-premortem-log.md`): nine Dependabot PRs triaged, Actions pins moved (codeql 4.37.9 as one SHA across init/autobuild/analyze, setup-node 7, download-artifact 8, repository-dispatch 4, `actions/attest` replacing the retired provenance action), `snapcore/*` swapped for its `canonical/*` origin on node24 and the snap republished to edge as revision 14, and the Held register re-grounded — four of its release-predicates could never fire (jsdom's issue closed, thinc's "spaCy 4 GA", a tokenizers cap already floated, snapcore a fork nobody maintains). Python floor decided in a sibling session: `>=3.12` on 1 Nov 2026, band 3.12–3.14 measured by `scripts/check-python-band.py` (`docs/design-python-floor.md`). **Model defaults:** ChatGPT → `gpt-5.6-terra` (strict Structured Outputs for OpenAI and Azure; `max_completion_tokens` gated on a legacy list), Gemini → `gemini-3.8-flash` (both former picker models were retired for new users while the deprecations page carried no date), Claude stays `claude-sonnet-4-6` — Sonnet 5 was tried and reverted after live failures, which the Online-light session later placed at stages 10–11 (quote clustering, thematic grouping) with the real templates; extraction passes. `config.py` now derives from `providers.py`; Swift ↔ Python default parity and every-offered-model-has-a-price are tests. **The live check:** `scripts/check-providers-live.py` makes one real `analyze()` call per offered model with the maintainer's keys — alive, model exists, accepts and returns our formats — gated in `check-release-ready.sh`; the Settings lights validate only the key. Design doc: `docs/design-llm-live-checking.md`. **Owed — filed there, listed here so the tracker sees them. Status re-measured 12 Sep 2026.** _Done:_ (2) the six real stage prompts replaced the invented one (`scripts/live_check_fixture.py`, `e6c0f9ea`), and `scripts/test-providers-live.py` proves the check can go red and cannot pass on a short list (`cc1520b5`); (3) the repair step is in — `_validate_repairing` re-reads a malformed payload only where the API does not enforce a shape, and warns rather than absorbs (`35517db7`, `tests/test_llm_shape_repair.py`); re-measured on Sonnet 5 four times, both failing stages complete, and the default stays `claude-sonnet-4-6` by decision; (4) closed 12 Sep 2026 — the matrix can no longer go silently green: each cell gets a wiped output directory (a reused one made `run` *resume*, calling no LLM while every invariant passed against the previous report — that is why it read PASS from 7 Jul to 4 Sep), and a configured provider that fails is no longer counted green. `tests/test_acceptance_matrix_synthetic.py` drives the real runner against a fake `bristlenose` for 7 scenarios, offline and free, mutation-verified; recorded as **G11** in `docs/testing/gaps.md`; (5) `pipeline_view/catalogue.py` is bound to `providers.py` by two parity tests, `gemini-2.5-pro` gone; (7) the stability corpus ran on `gpt-5.6-terra` and found a defect the shape checks cannot see — minutes rendered as hours in quote timecodes, now range-checked per quote with the prompts speaking HH:MM:SS (`a7d455d2`, `b89119b2`, `51fad05f`; "a fourth kind of provider drift", `6698ad5e`). (1) closed 12 Sep 2026 — the live check now runs preflight's own validator first, with the model under test, before the client is built, and `test-providers-live.py` proves the order offline; the two request builders are checked together, unifying them is still owed. _Still open:_ (6) Azure keeps the legacy shape with `api_version` pinned to 2024-10-21 — now a documented decision in `client.py`, not an oversight; `_MODEL_MAX_OUTPUT_TOKENS` still knows only the two gpt-4o rows; `drop_nulls` still has no guard test; Ollama stays on `json_object` by decision; the "schema sent twice" claim was mis-captured (only the Anthropic path uses a tool) and is dropped; (8) no weekly cloud-matrix run exists anywhere — not in workflows, launchd, crontab or scheduled tasks — and the Mac Settings light is unchanged, still rung 2 (fixed `claude-haiku-4-5` at `LLMValidator.swift:134`, a models-list GET for ChatGPT and Gemini), while the CLI/server preflight light does probe the selected model. Key rotation for the three live-run keys is not verifiable from the repo. **Two more surfaced by the 12 Sep truing pass:** `docs/design-decisions.md:472-478` still cites the Jul stability figures (~9% fragile tail) behind the shipped decision to remove the temperature slider, while `FINDINGS.md` measures 1.6% for the current default *with no temperature pin* — the doc that made the call is the one still quoting the superseded number; and the live check's release gate exercises **2 of the 6** stages, not six — `DEFAULT_STAGES = ("s10:clusters", "s11:themes")` and `check-release-ready.sh` invokes it bare, so the six-template fixture ships behind `--stages`. That is a substantive call (cost per release vs coverage), not a doc fix.

**5 Sep 2026 (night) — the release board, and the feed under it.** Asked for a watching GUI for the whole build-and-release train; researched (prior art: nobody ships vertical-time × platform-grid × effort; six archetypes tabulated), sketched three (`docs/mockups/release-train-{marey,board,scroll}.html`), and the board won on the Tube-map argument — connections and logic, not distance. The plan went through four review lanes and William before a line of code; what it deleted mattered more than what it kept (a regex reader of `release.yml`, a `--ci` flag polling GitHub from the viewer, a `--serve` with a bind address to get wrong, a `steps` verb). **Built:** `desktop/scripts/sink.sh` tees the `@bn` protocol into `.release/<v>/bn-events.log` with `ts=`/`run=`, normalised before `printf %q` (measured: a newline plus an apostrophe used to drop the whole line); `release.sh run` writes its own asserted boundary lines and snapshots `steps.tbl`; `verify`/`status` export the sink and write channel and CI facts; the preflight's rows and the two expiry clocks ride the same file. `scripts/release-board.py` + `release-board.template.html` draw it — no network, no time axis, no data as a third state, a **confounded-expectations log** as the drift guard, `textContent`-only rendering, no log text unless `--with-logs`. Suites: `test-sink.sh` 29, `test-release-board.py` 30 (the real 0.28.0 ledger is a committed fixture), both in `ci.yml`. Real-data proof: the preflight wrote 45 rows, `release.sh status` wrote per-job CI lines into the 0.29.1 run dir, and the board rendered it with every unfed pane saying so. A live ASC key id in `upload-testflight.sh`'s failure hint was found by the review and fixed alone. **Owed:** the driver-vs-board round-trip test; a redacted real `bn-events.log` fixture after the next release; `test-release-sh.sh` still touches the live `.release/` on one path (harmless now, worth a look).

**5 Sep 2026 (evening) — the two "ready" ceilings came off, the inventory reads what ships, and a review of that work found the green lights it could still fake.** `anthropic>=1.0` and `mcp>=2.0` are floor-only now; verified by a full suite on 1.4.0 / 2.1.1 and three live Claude calls through the 1.4.0 SDK, and the "httpx2 wave" turned out to be one member (openai and google-genai were never ceilinged). `THIRD-PARTY-BINARIES.md` and `check-dep-drift.py` now read `.venv-sidecar` — the first honest regeneration moved 31 rows and dropped `en_core_web_sm`, listed for months and never in the bundle. `copy_metadata("starlette")` puts 28 KB of dist-info in the bundle so the next artefact can say which starlette it carries. A five-agent usual-suspects pass then found one defect with four faces — neither script could tell a wrong or empty target from the right one (everything `gone`, no major, exit 0, green preflight row) — closed by one predicate: the target must carry the `bristlenose` distribution. Also: the preflight now says its drift rows describe the LAST sidecar build (a file-age test, not a duplicated fingerprint), the generator's crashes exit 2 rather than masquerading as "stale", and the drift harness can no longer pass while proving nothing. Two rules from the week landed with operators (the restate rule scoped to our own pin files; the abandonment check as the first step of `/cassandra --watch`). `#143`'s bundle overage bisects to **vite 8.0.10 → 8.2.2 alone** (+24 kB gzipped, runtime libs exonerated) — recorded, not decided. Sonnet 4.6 has no retirement clock (Active, not before 17 Feb 2027, 60 days' notice), so the Sonnet 5 retry is on merit. Private review log: findings 15–33 in `docs/private/reviews/dependency-premortem.md`.

**5 Sep 2026 (later) — the seven owed judgement calls, worked. All seven closed; four with code, three with a decision recorded.**

1. ~~**`gates_without_proof` 14 > 13, main's ratchet red**~~ ✅ `scripts/test-providers-live.py` (`cc1520b5`). No ceiling raise: the expensive half of a live gate is the network, and the failure logic is not, so the proof is **automated** — it stubs `check()`, runs offline, and re-proves on every run. Writing it found the gate's own worst failure, which is not a wrong verdict but a **short list**: `shipped_models` derived the set from `LLMProvider.swift` with `if m:` and no else, so one `return` added to a Swift case dropped two of Claude's three models and the run printed "5/5 passed" and exited 0. Enumeration is all-or-nothing now. Proven against the pre-fix script: 5 cases red, including un-enumerable exiting 0 where it must exit 2. Ratchet back to 13.
2. ~~**Sidecar venv's Python minor ungated**~~ ✅ (`f109c65e`). `build-sidecar.sh` derives the interpreter from `.tool-versions` instead of spelling `python3.12` three times — a pin that could not fail loudly, since the only check was "does python3.12 exist" and it does. A venv on the wrong minor is now a rebuild reason (the deps fingerprint cannot see it: pyproject + `pip freeze` are identical across minors), and `check-release-ready.sh` grew the row it lacked — it verified `.venv`, the dev env, never `.venv-sidecar`, the one the bundle is built from. Both arms exercised against a real 3.14.
3. ~~**Ten ignores with no Held-register row**~~ ✅ (`6a92443f`). 16 of 16 covered, checked mechanically against the `dependency-name` list. Five of the ten block nothing today; **two have predicates already MET and are waiting on an audit nobody scheduled** — lighthouse 13 (engines satisfied, no spec asserts an audit id) and jsdom (engines satisfied; the real question is three majors of `localStorage`/`matchMedia` against the shims in `test-setup.ts`). typescript 7 is the one live resolver hold, and typescript-eslint is the hinge of **both** npm chains while having had no row at all.
4. ~~**The scanner disagreement**~~ ✅ (`46b9abb8`). `build-dmg.sh` emits `Bristlenose-<version>.sidecar-sbom.json`, CycloneDX 1.6 from `.venv-sidecar`, hashed into the build manifest. Verified against `pip list` on that venv: 130 components, zero discrepancy. `--pyproject` is what produces a root component at all, and it reads a version pyproject by house rule never carries, so the root is stamped from `$VERSION`. Missing `cyclonedx-py` on a real signing identity is a `die()`, matching build-all step 2b.
5. ~~**`dependabot.yml`'s asymmetry**~~ ✅ decided (`5575763c`), analysis in `docs/dependency-premortem-log.md` § the asymmetry. **It was not one asymmetry but two facts.** **pydantic** is capped by `presidio-analyzer` (`>=2.12.5,<3.0.0`, sole reverse-dep) — a third-party cap we cannot lift, the opposite of anthropic/mcp whose ceilings were **ours**; it belongs in the presidio cluster beside cryptography and numpy and was filed under the wrong reason. **fastapi's ignore is retired** — no cap anywhere, no 1.0 exists, so it had never suppressed a PR, and the file's own policy is "only deps that empirically caused rework". An ignore is silent: it does not defer the 1.0 PR, it means nobody is told 1.0 shipped. Hand-review it via `/cassandra` when it arrives.

6. ~~**`#143` — the size gate was measuring chunk names**~~ ✅ fixed (`f020e234`). The first write-up proposed moving locale chunks to their own directory; a deeper measurement showed a better answer. The old budget was one glob plus 22 filename negations, and it was wrong in **both** directions: `index.html` on 8.0.10 was *eagerly* loading `common-*`, `desktop-*`, `settings-*` and `enums-*`, which the negations excluded — visitors paid for bytes the budget did not count — and then vite 8.2 merged 225 chunks into 211 and the same list started over-counting instead.

   | | 8.0.10 | 8.2.2 | Δ |
   |---|---:|---:|---:|
   | counted by the old allow-list | 207.7 kB | **231.9 kB** | **+24.1 → red** |
   | eagerly fetched by `index.html` | 205.5 kB, 20 chunks | 199.8 kB, 6 chunks | **−5.7 kB, 14 fewer requests** |

   `scripts/check-bundle-budget.py` now sums what the entry document makes a visitor fetch before first paint. Dynamic imports are out **by construction rather than by name**, so the locale split keeps its property and no rename moves the number. It also refuses three ways of scoring well by being broken (unbuilt tree, no JS refs, a reference to a chunk not on disk), and a chunk that stops being lazy now *raises* the number. `size-limit` removed; `npm run size:why` never worked anyway (`@size-limit/why` was never installed, so the flag was silently ignored). Proof: `scripts/test-bundle-budget.py`, 16 cases, offline. Budget held at 220 kB across the definition change so ceiling and definition stay reviewable apart. **Splitting `#143` is still free and correct** — and vite 8.2.2 now passes the gate, so the eight non-vite members are no longer blocked by it.
7. ~~**Snap migration, and the Sonnet 5 retry — both undated**~~ ✅ both dated. **Snap: hold on `canonical/*`** (`5575763c`) — option 1, already in `snap.yml`, revision 14 published to edge under forced node24, and no upstream deprecation says stop. What re-opens it is **arm64**, not 16 Sep. **Sonnet 5: the honest fixture first** (`e6c0f9ea`). `scripts/live_check_fixture.py` carries all six pipeline calls through the pipeline's own `get_prompt_template`, fields, `wrap_untrusted` envelope and Pydantic models. The invented prompt it replaces lied twice — it reproduced a double-encoding the real extraction prompt does not, and was blind to the real failures at clustering and grouping. Default set is now those two stages (also the two smallest prompts, so honest is cheap); `--stages` runs all six. **A vacuous pass is now a fail** — every model here validates with an empty list, which is exactly how a stage ships an empty report. Verified live: sonnet-4-6, haiku-4-5 and opus-5 all PASS on the real thematic-grouping prompt.

   **Still owed on Sonnet 5, unchanged:** the repair step (unwrap-and-retry on Anthropic tool-use and Google, nothing on OpenAI/Azure strict), and a stability-corpus run — one pass is not a run, and clustering and grouping are exactly the two stages that vary between identical calls.

Also found while measuring, and fixed in the same commit: `docs/design-perf-regression-gate.md` claimed a ~267 KB baseline failing over 305 KB, in both its problem statement and its threshold table. Neither number was ever enforced — the real one lived in `frontend/package.json`.


**5 Sep 2026 (small hours) — the CLI and the Mac app kept their keys in different keychains, and the fix found the test suite touching the real one.** `bristlenose configure` writes the login keychain through `/usr/bin/security`; the app writes the data-protection keychain, which `security` cannot address and an unentitled Security.framework caller cannot see (`-25300`, measured). Same service name, two items, neither side reading the other — the split had been documented as a *limitation* since June. Fixed in `bcdc03b9`: the app keeps a login-keychain copy of the five CLI-read keys and reconciles the two copies on read (`SharedKeychainItem`, a UserDefaults ledger, the copy that moved wins); every write on both sides is read back (`credentials.set_verified`; `configure` exits 1 rather than claim a key it cannot read back); credential names come from one table, `providers.py` `CREDENTIALS`, and `configure` had been printing the wrong item name for four of five providers since the product-naming change. **Two things the live runs taught that the design did not:** (1) a keychain read that can raise the ACL dialog must never run from a launch path or a test host — the first suite run hung the runner for six minutes on that dialog, so reads are `quiet` by default and only the Settings pane's selected row may ask; (2) `SettingsRefitTests` renders the real `LLMSettingsView`, whose key field read the developer's CLI-created items with interaction allowed and *wrote one back* — three more dialogs, answered by hand at 22:28 — so under a test host `KeychainHelper` now resolves to in-memory stores, pinned by type. Deleting another tool's keychain item is refused (`-25244`); the doc that said otherwise is corrected. **Owed:** the on-device walk (`docs/design-keychain.md` § "Verifying on a real Mac"; the QA backlog's "CLI interop" line is exactly it). ~~And a decision on `miro_refresh`, stored under an unregistered fallback name the app cannot read.~~ **Decided 5 Sep:** recorded, not changed — registering it renames the item and the `.env` key for existing users. **Still owed, and now self-recording:** whether the CLI's delete-then-add can replace an *app*-owned login item. `MacOSCredentialStore.set` logs a refused delete at WARNING with `security`'s own status, so the next `bristlenose configure anthropic` (the one app-owned item, per the doc's owner table) prints the answer; record it as a row in the doc's probe table. Fifteen design docs trued to the two-keychain reality in `bf93aecf`.

**4 Sep 2026 — branch triage, and a leak that branch deletion was never going to fix.** Closed PR #85 (`security-txt`): `website/` moved to the deploy repo, so the path is dead here — but `bristlenose.app/.well-known/security.txt` **404s**, so the item is still owed. **Carry to the deploy repo:** an RFC 9116 file with `Contact: mailto:security@bristlenose.app`, `Policy:` pointing at `SECURITY.md` on GitHub, a `Canonical:` self-link, `Preferred-Languages: en`, plus `.htaccess` `RewriteRule ^security\.txt$ /.well-known/security.txt [R=301,L]`; set `Expires:` a year out (the branch's was 2026-10-16). **The bigger finding:** the 2 Jun scrub untracked the private docs tree, but nothing gated *pushing* pre-scrub refs — four branches and **23 tags, 16 of them release tags (v0.14.3–v0.15.12, six with Releases pages)** kept it world-readable for three months. Branches and the 7 checkpoint/snapshot tags are now gone from origin. **The release tags and `main`'s own history (125 commits; `git show 7229e180:<path>` works) keep it, by decision** — the history-scrub runbook (private, on disk) stays unexecuted: no credentials, no third-party PII, the specifics already effectively disclosed, ~2,100 SHA citations to remap, and its `--force --tags` step would fire `release.yml` ~110 times. Measurements recorded under the runbook's Step 1. **Guard added:** `scripts/git-hooks/pre-push` refuses any ref whose tree carries an ignored file — *every* ref, because pre-commit's pre-push stage is provably first-ref-only (`_pre_push_ns` returns inside its loop). Native hook, symlink-installed per clone; `scripts/git-hooks/test-pre-push.sh` proves it. **Re-decide the history question at the desktop-repo split** — desktop → private, everything else → public FOSS, i.e. when the desktop app gets a price (memory: `project_private_docs_migration_plan`).

**31 Aug 2026 — codebook v2 got its affordances right, and the user-facing docs turned out to be telling people three untrue things.** The lens work is ordinary and in `git log`. What is worth carrying forward is the shape of what the day's audits found, because all three were invisible to every gate we own.

**`pipx install bristlenose` produced a CLI that could not run.** `serve` is an optional extra, and `_COMMAND_CHECKS` gates **run, analyze and serve** on `serve_deps` — a check returning `CheckStatus.FAIL`, on which `cli.py` raises `typer.Exit(1)`. So the README's primary install line yielded a tool that could only `transcribe`. Homebrew, Snap and Fedora install the extra; pipx and uv were the only channels told not to, and they are what the README leads with and the only route on ARM Linux. `doctor` had been printing the fix all along — nobody reaches doctor before the command they came for fails. Fixed across 13 instructions in README, INSTALL, and the website's install and welcome pages.

**The privacy page said "there is no Bristlenose server".** There is: the feedback form posts to `bristlenose.app/feedback.php`, on by default, and the page's "data leaves the machine **only** in these cases" list omitted it. The payload is genuinely minimal and identifier-free, which is exactly why nobody noticed — the page whose whole job is exhaustiveness had a hole in the list and a denial above it. Its "never in plaintext" credential claim was also wrong for the two shipped channels with no keyring at all (Windows, Snap).

**Two more of the same class, both since fixed:** `how-it-works.md` said "exports use codes by default" when `anonymise` defaults to `false` — a researcher trusting it ships participants' real names; and `the-pipeline.md` said the provider is used at stages 8–11 when `s05b_identify_speakers` also calls it, with up to eight minutes of transcript, **before** PII removal, so `--redact-pii` does not cover that sample.

**The pattern, for whoever runs the next audit.** None of these are code defects. They are documents that were true when written and were never re-read against the thing they describe, and no test in this repo can fail for a false sentence in a Markdown file. The three `user-documentation-review` agents that found them were pointed at code as ground truth and told not to trust the docs — that framing is what did the work, and it is repeatable.

**The v2 swap landed the same day.** `/report/codebook` now serves what was built as v2; v1 (`CodebookPanel`, `CodebookSidebar`, `CodebookTab`) is deleted, the `Tab.codebookV2` case is gone from the enum, and the lens took `tag` back from the temporary `tag.square`. The i18n graduated with it: `yourTags`, `builtIn`, `importCodebook`, `removeFromCodebook` and `projectTagsHeading` are v1's keys, already translated in 21 locales; **Browse Codebooks** took the native menu item's existing translations rather than being seeded; five new keys and two CLDR plural sets were seeded across 20 locales, machine-translated and **pending native review**, same status as the ca/pl/ru/uk/da/sv/nb/tr/nl/fi bulk.

**0.29.0 is out on all nine channels** — PyPI, GitHub, Homebrew, Snap,
TestFlight (3067), the notarised `.dmg`, the website changelog, and Fedora Copr,
which arrived hours after the rest. Two release-machine defects surfaced during
the run and are fixed on `main`; both are the same shape, which is why they are
worth carrying: **a single negative read was treated as proof of absence.** The
TestFlight probe asked App Store Connect seconds after a successful upload, got
an empty list because the index had not propagated, and offered to re-upload a
build number that is spent forever — Apple's own `DUPLICATE` refusal is the only
thing that stopped it. And Copr's SRPM stage ran on **aarch64** against an
x86_64 chroot, so `pip wheel` vendored the wrong architecture; the failure
surfaced inside mock as a resolution error naming `pyyaml` and nothing about
architecture at all. Both now have gates rather than luck: `BN_PROBE_WINDOW_S`
re-reads across the propagation window, and `make-srpm.sh` pins the platform and
asserts no foreign-arch wheel reached the wheelhouse. The Copr fix is proven,
not merely green — the rebuild landed on aarch64 again and produced x86_64
wheels.

**Owed, and specific:**
- **Native review of the codebook-lens seeds** — `frameworksSection`, `reviewProposals`, `onByDefault`, `availableByDefault`, `uninstallDiscards`, and the `tagCount` / `appliedToQuotes` plural sets. The Slavic four-form plurals (cs/pl/ru/uk) and the Finnish partitive are the least-trusted.
- **30 orphaned `codebook.*` locale keys** left by v1's deletion — recorded as `codebook-defects.md` D4, deliberately not swept: a file-wide regex over 21 locale files is how `menu.edit.undo` was deleted from all of them on 19 Aug. Delete by fully-qualified path, diff one locale first.
- **The v2 files are still named `CodebookV2*`** — functionally irrelevant, genuinely confusing now they are simply the codebook lens.
- ~~**The v2 swap itself** — rename to Codebook, decommission v1, restore the rightful sidebar icon.~~ ✅ shipped 31 Aug 2026 (`baa1aa0e`) — verified at HEAD: `CodebookPanel.tsx` gone, `Tab.swift` enum is `project, sessions, quotes, codebook, analysis`, `LensItem.swift:37` carries `.codebook, systemImage: "tag"`. The website needed no second pass, as predicted.
- **The website deploy gap is now larger, not smaller.** Item (5) in § Next session focus said six commits' worth was unpublished since the ~16 Aug rsync; three more landed today (codebook docs, the `[serve]` install fix, the privacy corrections) and the repo holds **21 unpushed commits** in total, most predating this session. One of them is titled *"track the two pages that were written, deployed, and never committed"* — so pushed-vs-serving is not reliably zero in either direction. Worth reconciling before the next `./deploy.sh`, which ships the working tree rather than a commit.
- **The website's own `NOTES-product-discrepancies.md` is about half stale** (dated 25 Jun). It still reports `render` and `--static` in the man page, both long gone. Three items were live; **one closed 12 Sep 2026** — ~~the man page's max-tokens default says `8192` against a real `64000`~~ (fixed, commit "man page: the documented max-tokens default was 8x under the real one"; an audit of all 18 documented defaults found this was the only wrong one, and the page's single model id — `gpt-4o-research` — is a legitimate Azure *deployment* example, not drift). Still live: `classify_flag`'s docstring advertises a `Pattern` flag that never returns, reaching users via `CHANGELOG.md:421` which the site renders live; and spaCy loads `en_core_web_sm` where the docs say `lg`.
- **`cli.py:1469`** — `analyze --output`'s help string describes the inverse of what the function does. The man page is the correct one; fix the CLI, not the doc.


**27 Aug 2026 — the sidebar's status line was audited against the messaging IA, five states were non-compliant, and the fix was wrong twice before it was right.** The visible defect: `.unreachable` rendered five English sentences with no locale key in *any* file including `en`, no `MessageKind`, and no glyph. Every gate was green — `check-locales.py` diffs each locale against English and there was nothing to be missing from; the tests passed throwaway payloads (`"volume gone"`, `"x"`) because the type was `String`, so every string was legal. The sealing mechanism was a `default: return nil` in the one function deciding whether a row draws a glyph, while its three sibling switches over the same enum each carry a written comment forbidding that arm.

**The correction is the more useful half, and it is a process finding.** The first fix invented a rule — *a signifier that names an action must perform one* — and used it to make two glyphs clickable that had deliberately not been, then routed a delta into `NewFilesSheet`, a modal carrying three separate retirement notices. Three times in one day the same move: reading the state of the tree as a specification. A comment saying cantFind's glyph "is a Locate affordance, rendered as a static image" was a decision; a missing route in the AppKit cell was a choice; "the popover is reserved for failure diagnostics" described where the popover had got to. In a codebase the current state *is* the spec, which is what makes programs tractable; in a design practice it is just where you got to.

The rule that shipped is the maintainer's and is better: **a row shows a glyph when there is something the researcher would want to *know* and might not — not when there is something they could do.** `Stopped` and `Transcribed` have an available act and get nothing, because they caused them. `+3 unanalysed` has the same shape of act and gets a blue ⓘ, because the app noticed something that happened outside it. Written into `design-desktop-project-status.md` §"The glyph rule", with `design-analysis-lifecycle.md` and the popover doc trued to match, and drawn 1:1 in `docs/mockups/sidebar-status-vocabulary.html`.

**Owed, and small:**

- **`NewFilesSheet` is one mode from deletable.** `.watcher` is now `ProjectFilesPopover`; only `.copy` has a live route, and it is a completion acknowledgement that probably wants `ToastStore` — which already exists — rather than a modal at all. Neither of the file's two stated retirement conditions has to happen first.
- **`ProjectDiagnosticPopover.showLog()` calls `dismiss()`, which does nothing.** `@Environment(\.dismiss)` is inert inside an AppKit `NSPopover` hosting a SwiftUI view — there is no SwiftUI presentation to act on. Masked because `.transient` closes on the focus change when Console opens, so it has never looked broken. `IconPickerPopover` and the new files popover both use the explicit-close pattern; that one site did not get it.
- **The `/end-session` skill had been prescribing a step nobody needed to take, for two months.** Its desktop-bundle section told every close-out to hand-run `build-sidecar.sh` when the freshness check read STALE. The `Ensure Sidecar Fresh` build phase makes Cmd+R self-heal that, and it landed in `578f13a9` — *"so cmd+r self-heals the bundle"* — on the **same day** `ad474adc` added the manual instruction to the skill. Corrected; the check stays as information, with the three cases where it does still matter named (a prebuilt `.app` run without rebuilding, `BRISTLENOSE_SKIP_SIDECAR_ENSURE=1`, a hand-rolled Release archive). Caught only because the maintainer asked "isn't the sidecar rebuilt by Xcode's clever stale detector?" — the third artefact this session that was confidently wrong and had gone unread.
- **None of it has been seen on a screen.** 1326 Swift tests and a clean build, and the whole session's lesson was that a suite cannot see a rendered surface. The ⓘ against a *selected* row, and whether clicking "Partial completion" now feels right or slightly surprising now that the status text is a click target, are both judgement calls no test makes. Same standing debt as the popover entries below.

**26 Aug 2026 — CI's Python matrix was 5× slower than it needed to be. ✅ Applied 28 Aug (`1c8fd3ee`), after the tag as planned.** Measured on the identical selection: **454.77s → 67.16s, 6.8×**, with exact parity (4244 passed, 5 skipped, 22 xfailed both ways). Coverage was being paid by all cells and read from one; it is now scoped to the canonical cell with `COVERAGE_CORE=sysmon`, and `-n auto` runs everywhere with `pytest-xdist` declared in the dev extra. One caveat survives: xdist-vs-serial coverage *number* parity is unverified, and matters only if coverage becomes a gate — it has no `fail_under`.

_(Filed alongside it: `docs/design-bun-adoption.md` — assessed, parked, **don't adopt**. Closed question, no action.)_

**24 Aug 2026 — the release train was reviewed, five defects fixed, and then deliberately parked.** Findings 38–42 and 44 are fixed and pinned by regression assertions proven red against the pre-fix scripts (`4ac9bc44`, `bfeacd76`). What is left open is a record of what was decided against, not a backlog — `docs/design-release-machine.md` §20 is the account, and the numbered findings live in the review notes kept outside the public tree.

- ~~**Nothing in CI runs any release-chain suite, on any OS**~~ — ✅ **closed 28 Aug 2026.** A `release-suites` job runs all six suites on ubuntu. It called this the single highest-leverage gap and it was right: the first Linux run was red with **33 failures**, both causes real defects in our own scripts (git identity in the manufactured sandboxes; `check-doc-surfaces.sh` unable to read a `FORCE_COLOR`-coloured `--help`). Neither was observable on macOS. `docs/design-release-machine.md` § "The one thing that would most change the odds" has the full account.
- ~~**The suites depend on ambient git identity**~~ — ✅ **fixed 28 Aug** (`a4650edb`), in the suites rather than the runner: a suite that manufactures a repo configures it. Two corrections to the note above, both worth keeping. The estimate of *four* failing assertions was low by a factor of eight — it was 31. And I mis-cleared this precondition before starting the CI work, on the strength of a local run with `HOME` scrubbed and both git configs nulled, which passed: macOS supplies a valid full name from the passwd record, so the identity guess succeeds locally and fails on a runner. **A local simulation of a foreign environment is evidence about the simulation.**
- **The tap workflow copy has diverged from the tap repo again** (`e0e4d0eb` hardened the shape guards locally; nothing pushed them). The preflight's `tap workflow` row says so on every run, which is what it is for. Pushing to the tap is an outward act and was left for you.

**20 Aug 2026 — the day has no session entry; these are its owed items, transcribed from the commit bodies so they are not only there.** Thirty-odd commits landed the workspace 3b fleet, per-window studies, derived agent scope, mcpb schema 2 and the welcome shelf. `/end-session` ran and signed off, but did not write this file, so every obligation below was reachable only via `git log`.

- **A stable per-project key is still owed** (`142df986`). Tool results name their project now, which makes drift visible — but the id still does not mean one thing and names are not unique.
- **The 90s serve-reap grace is a guess and is labelled one** (`4ec46822`). Measure it against how often testers close-and-reopen before treating it as a number.
- ~~**The `.mcpb` staleness class is fixed; the mechanism deserves a re-read** (`f5301982`, `391991c1`). The proxy source was edited, the pack step was never re-run, and every artefact downstream stayed eleven days old through six reinstall attempts **with a green freshness gate the whole way**. One real reinstall before shipping.~~ **Done 21 Aug 2026** — the real reinstall was walked. Keeping the history because the *reason* it was owed still generalises: six reinstalls looked fine while serving eleven-day-old artefacts, and the gate was green throughout, so "I reinstalled and it worked" is the sentence that was already false once.
- **The `≤480px` speaker-name rung still hides both spans**, so the assistive-tech gap survives at the narrowest width. It wants a visually-hidden swap rather than a delete. (Noted in the code; the 750–480px half was fixed in `cb4f1e28`.)

**21 Aug 2026 — Catalan's native pass has an owner and a horizon: J, over the next few weeks.** That is the reviewer obligation `project_catalan_locale_glossary_first` exists to protect, and it is now scheduled rather than open-ended. **It is also a release fork, so decide it deliberately:** 0.26.0 currently carries `ca` as ~1,220 machine-seeded strings on top of a glossary nobody native has read. Either the release waits weeks for J, or it ships `ca` unreviewed and J's pass lands in a later version. Shipping first is defensible — the channels are internal TestFlight and an expiring `.dmg`, and localisation here is recognition, not utility, so an imperfect Catalan that improves is not the same failure as a wrong one that stays. But it is a choice, not a default, and it should not be made by the release simply happening.

**21 Aug 2026 — the MCP Agents pane grew a projects register, and eight reviewers left two questions that are the maintainer's, not theirs.** The table ships (`2741a61c`, `5f5c8eda`, `028d539b`): which studies an agent can read, which could be, sessions, last asked, and a checkbox that revokes. Twenty-nine review findings were fixed in the same pass. **The two that need a decision are recorded only in the maintainer's review log, which is kept outside the public tree** — so they are transcribed here, which is the whole reason this file exists:

- ~~**Does "Readable now" mean permission, or reachability?** (Finding 84.)~~ **DECIDED 22 Aug 2026 — permission.** `design-mcp-extension.md` §5a-ter. `HandshakeExposure`'s own first property already said it ("scope is a permission and serve lifetime is a cache"), and reachability would need a second derivation, which is exactly what the one-derivation rule exists to prevent. The string was the wrong half: **"Readable now" → "Open to agents"**, 2 keys × 21 locales, no predicate change. The predicate is now bounded by a written rule — *a terminal serve state may be subtracted, a transient one may never be* — recorded in the code as well as the doc. **The dependent finding was 88** (should the roll-up announce when it changes): a11y's "announce only when the number moves" is only safe under permission semantics, because under reachability the number moves on serve events the researcher did not cause. **Decided 22 Aug 2026 — it speaks.** Same section. It is parity, not chatter: the count changing already ships as feedback, to eyes only. Three refinements the finding didn't reach — the *grant* direction (re-ticking a receipt) is the stronger case and it framed only the revoke; the announcement speaks the new value, reusing the string already localised in 21; and it speaks at zero too, where the header goes silent but the transition matters most. `NSApp`, medium priority, gated on Settings being key. **Unverified with VoiceOver** — joins the standing debt of never having seen this pane rendered.

- ~~**The revocation receipt is two-way for one visit only.**~~ (Finding 85.) **DECIDED 22 Aug 2026 — keep it as built.** `design-mcp-extension.md` §5a-quater. The receipt prevents a *disappearance*, not an *absence*: a row vanishing under the cursor reads as "I deleted something"; a row not in the list next Tuesday reads as "not shared", which is true and identical to never-shared. Both alternatives refused — an "ever shared" list would make the register an audit history two days after §7a put history in the log, and the undo route cites an 8-second toast+undo **deleted on 19 Aug** (braiding a revoke into `UndoableRemovalStore`'s single pending slot would silently destroy an undoable project removal; and a preference control is its own undo). No timer, for the reason this app already wrote down when it removed the fuse from undo. **What did change:** the receipt row was still `.tertiary` — 1.88:1, unimproved by Increase Contrast — on the one row whose job is naming the project you just revoked, because the recorded fix landed on the wrong arm of the ternary. Fixed, with the scope note that had the same value. The rung was then picked from a side-by-side: **two steps** — rows `.primary`, receipts `.secondary` — the mockup's drawing rather than the flat `.secondary` Finding 64 asked for, which cleared the measurement but flattened the register. The scope note stays `.secondary`; the "Never" cell deliberately does **not** recede by colour.
- ~~**There is no durable record of which agent read which study.**~~ (Finding 87.) **REFRAMED AND DECIDED 22 Aug 2026 — the record exists.** `design-mcp-server.md` §7a; user-facing in `SECURITY.md` § Agent access record. `bristlenose.log` has carried one timestamped `mcp_tool | tool=… | project=… | elapsed_ms=… | result_bytes=…` line per call since the 30 Jul spike (verified against `trial-runs/fossda-opensource`), so "nothing logs what we serve an agent" was false; what was missing was anyone having said so. **`mcp-calls.jsonl` is refused** — it would duplicate that record, inherit the whole re-identification-key obligation, and still not answer "which agent", which is *unknowable* rather than unlogged (`clientInfo` is self-asserted; know the protocol, never the client). The pane's own sentence was the real defect: "no history is kept" is a product-wide negative claim and false, now cut. **Deferred with a trigger:** nothing *surfaces* the record; revisit when a real controller obligation lands (a tester or their client asking for an access record, a DPIA naming agent access, or external beta) — and the answer then is how to surface it, never a client-identity field.

**22 Aug 2026 — the diagnostic popover's reason column was half-translated since it shipped, and the reason it was invisible is worth more than the fix.** The pane resolved header, count line, bucket labels and Show Log through `i18n.t`, and rendered the one sentence that says *why a participant is missing from the findings* in English to all 21 non-en locales. It was not a missed key: all eight refusals share one `category`, the discriminator lived only in the English prose of `Cause.message`, and so there was **nothing to translate from**. `refusals.py` had promised the opposite in a comment since Aug 2026 — *"the user-facing surfaces localise from the reason, not from this text"* — describing a field nobody had added. Now added: `Cause.reason` carries `UnusableReason` on the wire, contract fixture at **v7** with `run_completed_partial_refusals` (the first scenario to pin a refusal at all — the ingest bucket had shipped uncovered by that contract), 8 keys × 21 locales, and the Swift pane keys off the reason with a fallback to the English. Full write-up in `design-pipeline-diagnostic-popover.md` (22 Aug entry) and register row 18 of `docs/i18n-defects.md`.

Three things owed or worth knowing:

- **Not seen on a screen.** Built clean, 4143 pytest and 1283 Swift tests green, including a 21-locale × 8-reason matrix that asserts the pane resolves every string. But that is still a test asserting what someone thought to ask — the entry directly below this one is about exactly that failing. **Set the app to German or Catalan and open a popover on a folder with a refusal in it** (`experiments/folder-of-horrors/synthesise.sh` builds one). The rows are ~1.35× the English width — measured, and recorded as E3 in `design-pipeline-popover-sizing.md`, which is the live constraint on the sizing work.
- **Twenty locales are machine-seeded by me, one is Catalan.** Consistent with the accepted standard (nine locales already ship that way), and `ca` folds into J's pass rather than adding an obligation. Worth a native eye on the two that carry an em-dash clause — `empty` and `no_audio` — where the register is easiest to get wrong.
- **The adjacent defect is still open and I did not touch it.** A run whose *only* failures are refusals still collapses to `.unknown` in the pill and reads **"Run had failures"** — `unusable_input` is absent from `PipelineSummary.pillPrecedence` and from `desktop.pipeline.diagnostic.pill.*`. That is the same false-severity wording the 19 Aug work fixed *inside* the popover and not on the pill the sidebar shows, and it is recorded in the popover doc's own banner. It needs an English string settled before it can be seeded to 21 locales — so it is a copy decision first, not an engineering one.

**22 Aug 2026 — the pane was finally seen on a screen, and the first look found a defect four reviewers and 4,000 tests had not.** The two-step contrast reads correctly and the roll-up is correctly silent with nothing open. But **a receipt row sits under a group header that is false for it.** `AgentProjectRegister.rows` computes `group` from `shown` alone, with no `access` term, so a revoked project keeps the group it had: in the screenshot, *"IKEA with uxfriends — Access turned off"* under **Available when opened**, which promises that opening the window would make it readable. It would not. **The common case is worse** — untick a project whose window *is* open and the row reads **Active** · *"Access turned off"*, i.e. the register saying a revoked project is readable right now, on the surface whose only job is being right about exposure.

Not fixed, because the fix is a design call the maintainer should make with the pane in front of them, and the two candidates differ: the row must **not** move (moving it re-creates the disappearance the receipt exists to prevent — `design-mcp-extension.md` §5a-quater), so it is either *dim the header's promise while a receipt is under it* or *drop receipts to the foot of their own group behind a hairline*. The lean is the second: same section, same reading order, no longer inside a claim that isn't true of it.

Two things this is worth recording for, beyond the defect. **It is the third instance of the pattern `desktop/CLAUDE.md` already names** — a green suite, eight review agents and a hand-built mockup all missed something one screenshot made obvious in a second, because a test asserts what someone thought to ask. **And "absence is information" has a first-run failure mode:** with nothing open the pane prints no roll-up at all, which is correct and decided (Finding 61) — but absence only informs a reader who has seen the presence, and a first-time reader has never seen the line. A ticked row under an unparsed group header can read as "exposed". Not a bug; worth a look when the pane next gets one.

Also owed, smaller: **no arrow-key navigation in the register** — Full Keyboard Access is off by default, so on a stock Mac Tab reaches nothing in that pane; the composed accessibility label recovers most of it and the mockup legend now says so honestly. And **`main` had not compiled for a day** (`f9af3a8e`) because a reader landed in one commit and its definition sat uncommitted in the shared tree — the trap `desktop/CLAUDE.md` opens with, fired for real, and invisible to CI because CI does not build the Swift target.

**22 Aug 2026 — D1 was decided after three months open, and deciding it turned up a live bug.** The May brief asked for a `StageGuard` contract over all 12 stages. **Rejected**, and the reasoning is worth keeping: the premise had decayed (7 of 12 stages instrumented now, not 3), both findings D1 was told to generalise were *already* generalised and better (`design-analysis-lifecycle.md` §5.1 carries five outcomes, not four), and the abstraction would have **regressed** the migration D1 listed first — s05b's empty returns are deliberate degradation, and its heuristic pass runs *before* the LLM refinement, so `empty_is_ok=False` would abandon runs that complete correctly. Rule-of-three had fired (the predicate sits at eight sites) but they differ in the *rollup arithmetic*, not the predicate: it was the **wrong** abstraction, not a premature one. Reframed entry + superseding banner written; full reasoning in the D1 handoff.

What the audit found on the way, and this is the part that mattered: **`run_transcription_only` still carried the pre-fix transcripts rollup.** `6497711a` fixed `run()` on 20 Aug and did not mirror it, under a comment reading *"Mirror the run() rollup"*. A silent recording gave `attempted=N, succeeded=N-1, failed=[]` — **outcome 3, not 3b**: the headline count was honest, and nothing anywhere named the missing files. Second divergence found while fixing: the abandon predicate lacked `and not _silent_sids`, so an all-silent folder raised an *error* where `run()` reports a stated outcome. Both mirrored (`d776059e`), three tests each verified failing on the pre-fix code first.

Then the net that should have caught it (`f28aaf7e`): **`transcribe:no-key`**, the acceptance matrix's first cell that needs no credentials at all — before it, a machine with no keys ran only a committed-fixture read and exercised no pipeline code. It watches the one command cross-bucket continuity can never reach, because `transcribe` *is* the upstream bucket. A **zero-cue `.vtt`** turned out to reach the same orchestrator state as a silent recording, so the cell costs no Whisper and ~3s. Verified red on the pre-fix commit, not merely green on the fix. **Owed and deliberately not done:** the media cell (extract-audio + Whisper + handoff) that `acceptance-matrix.md` has proposed since June is still unbuilt — proving Whisper-returns-nothing-for-real-silence was **decided against** 22 Aug (`59fbf7de`), and that is recorded as closed, not owed, so nobody re-opens it.


**~~21 Aug 2026 — plural agent scope left one premise behind in the code.~~ Closed 22 Aug 2026 — and the premise was the only thing left behind.** The fan-out has been plural since `037b371e`: `ServeFleet.applyEnvChange()` asks per project, `isExposed(_:)` reads the plural `handshakeProjectPaths`, and `ServeEnvStalenessTests` pinned it with a comment that already said so. So "flipping Anonymise now needs to reach N sidecars where the code reaches one" was wrong about the code and right about the comment — the header doc, a sentence mangled by a partial edit, and a stale singular `handshakeProjectPath` reference all survived; all three are trued, and `design-mcp-server.md` §6a records the generalisable half (a comment that states a premise as a *justification* is load-bearing documentation, and it is the artefact nobody re-reads when the premise changes). **§8's `MCP_EXPOSED`/`MCP_DENIED` gate is struck**, not merely still-owed: MCP exposes four tools, not routes, so the route classifier copied from export would gate the wrong surface; the property is already held by the exact-set assertion in `test_tool_schemas_take_no_filesystem_paths`, now commented as load-bearing. Original entry:

**22 Aug 2026 - a documented invariant about moderator codes said the opposite of what the code does, and the code is lossy.** `bristlenose/stages/CLAUDE.md` claimed `m1` in session 1 and `m1` in session 2 were "independent entries in `people.yaml`". They are the same entry. `assign_speaker_codes()` resets `mod_counter`/`obs_counter` every session (`s05b_identify_speakers.py:450`) while `compute_participant_stats()` keys its dict by code alone, so the last session's moderator overwrites every earlier one and `merge_people()` carries the survivor into the file. Reproduced on a two-session study; the line is corrected and now carries the consequences. **Two of those are worse than the lost stats:** the sessions table groups by `computed.session_id`, so every session but the last shows no moderator at all; and because `merge_people()` preserves *editable* fields by key while replacing *computed*, a name typed for session 1's moderator stays attached to an entry whose stats are now session 2's - mislabelling, not loss.

**Not re-keyed, deliberately.** `(session_id, code)` is the honest shape - it is what serve mode already has, where `SessionSpeaker` joins person to session - but `merge_people()` preserves entries *by key*, so reshaping without a migration strands every existing `m1` and the researcher's typed name does not follow to the new key. That is this same failure class, aimed at the one file researchers hand-edit, with files in the field since 14 Jul. Recorded as **Layer 11c** in `design-transcript-speaker-editing-roadmap.md` with the full reader list a migration must touch. What changed instead: the collision is no longer silent - one aggregated WARNING per run naming every reused code and the sessions it spans, pinned by `test_multi_session_moderator_codes_collide_and_are_warned`. One caveat found in passing: `_import_speakers()` fills `Person` name fields via a flat `people[code]` lookup, so serve mode's *rows* are per-session but the *names* it imports inherit the YAML collision.

**Open, and it is a design call, not a mechanical one:** a log line is the weakest available surface for "your report is missing a moderator". Whether this earns a pill or popover message is gated on `design-pipeline-diagnostic-popover.md`'s five-kind taxonomy, and choosing a `MessageKind` is exactly the judgement that doc exists to hold.

**22 Aug 2026 — the export's read-only gate had five selectors that matched nothing, and now it cannot again.** `theme/templates/export.css` is pure CSS, so renaming a control in `frontend/src` defeats a rule with no error — it stops matching and the affordance reappears in every exported report. `tests/test_export_css_selectors.py` now asserts every class it hides is still rendered under `frontend/src`. **Matching is whole-token, and that is the load-bearing detail:** `badge-accept-flash` is a live animation class, so a substring search reports the stale `.badge-accept` as *found* — a naive gate passes the exact bug it was written for. **Three of the five were live leaks, not dead rules:** the hidden-quote counter and its Unhide-all (`.bn-counter`; the real class is `.bn-hidden-badge`), the codebook "+ tag" row (`.codebook-add-tag` → `.tag-add-row`), and "Browse codebooks" (`.codebook-picker-btn`, which had no class at all — one was added). The other two were dead: `.bn-tag-input` → `.tag-input-wrap`, and `.bn-export-btn`, whose control already returns `null` offline. `print.css` is the sibling read-only surface, names the same controls, and had **not** drifted — cross-check it when repointing one.

- **One gap left open on purpose, and it is one condition wide.** `.tag-add-row` is now hidden by CSS alone: [CodebookPanel.tsx:448](frontend/src/islands/CodebookPanel.tsx:448) gates the codebook "+ tag" row on `!isFramework`, where its siblings use the `isReadOnly` computed one line up at :349 (`isDefault || isFramework || isExportMode()`). So the JSX half of the defence-in-depth is still missing and the CSS rule is the only thing hiding it. Left out because it changes the panel's gating logic rather than the export CSS — but note `isReadOnly` is the wrong fix as-is, since it would also hide the row for *default* groups in serve mode; the narrow change is `!isExportMode()`.

**22 Aug 2026 — one datum, four renderings, and the audit that came out of chasing the smallest one.** The Sessions grid rendered a session's length as `26:31`, in a row that also carries a date and a start time — a time of day at a glance. Fixed (`c23734c0`): the grid, the Dashboard session table and the sidebar now use `formatDurationHuman`, matching what the window subtitle, the native sessions popover and the `duration_human` API field already said. Two things the brief did not know: `Dashboard.tsx` was a **fourth** live rendering, and the popover builds `"duration · date"` with the *identical* construction as the web sidebar, so the two agreed byte-for-byte below an hour and diverged above it. Measured against the 236 sessions in the local trial corpus rather than argued — the human form is *narrower* than the timecode it replaced (mean 3.28 chars against 5.09), so terseness argued for the change, not against.

That turned into a register, because duration was not the only one. **`docs/design-shared-formats.md` + `tests/fixtures/shared-format-contract.json`** (`5570b6f4`) catalogue every format duplicated across Python / TypeScript / Swift: which agree, which do not, which differ on purpose. The mechanism is not new — `pipeline-summary-contract.json` has been read by both a pytest and a Swift suite since 7 May; this is that pattern extended from *parsed* contracts to *rendered* ones. The distinction was already in the tree, in `SessionsFinderDate.swift`'s header, which is the exemplar docstring for any new mirror. **Divergent entries are catalogued, not asserted** — a gate that encoded today's drift as correct would be worse than no gate — but both suites check that a divergence is *still* real, so a fix cannot leave the register describing a problem that is gone.

Both gaps it found closed the same day. **`timecode`** (`01f8a83a`): 9 implementations, 3 formats, 8 of them copy-paste twins; a quote card said `05:30` and the CSV of that same quote said `5:30`. Decided — minutes pad, hours never (`05:30`, `1:12:45`): minutes genuinely range 00–59 so padding buys column alignment, hours never reach two digits in research data, and it matches what video editors show, which is the tool researchers trim clips in. **`finder_filename`** (`d78246e7`): one operator — floor against `Math.ceil` on the same 2:1 split — which meant they agreed only on 2- and 5-character extensions, i.e. disagreed on every common media file and agreed on `.docx`. The earlier "1 of 4 sampled" reading was an artefact of the sample. Its docstring was wrong too, claiming an output *neither* implementation produced.

Three things owed or worth knowing:

- **The next version bump owes a changelog line — both changes are user-visible.** A duration now reads `26m` where it read `26:31`, and a timecode is consistent between the report and every export (`05:30` in both, where the CSV used to say `5:30`). Landed after the `v0.26.0` tag, so they belong to whatever ships next; the tag deliberately does not move. _The 12 Sep 2026 time fixes join this line — see that entry's "Owed (user-facing)" bullet._
- **The bundled sidecar is stale** — this session touched `frontend/`, so `desktop/scripts/build-sidecar.sh` (it runs `npm build`) before the next `.app` QA. Not a release concern; CI builds the frontend on a clean checkout.
- **The board card is closed in the tree but not on the board.** The §2 card *"grid duration renders as a timecode — Should"*, filed 14 Aug and struck in the entry below, still needs `/sync-board` to reach GitHub Projects.
- **The lesson that generalises is not about formats.** Changing `timecode` looked like pure display, and it silently broke the transcript reader: `pipeline.py`'s line regex wanted exactly two digits, so a `[1:00:00]` segment stopped matching and was **dropped with no error** — a marathon session would have quietly lost every segment past the hour, with a short transcript the only symptom. Checking `parse_timecode` did not find it; that parser was already tolerant. What caught it was a round-trip assertion in an existing test. **Before changing a rendered format, check whether anything parses it back — every reader, not the one named after the format.** The reader is now `\d{1,2}`, which is also permanent backward compatibility: transcripts already on researchers' disks carry the old padded form.


**21 Aug 2026 — plural agent scope left one premise behind in the code.** Surfaced by a `--topic mcp` truing pass, flagged in `docs/design-mcp-server.md` §6a. `ServeEnvStaleness.swift` justifies its lazy stale-env strategy on *"exactly one project is exposed to an external agent at a time"* and restarts only the exposed instance immediately. Derived scope made that false: N projects can be exposed at once, so flipping **Anonymise** on now needs to reach N sidecars where the code reaches one. That is the governance case the comment itself makes, so it is the one worth not leaving to a later reader. Related, same pass: `design-mcp-server.md` §8 prescribes an `MCP_EXPOSED`/`MCP_DENIED` build gate that has **never existed** — zero occurrences in the tree — and had read as shipped policy since 30 Jul; the doc now says so, but the gate is still owed if the property is wanted.

_The 19 Aug owed list below is otherwise unchanged. Its item 1 — the Re-analyse sheet's pixels — was closed 22 Aug 2026; items 2–4 remain._


**19 Aug 2026 — a 58-file adversarial drop found a fourth failure outcome nobody had named, and the accepted-format list grew 16 → 27.** An acceptance harness drove the real stages over a deliberately horrible folder. First run: **38 ingested, 0 refused, 17 silently dropped, 3 aborting the whole batch** — and 16 of the 17 drops were real media with a real audio track. After: **53 / 5 / 0 / 0**.

**The fourth outcome is the finding.** The brief's taxonomy had three (ingested / refused-by-name / silently-dropped, the last being the only real failure). There is a fourth and it is worse at scale: **one unreadable file aborted the entire run at stage 2**, discarding every healthy session with it. A 42-session folder holding a single zero-byte `.mp4` lost all 42. Cause was a correct principle at the wrong granularity — `_extract_one` deliberately let `AudioToolError` propagate so a broken *toolchain* can never be mislabelled "no audio", but a corrupt *input file* is not a broken toolchain. Split `MediaFileDamagedError` out as a subclass raised only when ffprobe ran and delivered a verdict, with the classifier **defaulting to toolchain-failure when stderr is ambiguous or empty** so fail-loud survives where it belongs (the snap's `libblas.so.3` case still aborts). The specimen was not synthetic: three zero-byte `.mp4` research recordings from 2021–22 have been sitting in `~/Downloads` for four years.

**Stage 1 stopped dropping silently.** `discover_files(dir, skipped)` collects declined files with reasons and `ingest()` logs each **by name**; AppleDouble stays silent, because that is noise the user never created. Outcome 3 is now structurally zero — but note it reaches the **log only**, and the console only with `-v`.

**Five accepted-extension sets, not four.** The brief found four and assessed the fifth as safe; `CloudImportLocalMatch.mediaExtensions` was in fact missing `ogg`/`wma` against its own documented contract. The watcher's narrow set was **drift at birth, not a decision** — separate commits (`0cfc99da`, `10680cbd`), and it *included* `mp4`/`mov`, which disconfirms the placeholder-churn theory. All five now pinned by `tests/test_accepted_extension_parity.py`, which parses the Swift literals so it fails on source drift rather than on a stale copy in the test. It caught the widening immediately, and found the `ogg`/`wma` gap on its own.

**`.ts` is excluded on purpose.** macOS maps it to MPEG-2 transport stream — `mdfind` returns **33,381 TypeScript files** on this Mac as `public.movie`, plus 268 `.d.mts`. Accepting it by suffix would ingest a frontend checkout as recordings. Now in `CLAUDE.md` gotchas and in `design-sidebar-drop-behaviour.md` §Extension-set invariants, which is the first public-doc coverage that invariant has ever had.

**BUILT 19 Aug 2026** (`7a09ce88`). `PipelineSummary.ingest` is the slot that was missing; `bristlenose/refusals.py` is the classifier. Reasons are specific because the remedies differ — zero bytes is a fact from `stat`, and beyond that a truncated recording keeps its container header while a text file wearing `.mp4` has none, which is the difference between "ask them to re-send" and "check what you attached". Correct on all three of the corpus's damaged files. CLI names every refusal; the popover shows ten and an overflow row.

Three things surfaced while building it, all fixed in the same commit: the scan was reporting *our own output directory* as declined (8 refusals on the corpus became 3); the event-size cap listed its buckets by hand and so missed the one bucket able to hold sixty entries; and an unrecognised cause category failed the decode of the **whole** terminus event, which its own `outputTruncated` comment records having happened once before.

**Still owed:** the popover's rendering has not been seen on a real failing run — the Swift decode and the bucket label are unit-tested, the pixels are not. **Same debt, more of it, 22 Aug 2026** (`5a380eab`): the envelope stopped being a fixed 360×320 and now sizes to its content up to a 320 ceiling, so *how tall it comes out* is unseen too. The sizing tests measure through `NSHostingController.fittingSize`, which is the AppKit presenter's path. **The shipping SwiftUI `.popover` was seen on a real partial run the same day and sizes to content correctly** (four refusals, no scroller, no dead space — the case that used to leave ~147pt empty). Still unseen: the ceiling case and the overflow row — `showcase_failed_multi_category`, `showcase_truncated_varied`, then the folder-of-horrors corpus. Also unchecked: what a legacy scrollbar (*Always show scroll bars*) does to the reason column's wrap, which can move the fit decision (`design-pipeline-popover-sizing.md` §6.6).

**Both owed items closed, 19 Aug 2026.** The website page was `docs-src/supported-files.md`, not `cli.md` — and the deploy repo *is* on this machine, so "not reachable" was wrong twice over. It listed 16 formats against 27, and now also says what happens to a file outside the list and how deep the scan goes (the input folder plus **two** levels, not three: `_MAX_SCAN_DEPTH = 3` guards on `depth + 1`). Committed there; the rsync deploy is still owed, as below.

`hello-world-architecture.md` had its second pass. Three cited paths pointed at files that do not exist, §8.4 described an island-injection era that no longer runs and praised the removed fallback, Part 13 told contributors to start Vite in a second terminal (`serve --dev` starts it itself), every number in Part 15 had drifted, and the Epilogue claimed "no data leaves the machine" in a document that spends Part 4 describing the outbound analysis call. The JS-module count is settled by removing it from prose: `_JS_FILES` is the source, `MODULES.md` now names the 10 modules it does not document, and root `CLAUDE.md` points rather than counts.

**Still unverified in that doc, and stated as such in its banner:** the LLM prompt texts quoted in Parts 3–4, and the SQL in §8.2.

---

### 19 Aug 2026 — the analysis lifecycle, and every toast in the app

**Shipped:** the five planned slices (`docs/design-analysis-lifecycle.md` §6 —
detail-pane count, unanalysed-sheet verb, freshest-failure headline, refusal
surfacing, Re-analyse end to end with the counted sheet the mockup drew), plus
three affordances that were being offered with nothing behind them — Analyse on
an empty project, Re-analyse on an unanalysed one, and **Turn On Agent Access on
a project with 0 sessions**, which passed both arms of a gate named for
"analysed" and tested for "has been run at".

**All six `desktop.toast.*` messages removed.** Four drop refusals now answered
by `validateDrop` returning `[]` — no highlight, the operation-not-allowed
pointer, the item springing back — one by a dimmed menu item, and the removal
toast by an undo that no longer expires. Decisions and the HIG citation in
§4.2; the superseded drawings are kept beside the replacements in
`docs/mockups/analysis-lifecycle-states.html`.

**Owed, in priority order:**

1. ~~**The Re-analyse sheet's pixels have never been seen.**~~ ✅ **done 22 Aug
   2026** — rendered at native metrics (`e583105e`), and it yielded the same way
   the popover did: three findings a green suite could not see. (a) The buttons
   broke the HIG on three counts — `.defaultAction` on **Cancel**, so the accent
   fill sat on the escape hatch, left of a red destructive button; fixed, and the
   rule promoted to `design-decisions.md` because it governs every confirmation,
   not this one. (b) The title *wraps* — settled by rendering the layout chain
   through `ImageRenderer` twice, with and without `.fixedSize`, and getting
   byte-identical PNGs (`9dee6868`); the CSS mock's heights were ~7.5pt light and
   are corrected. (c) A failed re-analysis stopped being a failure overnight
   (`1d5639f5`) — `parseManifest` answered `.idle` at its manifest guard without
   reading the events log beside it, so the row went blank on the next launch and
   **Show Diagnostics…**, gated on the failure state, went with it. Pixels and the
   two sidebar rows in `docs/mockups/reanalyse-sheet-pixels.html` and
   `analysis-lifecycle-states.html`.
2. **Nine `toast.show(` call sites survive** in flows nobody reviewed —
   feedback, copy errors, cloud import. The rule applies to them; the decision
   hasn't been taken.
3. **Three design docs still propose toast-based undo**
   (`design-project-sidebar.md`, `design-sidebar-tag-assign.md`,
   `design-quote-triage.md`). None is built. `design-undo-debt.md` asks for one
   sweep, not per-doc.
4. **Whether a failed project should accept dropped files at all.** The refusal
   is unchanged — only its grammar moved from a toast to the ⊘ pointer — but
   copying files into a project whose run failed is arguably harmless and
   useful. Not decided.
5. Non-English strings for the sheet are machine-seeded, pending native review.
6. **`humanCategoryLabel` is still English-only** — a pre-existing gap the cz
   i18n review flagged (Finding 13, deferred): it is the single source for both
   the UI label and the English-only copy payload, so localising it in place
   breaks the plaintext. Needs a UI-vs-plaintext split, not a translation.
7. ~~**Unexplained:** the Sessions table's ID column skips numbers.~~ **Answered
   and fixed the same day (`6497711a`).** Fifteen gaps, not nine: four were the
   damaged files already named, and eleven were sound effects and test tones
   that transcribed to nothing and were counted as *successes*. Session ids are
   assigned contiguously at ingest, so a gap always means a session was created
   and then lost. **The gaps stay** — deliberate for alpha: a researcher's
   folder will not contain eleven sound effects, and a gap makes a failure
   visible. What was missing was something explaining each one, which is now
   the `NO_SPEECH` row in the diagnostic.

**18 Aug 2026 — the child window is built, and the first run on screen raised a question that may replace it.** Seven commits, 1138 desktop tests green, ship-blocker closed: a child holds a lens rather than a project and reads its title from the serve, so it cannot name a study it isn't showing — unrepresentable rather than guarded. Decisions, states and menu enablement are in `docs/design-workspace.md` §"What a child window is" and `docs/mockups/window-master-child-states.html`. **The open question is whether master/child survives:** a relaunch bug briefly gave every window a project list, and the reaction to that state — keep the list everywhere and accept that clicking jumps all windows — describes a simpler peer model that closes the same defect with no role, no sidebar omission and no menu gating. It was a reaction to a bug, so it needs asking rather than acting on; the argument both ways is in the `child-windows-outstanding` brief among the maintainer's private handoffs. **Also owed:** the lens-row and project-row context menus (the last specced piece), and 32 untriaged findings from the first review this area has ever had.


**18 Aug 2026 — the Accounts pane was localised the same day it shipped, and the gate that missed it has a second hole worth knowing.** 21 keys (`desktop.settingsTabs.accounts` + a 20-key `desktop.accounts`) across all 21 full locales; `zh-Hant-HK` untouched and inheriting. `AccountAttention.sentence` now takes `I18n` like `CloudPlatform.windowTitle(_:)` — the state engine stays a pure value and the locale arrives with the render. **The finding:** `check-locales.py` diffs each locale *against English*, so **an absent `en` key can never be reported missing** — a surface never enrolled in English is invisible to it by construction, and `--strict` does not help. Distinct from the allow-list hole below (31 Jul entry); both now in `CLAUDE.md:230-231` and `design-i18n.md`'s gotchas. `design-cloud-import.md` §10 had predicted this recurrence in writing on 16 Aug and it landed in 48 hours. **Owed:** (a) the 21 locales carry a machine seed — same native-review standing as the other machine-seeded waves; (b) **`docs/platform-text-map.md` is three sweeps behind** and wants a mechanical pass — its Desktop-only table covers 8 of `desktop.json`'s 22 sections and ~150 of ~598 keys (`cloudImport` 120 and `ollamaSetup` 43 both absent), says "20 full locales" in four places, and its `dt()` table claims 4 keys where 1 live call site survives; regenerable from `en/desktop.json`, noted in the doc itself. (c) One genuinely open question, parked in `docs/i18n-defects.md`: shipping this English verbatim into 21 locales did not apply *settle the English first* — does "settled" mean "no longer being edited" or "reviewed and signed off"? The rule needs the wording either way.

**18 Aug 2026 — Settings ▸ Accounts SHIPPED, and the credential storage under it was rebuilt. Six review agents found four ways to lose a sign-in; all fixed. Read §7/§9/§10 of `docs/design-cloud-import.md` — they were trued in the same pass and two of them said the opposite of what the code now does.**

**The pane.** A section per service — Teams, Meet, Miro — with four states derived from disk and **no network call**: unavailable / not connected / connected / needs attention (two causes). That "no call" property is the design, and its consequence is the thing to carry forward: anything knowable only from a call must be *persisted when that call happens*. Google's account tier is free (it falls out of the address domain, so a consumer account is flagged before the researcher goes hunting an empty list); **Microsoft's `DriveTier` is not, and is the one state still owed a writer** — a personal Microsoft account still reads as merely connected. Pinned by a test named for the gap so it reads as owed rather than broken.

**The storage.** `KeychainHelper` keyed every item on a fixed account string, so a second sign-in took `SecItemUpdate` and **overwrote the first account's token in place** — no error, `set` returned `true`. §7 named this a year ago. Now one item per account, `kSecAttrAccount` a SHA-256 of the address, with enumeration and a one-shot migration that deletes the legacy item. **The address is the key, not `oid`/`sub`** — parsing the id claims was cut for v1; a rename produces a second row that is visible, labelled and removable. **iCloud sync STAYS** (§7 and §10 said non-synchronizable; both are now corrected): the stolen Mac is the realistic loss event, iCloud Keychain is E2E regardless of ADP, and `kSecAttrSynchronizable` cannot be flipped by `SecItemUpdate`, so the migration was the only cheap moment it could ever have changed.

**What the review found, and it was not the pane.** The finding logged **LOW** was the one that defeats the feature: `try? await client.refresh()` made a dropped connection indistinguishable from a refusal, and a tombstone strips the refresh token — so **a moment of bad wifi permanently destroyed a working sign-in and told the researcher their provider had revoked them**, on the aged-out-token path, which is the ordinary next-morning case. Four more of the same family, all one root (a key derived from a value that could regress): identity nil-ing on a transient `/me` failure and deleting the correctly-keyed item; the rekey deleting the *other* account when you signed in as a second one; Google learning its address on first listing and never publishing it; Teams having no backfill at all. Also: disconnect did not stop an in-flight transfer (`store = nil` cancels nothing), and Miro's disconnect left a working token in the sidecar's **baked env** and in the parked sidecar.

**Owed, in order** — full detail, including the one genuinely open decision, is in the `accounts-outstanding` brief among the maintainer's private handoffs, kept outside the public tree. (a) Microsoft's `DriveTier` verdict persisted onto the grant, which lights the fourth state for Teams. (b) A second account is stored but **invisible and un-removable** — `connections.first(where:)` collapses it, and sync makes two reachable today with no Add Account button; cheapest interim is to log when `connections` holds more than one. (c) `SECURITY.md` item 5 is done, but the **anonymisation-boundary rewrite in that same file is another session's uncommitted work** — do not clobber it.

**One thing that is not about accounts at all: `main` did not build the Mac app.** `CloudPlatform.swift` shipped in `327f11c3` reading `BristlenoseFlags.cloudImportZoom`, whose definition was never committed — it sat in a working tree, so every build here passed and a fresh checkout failed. CI does not build the Swift target. Fixed (`c837f8b5`), and the habit is worth keeping: **a shared working tree hides missing commits**, and building in a throwaway worktree at HEAD is what surfaced it.

**Vendor glyphs are gone, and the "lawful SF Symbol" line below is superseded.** The 16 Aug entry says the generic SF Symbol is "the lawful answer, not a stand-in". Lawful it is — but that was never the argument for *having* one. A stand-in glyph for someone else's product is there because we can, not because it helps; beside the vendor's name it reads as their mark. Removed from Settings ▸ Accounts **and** from `File ▸ Import` (where they were the only `systemImage` in the entire menu bar), and `CloudPlatform.symbolName` is deleted. The real marks would need licences from all three vendors and that paperwork is **settled against** — nice to have, not worth three trademark conversations.

**16 Aug 2026 — a live Google tenant took the transcript out of v1, and found the docx parser ingesting AI summary as testimony. Four sessions were open on Google/Teams; they have been COLLAPSED and the docx half is committed. Read this before touching either.**

**The docx work is LANDED — the four sessions have collapsed to this.** `afeb7e5c` (design-doc truing — §3 gains a Google-mechanics block and a tier gate, §5's "strongest artifact position" inverted in place, §6 gains the observed specimens), then **`29f5d9be`** (the code: `s04_parse_docx.py`, `s01_ingest.py`, `pipeline.py`, `tests/test_ingest.py`, `tests/test_parse_docx.py`, `tests/fixtures/platform-transcripts/`) and **`a3b37daa`** (the prose: the CHANGELOG/README entry, and `stages/CLAUDE.md` trued — it had been describing the bug in the present tense, citing `matched_headers`, the `< 2` guard and line 54, all three of which the fix changed). Verified `3962 passed, 83 skipped, 22 xfailed` against a 3925 baseline, repo-wide `ruff check .` clean, run twice. **The tree still holds two unrelated bodies of work** — the welcome-screen changes (Swift, frontend, `export.css`, `glossary.md`, `docs/design-welcome-screen.md`, mockups, and the *other* CHANGELOG/README bullets) and a `people.py` + `test_people.py` change. Both commits above were staged hunk-by-hunk to keep out of them; **still no `git add -A`**, and note `commit -- <path>` reads the worktree, so it is not a safe shortcut on the three files all three bodies touch. The fixtures are **derived** from two files in `~/Downloads` that are not durable — re-capture rather than reconstruct.

**A refusal has to be *recorded*, not merely raised — that was half the fix.** A docx-bearing session sets `has_existing_transcript`, so s02 skips audio extraction and leaves `audio_path` `None`, which *also* excludes it from `needs_transcription`. Raising from the parser would therefore have swapped a loud-but-wrong parse for a session that vanishes from the run with nothing recorded anywhere and an abandon check that can never fire — the same silent drop `docs/design-cloud-import.md` §6 exists to forbid. `_gather_all_segments` now emits a `StageFailure` (`MISSING_INPUT`, `source_file=<basename>`) and counts the lost session in `attempted`. Deliberately widened to **subtitle** parse errors in the same pass: identical `except: log` ten lines above, identical silent drop. `Cause.message` is bristlenose-authored and never quotes document content — it reaches `pipeline-events.jsonl`, a named re-identification surface.

**One gap left open on purpose, and it is a product call, not a bug.** A Meet Doc whose Transcript tab is *empty* is **untimed**, so it falls past the timecode refusal into `_parse_plain_paragraphs` and its Gemini summary would still be ingested. Closing it needs either the localised `📝 Notes` / `📖 Transcript` headings — the exact fragility the parser is built to avoid — or refusing **every** untimed `.docx`, which would also reject transcription-agency Word files (`Interviewer:` / `Participant:` prose, no timecodes), a real UR input the man page's ".docx (Microsoft Teams exports)" doesn't cover either way. Decide it on whether agency `.docx` is a supported input; don't close it by reaching for the headings. Two smaller ones alongside: **`p.runs` carries bold on the speaker name and both parsers still read `p.text`** — unused because the mandatory preceding timecode already does the discriminating, but it is the stronger signal if the pairing rule ever needs relaxing; and Teams' `<Name> stopped transcription` marker is absorbed into the final turn, pinned as a *visible* test so it stays a known cost rather than folklore.

**`_GMEET_TAIL_RE` requires a trailing kind and never reads it — keep it that way.** The recording and the notes Doc carry **different** timestamps (22:45 = meeting start, 23:02 = when Gemini finished writing), so the whole dated tail must go; normalising the timestamp still leaves two keys and still makes two sessions. The trailing kind is mandatory because it stops a title that merely ends in a date being truncated, but its *text* is never matched — "Recording" and "Notes by Gemini" are localised per tenant, and enumerating the English words is precisely what cost `_TEAMS_SUFFIX_RE` six months. `tests/test_ingest.py` now has a Google Meet observed-specimen block beside the Teams one; a case below either marker must be a filename seen on a real tenant, with tier and capture date.

**The decision: Google's transcript is out of scope for v1.** Import the video, let Whisper do the words. It is not a file — Google saves it as a **tab inside** a Doc named `<title> - <timestamp> - Notes by Gemini`, beside a Notes tab holding Gemini's summary, and a Google Doc has no canonical byte form (`files.get?alt=media` fails outright; every `files.export` rendering **flattens both tabs into one stream**). Measured: 19 paragraphs, **one** of them speech, the rest Gemini's summary, its own UI chrome, and an attendee email address. Descoping removes the Docs API, the tab walk, the `documents.readonly` question and the contamination risk in one move, and §8 has never established that a platform transcript beats our own transcription anyway. **The consequence to take deliberately, not by default:** this also removes the main argument for `meetings.space.readonly`, so v1 could run on `calendar.events.readonly` + `drive.file` + Picker alone — which additionally serves the paying *consumer* accounts (Google One Premium records; the Meet REST API is Workspace-only and blind to them). What that costs is the pre-scoped Picker: no `conferenceRecords` means no `driveDestination.file` id, so `file_ids` can't be populated and the researcher browses rather than confirming a ticked set. **Decide it on list UX, not on the scope bill.** Full reasoning in `docs/design-cloud-import.md` §3.

**The bigger finding is Teams, not Google, and it was in shipping code.** A `.docx` paragraph is **not a line** — Teams packs an entire turn (the `Martin Storey  0:04` header *and* every line of speech) into one paragraph separated by `<w:br/>`, which `python-docx` renders as `\n` inside `p.text`. A parser whose unit was the paragraph saw one unmatched blob per turn, so **real Teams transcripts also parsed to segments at `start_time=0.0` with no `speaker_label`**. Every constructed fixture put one line per paragraph — the shape the bug cannot occur in. The generalising lesson, now in `bristlenose/stages/CLAUDE.md`: **the unit of structure is the LINE, never the paragraph**, and any new fixture must carry the real intra-paragraph breaks.

**Still open on Google, and one recorded call closes both.** Does the Picker surface a Meet recording (never confirmed by Google's docs or any community report), and what does an *attendee* see in their own Drive after the July shortcut change — the 16 Aug call was solo, so nothing has been observed. **Invite a free `@gmail.com` to a recorded call and both close together.** Nothing has completed a download on any platform yet; treat every transfer-path claim as untested.

**Tenant facts worth not re-deriving.** Recording needs **Business Standard or higher** — free accounts can't, and Business Starter can't *despite costing money*; a wrong tier yields an empty list, not an error. Transcripts are **on by default with no admin toggle** (recording has one, transcription doesn't); the host starts it in-call, and its absence from Admin console is not evidence the edition lacks it. The tenant sits on a throwaway domain taken at signup, deliberately **not** `bristlenose.app`, whose MX carries live DreamHost mail with a `-all` hard-fail SPF. **No OAuth client is registered in any Google Cloud project** — `GoogleMeetConfig` still returns `nil` — and when one is created it must live in a Cloud project owned by an account that outlives the tenant: the throwaway domain is a *data source*, not the app's identity.

**16 Aug 2026 — Zoom PARKED behind a flag; its two out-of-tree blockers banked on the way out.** `File ▸ Import ▸ Zoom` is withheld by `BristlenoseFlags.cloudImportZoom` (default off, `defaults write app.bristlenose BristlenoseCloudImportZoom -bool YES`) so Teams and Meet reach releasable quality first — a scheduling call, not a design one. Nothing deleted or commented out: the adapter, OAuth client, transfer policy and tests all still compile, and Zoom keeps its Diagnostics fixture rows (that menu reads `allCases`, deliberately). Filtering happens at one chokepoint, `CloudPlatform.shipping` — now `offered(zoomEnabled:)`, a pure injectable rule, with `built` as the unfiltered list — so any future import surface inherits the parking instead of re-deriving it. **Ingest of Zoom _files_ is untouched** (`s01_ingest.py` filename + folder-pattern recognition, reached by drag-drop). **Banked and durable:** `com.apple.developer.associated-domains` = `webcredentials:bristlenose.app` on _both_ build configs, and the `apple-app-site-association` staged (not deployed) in the website repo at `content/.well-known/` with a scoped `.htaccess` forcing `application/json` — verified the apex serves 200 with no redirect and that `deploy.sh`'s `cp -R content/.` carries the dot-directory. **`webcredentials`, not `applinks`** — the `.https(host:path:)` callback validates against that service specifically and an applinks entry fails by _hanging_, silently. **Two claims in the 15 Aug entry below are now stale:** the Zoom `associated-domains` entitlement exists, and `CFBundleURLTypes` was never required at all (corrected in the design doc on 15 Aug evening — Teams signed in without one). **Owed before Release ever archives:** the `Bristlenose Mac App Store` profile must be regenerated with Associated Domains on the App ID — Manual signing, will not self-heal, and will not complain until someone archives for TestFlight. Unpark sequence, free-vs-paid boundary and UK cost research: `cloud-import-zoom-parked.md` in the maintainer's private handoffs.

**16 Aug 2026 — vendor branding for `File ▸ Import` designed, nothing built (`860aa0b2`, `docs/design-cloud-import.md` §9a).** Brand here is **three** surfaces under three instruments, and the design was drifting toward the one that is forbidden: the **sign-in button** is granted and specified, the product **name** is nominative use and free, and the product **icon** needs an express licence from Microsoft, Google and Zoom alike. So a Teams/Meet/Zoom glyph beside each `File ▸ Import` item can't ship, and `CloudPlatform.symbolName`'s "placeholder in every case" comment is **backwards** — the generic SF Symbol is the lawful answer, not a stand-in. Rule proposed: the mark appears once per flow, at the authorisation moment, nowhere else. **Two claims in the tree are wrong and cost nothing to fix:** `CloudPlatform`'s "Zoom has its own" branding spec (there is none, and Zoom's Marks licence reaches **SDK Apps only** — we are an API app, so it confers nothing), and, one surface over and shipping **today**, `provider-google.imageset` is a monochrome Gemini spark where Google explicitly forbids monochrome marks, and `provider-azure.imageset` is the Azure logo redrawn. Owed beyond assets: ~~**Settings ▸ Accounts** — Microsoft *requires* a way to sign out and switch account, `CloudImportSource.swift:55` already cites the "one place to disconnect" rule, and that place doesn't exist; it is the same work as §7's `(platform, account)` keying.~~ ✅ **both shipped 18 Aug 2026** — the pane and the `(platform, account)` keying, exactly as this line predicted they were one job. Microsoft's sign-out requirement is met; *switch* account is not (see the 18 Aug entry above). The `symbolName` claim in this same paragraph is also superseded: the glyph is gone, not corrected. Two traps recorded in §9a: the vendors' whole-button **image lockups** can't be localised or made accessible (build native, use the standalone mark), and an asset-catalog imageset left on `template` rendering intent silently converts a full-colour mark to the forbidden monochrome one. Unverified: Microsoft's redlines are a PNG, so the mockup's `41px`/Segoe-UI numbers are the mockup author's, not the guidelines'.

**14 Aug 2026 — latent gender-agreement bug in every Romance `pipeline.json` (found during the Catalan review; NOT fixed, wants native calls).** `pipeline.json`'s `stageComplete` is `"{stage} <past participle>"`, and `{stage}` interpolates the 11 labels from `cli.json` → `stage.*`, whose grammatical gender varies. The participle is masculine singular in all five Romance locales, so it disagrees wherever the stage noun is feminine: **it 9/11** (`{stage} completato` — every `-zione` noun: Acquisizione, Trascrizione, Generazione…), **fr 8/11** (`terminé` — every `-tion` noun), **es 5/11** (`completado` — the `-ción` nouns; es/pt escape more only because half their stage labels are nominalised infinitives, which is its own register inconsistency), **pt-BR 4/11**, **pt-PT 4/11**. **30 broken renderings.** The morphology is mechanical, not a judgement call — `-zione`/`-tion`/`-ción`/`-ção` are feminine, `-mento`/`-ment` masculine — so the *defect* is certain even though the *fix wording* is not. **Not urgent, and that is exactly why it must be fixed now:** the `pipeline` namespace has **zero live consumers** — `LAZY_NAMESPACES` in [frontend/src/i18n/index.ts:31](frontend/src/i18n/index.ts:31) is `common`/`settings`/`enums` (+`desktop`), `I18n.swift:43` the same four, and no Python `t("pipeline.*")` call exists (the `t("pipeline.…")` hits in `SettingsModal.tsx` are a **key-prefix collision** — those keys live under `settings.json`, per `docs/design-i18n.md:244`, not in this file). `cli.json` is equally dead, so the interpolands are latent too. Nothing renders it today; wiring the namespace up would ship all 30 at once. **`ca` is already correct by construction** and is the model for the fix — `"S'ha completat {stage}"` puts the participle *before* a postposed subject, where it stays invariable; the generalisation is **never interpolate a noun into a slot that governs agreement**. Do not machine-translate a replacement: this needs the Italian reviewer (`project_boss_italian_formal_ia_reviewer`) and equivalents, and the Catalan locale must not be touched. **Same family, same file, also unfixed:** fr `stageStart` is `"Exécution de {stage}…"` → *"Exécution de Importation"*, missing mandatory elision on the 4 vowel-initial labels. **Adjacent terminology, flagged not fixed:** pt-BR/pt-PT leave `piiRemoval` as the English acronym *"Remoção de PII"* where it/es/fr/ca all localised it; and `done` leaves "Pipeline" untranslated in all five while `ca` localised it to *"el processament"* — a glossary call, not a bug. **No gate catches any of this** — `scripts/check-locales.py` validates placeholder *sets*, not grammar, and could not in principle.

**14 Aug 2026 — Sessions lens navigation: native switcher popover replaces the embedded sidebar.** Four commits — `0f1d0c32` (additive core), `7187df8a` (rewiring), `6de94c29` (first-live-QA fixes), `53e8d7aa` (the removal, and the **sole revert target**: reverting it alone restores the sidebar *and* every path to it, leaving popover+sidebar as the fallback). The Sessions toolbar slot now presents an `NSTableView(.sourceList)`-inside-`.popover` switcher — real selection capsule via the now-shared `SourceListSelectionRowView`, menu-role hover wash (a recorded reversal: container idiom beats control idiom), token-matched type-select, chooser interaction (click commits+dismisses, arrows highlight only, Escape closes). ⌘⌥L on Sessions posts `.showSessionsSwitcher`; lens activation restores the last session route (`SessionsRouteMemory`, cleared on run completion because positional ids renumber). Browser and all other lenses untouched — the maybe-someday extension to other lenses is recorded as explicitly not-yet. Full decision record: `docs/design-sessions-popover-navigation.md`; the 49-finding plan review is at zero open. **Owed:** Edo badge colorsets (`SidebarPalette` badge concepts fall back to system semantics until generated from `palette-edo.css`); native-review flags from the locale seeding — ja observer オブザーバー vs 観察者 and ko participant 참가자 vs 참여자 (pre-existing divergences, flagged not reconciled), it *"Tutte e {{count}} le sessioni"* construction; two §2 board cards filed (session "Start" is file birthtime — Must; ~~grid duration renders as a timecode — Should~~ ✅ shipped 22 Aug 2026 (`c23734c0`)); `#N` removal from the Sessions index parked.

**6 Aug 2026 — Sessions lens is a responsive CSS grid; one i18n follow-up owed.** The raw `<table>` at `table-layout:auto` is gone (`9f1f8058`) — replaced by `theme/organisms/sessions-grid.css`: container queries on `.bn-session-table`, `grid-template-columns: subgrid` rows, a speakers/start shrink pair, and a narrow-width ladder that sheds surnames → user journey → names entirely (the code badge never moves). Pure CSS, so it works from `file://` in an export and needs no JS width measurement. Design + evidence in `docs/mockups/sessions-grid-playground.html` (variants A/B/C, `004cd6a9` + `83d4c25a`). **Owed:** the new `sessions.colJourney` key was machine-seeded into the 19 non-en locales and wants native review — same standing as the existing machine-seeded pl/ru/uk/da/sv/nb/tr/nl/fi set. Also unregistered in `bristlenose/locales/glossary.csv`, which is where agreed QDA/UX terms are supposed to live for translators. Two specificity traps are now documented in `theme/CLAUDE.md`: `.bn-session-duration` and `.bn-session-meta` sit in the templates layer, which concatenates AFTER organisms, so bare `.bn-cell-*` rules tie and lose on source order — both shipped silently wrong in the first cut and were caught only by measuring computed style.

**8 Aug 2026 — the release is scripted end to end; Transporter is out of the loop.** `build-all.sh` used to stop at a signed `.pkg` and tell you to drag it into Transporter — the one irreversible, outward-facing step, done by hand, recurring every 90 days as TestFlight builds expire. Now: **`check-pkg-shippable.sh`** (gates the `.app` *inside* the `.pkg` — today's gates c–f read the xcarchive copy, so nothing had ever checked the shipped bundle; verified `pkgutil --expand-full` round-trips signatures) with **7 negative fixtures** proving each assertion fails on its own violation; **`upload-testflight.sh`** (gate as unskippable precondition, `--upload-package --wait`, `PROCESSINGSTATE` parsed as an allowlist, then an *independent* `--build-status` because altool can exit 0 on a build that never lands); **`bump-version.py --build-only`** (re-ship a marketing version with a higher build — the 90-day refresh had no lever, and the old path died on `git tag` *after* rewriting three files); **`check-release-ready.sh`**; and **Tier 0 CI** — `ci.yml` now builds wheel + sdist + `twine check` + asserts the SPA is actually inside the wheel, and `snap.yml` regained push/PR triggers. **0.25.1 (2450) is on TestFlight** (expires 5 Nov) and **Snap edge is at 0.25.1 rev 7**, both verified against the vendor rather than a green tick.

Three things worth carrying: **`--validate-app` catches duplicate build numbers server-side in ~24 s on a 644 MB artefact that never transfers**, which retired a planned local pre-flight built against a query altool doesn't have (`--build-status` takes a build number as *input*; there is no enumeration verb). **Restoring snap's push trigger would have silently armed a dormant publish clause** — `publish-edge` read `(main && push) || workflow_dispatch`, dead since May, and would have started auto-publishing to edge on every commit. And the snap build **passed first time**: whatever broke it in April had been incidentally fixed, so the trigger sat off for ~4 months guarding a failure that no longer existed. A parked trigger is a decision with no expiry date on it.

**3 Aug 2026 — pipeline-view catalogue caught up with the desktop Ollama picker (step 1 of 4; 2–4 parked post-TF).** The desktop shipped a curated four (`llama3.2:3b`, `gemma4:e4b/26b/31b`) into `OllamaCatalog` on 30 Apr 2026; the Python side never followed, so `pipeline_view/catalogue.py` still claimed local meant one model. Two surfaces on the same Mac disagreed for three months and nothing failed — because the **de-facto source of truth was a markdown table**: `OllamaCatalog`'s doc comment says it mirrors `LOCAL_MODEL_RAM` in `docs/design-gemma4-local-models.md`, and that constant **does not exist in Python**. That doc now carries a load-bearing-code warning at the top. This session added the three Gemma models to the catalogue with `min_ram_gb` (8/24/32, matching Swift) plus `pipeline.reasons.insufficient_ram` across all 20 full locales — reused verbatim from each locale's already-reviewed `ollamaSetup.chooseBody` ("…need more memory…"), so the Pipeline view and the setup sheet read in the same words. **`llama3.2:3b` deliberately carries no `min_ram_gb`**: the predicate is `memory_gb is not None and >= value`, so a floor on the fallback model would make it vanish on exactly the hosts where RAM detection failed (Swift can afford `minRAMGB: 4` because `ProcessInfo.physicalMemory` always returns a number; we have a `None` case). **Remaining, deliberately parked until after TestFlight:** (2) a mechanical parity gate binding `OllamaCatalog.curated` ↔ `catalogue.py` (test-only, so it reuses the tag rather than bumping); (3) CLI deriving from the catalogue — **a 48 GB M4 Max CLI user still gets `llama3.2:3b`**, and `ollama.py`'s `PREFERRED_MODELS` still has no gemma entry and no `"gemma"` prefix check; (4) emitting the catalogue as bundled JSON so Swift *reads* it rather than mirroring it — the exact mechanism `I18n.swift` already uses for locale files. Step 3's **default** change is additionally gated on the design doc's still-open issue #2 (Gemma 4 structured-JSON reliability unvalidated against the five pipeline prompts) — worth confirming whether that validation happened before the desktop shipped 26b/31b, since the doc's table still lists it as MUST-DO. Sibling open item, unrelated but adjacent: `catalogue.py:149` still displays "Opus 4" and `pricing.py` has no Opus row (Opus 4.8 P2, overdue since ~18 Jun).

**31 Jul 2026 — two i18n gaps seeded across 19 locales; three pre-existing defects and one terminology split left open.** `c2fd38d3` filled `export.scope.*` (`en`-only) and 19 `desktop.json` keys missing from **all** 19 non-en locales — `menu.project.analyse` had been rendering English "Analyse" in every locale's project context menu. Gap 1 needed no translation: each locale already carried the same label set, reviewed, as `menu.quotes.copyScope{All,Selected,Starred}`, reused with `{{count}}`→`{{n}}`. **The gate that should have caught this is an allow-list, not a parity check** — `tests/test_pipeline_diagnostic_locale_keys.py` verifies hardcoded tuples, so a new `en` key never obliges anyone to extend them; the three seeded keys inside its remit were added, but _the systemic fix is a real en→locale parity test_ (flatten `en/<ns>.json` vs each locale, fail on any missing key) — needs a decision about which divergences are legitimate (`zh-Hant-HK` is deliberately partial; some keys are en-only by design). **Amended 18 Aug 2026: that proposed test is what `scripts/check-locales.py` already does, and it would not have caught the Accounts pane** — flattening `en` and diffing against it cannot see a surface `en` never had. It is the right fix for *this* entry's class (a key in `en`, missing elsewhere) and no fix at all for the other one. Build it if you want the gate, but do not book it as closing both. **Left open, deliberately:** (1) `export.scope.starred` renders a *different word* from `export.colStarred` in the same file for **cs, uk, it, zh-Hant, ko** — in cs/uk it's actively ambiguous ("Označené"/"Позначені" = marked, sitting beside "Vybrané"/"Вибрані" = selected); fixing means either breaking the web/menu parity or editing shipped native strings, so it's a taxonomy call, not a seeding fix. (2) Three pre-existing defects an `i18n-review` pass surfaced: fr `menu.quotes.star` is a noun ("Favori") toggling against an imperative "Retirer des favoris" in the same menu slot ([MenuCommands.swift:862](desktop/Bristlenose/Bristlenose/MenuCommands.swift:862) flips them); nl `menu.project.reAnalyse` is infinitive where its whole group is imperative; and ru/uk `chrome.newProjectSaveMessage_one` fires at 21/31/41 (CLDR `one` = n%10==1) with no `{{count}}` in the string, so 21 dropped files reads as singular — that one needs `{{count}}` added to the **English** source first. (3) Worth verifying against applelocalization.com: `chrome.addFilesPrompt`, `chrome.newProjectSavePrompt`, `chrome.newProjectSaveTitle`, `menu.file.addFiles` are all panel-button / menu strings Apple has canonical per-locale wording for.

**28 Jul 2026 — Cloud/storage policy DECIDED + a live read bug found.** Nine storage models worked through (iCloud sync, auto-tiering, per-project keep-local, Cloud Archive, File Provider extension, proxy/transcode, LRU budget, archive-as-truth, do-nothing); policy landed at **`docs/design-project-storage.md`** with a yes/no/maybe/avoid table (paired with **`docs/design-cloud-import.md`** — see below). Answer: **BN manages nothing** — no tiering, syncing, transcoding, evicting or deleting; it reads whatever folder it's pointed at and stays useful when media is absent. **The actionable half is a real bug, not a feature:** uncoordinated reads of *dataless* (cloud-evicted) files can fail with **`EDEADLK` / errno 35** _[4 Sep 2026: errno is 11 on macOS — 35 is Linux's; and this app runs materialisation ON, so it blocks rather than errors — see `design-copy-error-surfacing.md` §5]_ instead of materialising — affects iCloud, OneDrive/SharePoint, Google Drive, Dropbox, Box, i.e. the modal researcher setup — and BN does uncoordinated `open()` + ffmpeg subprocesses with 30 s timeouts (so a fetch reads as a corrupt file) + blind `copyItem` (the known force-quit hang). Fix list: `NSFileCoordinator`-wrapped reads Swift-side / materialise-then-read Python-side; free-space precheck before a run + legible `ENOSPC`; never blind-copy a dataless source; stay useful when source media is gone (dogfood walk not yet done); make "missing after analysis completed" **neutral, not a warning** — today it nags the researcher for the one act that frees their disk. Also corrected two long-standing planning figures: a study is **~26 GB, not 500 GB+** (99% of corpora are 720p platform recordings at ~1.3 GB/hr, not 4K local screen captures), and "they can re-fetch from Teams/Zoom" expires (Teams default 60–120 days). Contradicts the "macOS materialises on read — the CLI is right to do nothing" line in `docs/design-desktop-project-status.md`; that doc wants truing. Same session: tempered the local-first/privacy framing out of `CLAUDE.md` + `docs/ROADMAP.md` (it isn't the stance, and "nothing is uploaded" is false — the analysis *is* an outbound LLM call).

**15 Aug 2026 — cloud import BUILT, all three platforms; nothing registered, so nothing works yet.** ⚠️ **Header and four claims below corrected 18 Aug 2026 — see the inline strikes.** This entry carried the same stale summary the design doc's status block did, which is the point worth keeping: the rot lived in *two* summaries, and truing one would have left the other reassuring. `docs/design-cloud-import.md` (trued five times now, most recently 18 Aug). Teams, Google Meet and Zoom live behind `File ▸ Import` on one spine — `CloudImportSource` / `CloudImportStore` / `CloudImportWindow` / `CloudPlatform`, plus `CloudDownloader` doing §6's verified transfer for all three. ~~**The gate is registration:** no OAuth client exists on any platform, Teams needs a `CFBundleURLTypes` entry…~~ **Corrected 18 Aug:** Entra (15 Aug) and Google Cloud (18 Aug, `2cb58cf6`) both exist and both sign in; `CFBundleURLTypes` was never required (`ASWebAuthenticationSession` routes the callback to the initiating session, never through LaunchServices). **The gate moved to *verification*** — Microsoft Publisher Verification (minutes, once the business-identity check clears) and Google's sensitive-scope review (weeks). One item to start, not two, and Google sets the date. Zoom still needs its `associated-domains` entitlement + a deployed AASA, and is parked behind `BristlenoseFlags.cloudImportZoom`. ~~Also owed: no token is persisted (in-memory only, ~1h access tokens, no `refresh` call), and no live adapter derives the `stat`-based row state — all three hardcode `.notImported`.~~ **Both shipped.** One Keychain item per account through a single writer, renewed before the listing *and* the fetch; and the row-state derivation moved up into `CloudImportStore.rebuild()`, where it serves every platform at once — it is a fact about the destination folder, not about any vendor. Still owed there: the 2s duration tolerance has never been checked against a real Teams pair, and that failure direction withholds a file with no override. Research repriced Google — `drive.meet.readonly` IS restricted as feared, but `meetings.space.readonly` (sensitive) + `drive.file` (non-sensitive) reach the same artifacts, so **zero restricted scopes and no CASA**. Zoom shipped public-client PKCE Apr 2026 but rejects custom-scheme redirects, hence the AASA. ~~Four~~ **Three** open probes (the Picker one was answered 17 Aug by a completed round trip), each ~1h against an account we don't have: Google's Picker-vs-Meet-recording, Microsoft's tenant self-consent, Graph's per-file `expirationDateTime`, and whether Teams drops the transcript beside the `.mp4` (that last one would revise §1/§3/§5). Buildable now, blocked on nothing: transport tests against a fake `URLProtocol` — there is no stub anywhere in `desktop/`.

**29 Jul 2026 — cloud-eviction bug REPRODUCED end-to-end; fix list is small and exact.** Real project on Dropbox, three 307–649 MB screen recordings, Make online-only, Analyse → **run fails** with `ffprobe timed out after 30s probing <file>` (red ✗ on the row). Control case — same project, files made available offline → runs clean. Only variable is residency; nothing wrong with cloud paths, sandbox, or pipeline. **The message means the opposite of what happened** — researcher reads "my video is broken", truth is "my file was still downloading". Evidence + recipe now in `docs/design-project-storage.md` §3 "Reproduced end-to-end". **Fixes, all small:** (1) `is_dataless()` pre-check (`os.stat().st_flags & 0x40000000` — cross-provider, works today) before ffprobe/open in [audio.py:48](bristlenose/utils/audio.py:48) + [audio.py:141](bristlenose/utils/audio.py:141) (the second *raises* `AudioToolError` and kills the run; the first only warns); (2) on dataless → report **info** "Fetching from <provider>…", **no wall-clock cap** (measured: 90+ s after requesting the fetch, zero bytes had landed — no timeout value works); (3) same guard before `copyItem` Swift-side (the known force-quit hang); (4) soft hint only after several minutes ("still fetching — is Dropbox running?") since BN cannot distinguish a healthy fetch from a stalled one. **Two structural findings alongside:** `.inCloud` is *unreachable* — [ProjectAvailability.swift:141](desktop/Bristlenose/Bristlenose/ProjectAvailability.swift:141) short-circuits on `fileExists` of the **folder**, which is true even when every file inside is evicted, so the cloud glyph can never appear for evicted media (explains "I've never seen it"); and [ProjectFolderWatcher.swift:228](desktop/Bristlenose/Bristlenose/ProjectFolderWatcher.swift:228) *already* calls `isCloudEvicted` per file and throws the answer away with `continue` — one line turns it into `evictedFiles` and gives whole-vs-partial for free. **Also:** the provider classifier [ProjectIndex.swift:650](desktop/Bristlenose/Bristlenose/ProjectIndex.swift:650) is an allowlist of three — **Google Drive and Box fall through to `.local`**; should match `~/Library/CloudStorage/` structurally and take the first path component as the label. **Progress is not observable and that's permanent** — Dropbox swaps files in atomically (measured ~50% on Finder's pie with `st_blocks` still 0), `percentDownloaded` is deprecated, `globalProgress` is per-provider aggregate → indeterminate spinner is the honest shape, never a ring. Detection itself is fine: `SF_DATALESS` and `ubiquitousItemDownloadingStatus`/`ubiquitousItemIsDownloading` all work **cross-provider** (verified on Dropbox, not just iCloud). Minor: the app's "Show details" panel shows three import-machinery frames and **no exception type or message** — the one line that matters only appears in the per-project log; that alone made this hard to diagnose.

**28 Jul 2026 — cloud import designed (nothing built, post-TF).** `docs/design-cloud-import.md`. **BN holds captured originals, never references** — a Teams/Zoom link can vanish, so pull the bytes and own the retention clock. Doesn't need a server: a user-initiated fetch is OAuth **public client + PKCE** (`ASWebAuthenticationSession`, Keychain refresh token — the Miro shape). **Teams is unusually clean:** non-channel recordings land in the *organiser's own* OneDrive `/Recordings`, and the researcher schedules the interview — so `Files.Read` (delegated, own data, user-consentable) covers the modal case with **no admin consent**, and Graph metering ended 25 Aug 2025. Never reach for `Files.Read.All` / `Sites.Read.All` (admin-only since Aug 2025 = the procurement gate the ICP avoids); never `drive.readonly` on Google (restricted scope → CASA, $500–4,500/yr) — `drive.file` via Picker instead. **v1 = `Files.Read` only:** list `/Recordings`, parse title+date from the filename, single-select, download into the selected project, store `driveItem` id + account per session (dedupe + retry + provenance, and **already-imported state belongs in v1** — it does most of v1.1's reliability work). v1.1 = multi-select + per-row outcome (reuse `partial`) + resumable. v2 = `Calendars.Read` for surname/@domain/description search + attendee roster — **which could retire the pre-PII-redaction speaker-ID LLM call** that `SECURITY.md` currently has to disclose. Then Zoom (Unlisted app, review), then Google (sensitive-scope verification). Key mechanics: calendar is the index (Graph can't filter by attendee → pull a window, filter client-side, instant type-ahead); read `expirationDateTime` per file and sort soonest-expiring so the list is a triage queue ("expires in 11 days" — MS expiry emails are now a per-tenant setting, so researchers may not be warned); 30-day default window; join calendar↔recordings so the list only offers meetings that have something to fetch (also stops BN holding a general diary). **Priority is the video file; transcript/roster/brief are gravy — and never substitute the platform transcript for Whisper** (real-time ASR, weak diarisation). Manual drag-drop stays first-class forever: organiser-owns-it breaks for guests in a client's tenant, which is a real freelancer case.

**28 Jul 2026 — a11y: verify the WKWebView is not a keyboard trap (menu escape REMOVED).** View ▸ Move Focus to Projects (⌘0) is gone, along with its whole chain — `.focusProjects` notification, `ContentView.focusProjectsList`, `firstSidebarTableView`, `focusLog` (~2.2k chars across 4 files). It existed as a "§10.1 keyboard no-trap" escape, but **a menu item is not an accessibility affordance** — a keyboard user stuck in the web view will not discover the fourth row of the View menu, and WCAG 2.1.2 asks for "unmodified arrow or tab keys, **or other standard exit methods**", which means the platform's own (⌃F6 / ⇧⌃F6 pane cycling, ⌃F2 menu bar, Tab exiting at document end) — not a bespoke command. ⌘0 went to **Actual Size**, which is what every Mac app binds it to and what this file already documented as WebKit's own meaning. **The task this leaves:** with Full Keyboard Access on (⌃F1), Tab into the report and confirm ⌃F6 and Tab both escape the WKWebView back to the sidebar. If they don't, the fix is the window's **key-view loop** (`nextKeyView`, ensuring the sidebar `NSOutlineView` is reachable), not another unread menu row. Tell that the old item was papering over this: its implementation carried a "no sidebar table view (collapsed?) — focusing content view" fallback and warning logs, which is a workaround's shape, not a design's. Also now orphaned: `desktop.menu.view.moveFocusToProjects` across 20 locales.

**28 Jul 2026 — Check Health wired → native Health window.** New serve endpoint `GET /api/doctor` (bearer-authed, deliberately NOT auth-exempt — exposes env detail; runs `doctor.run_local_checks`, the local non-network subset) feeds a native `DoctorReportView` (`health` Window scene, `.commandsRemoved()`), opened from Diagnostics ▸ Check Health via `openWindow(id: "health")`. Committed: backend + view + BristlenoseApp scene + tests + two trued design docs (`99c95551`). ~~**Two pieces left uncommitted, entangled with the in-progress menu-icons branch — land them there:** (a) the `openWindow` menu-wiring line in `MenuCommands.swift`; (b) the orphaned `desktop.menu.app.checkHealth` key removal across 20 locales + the `fix-the-menus.md` update.~~ ✅ **both landed** — verified 1 Aug 2026: `MenuCommands.swift:124` carries `openWindow(id: "health")`, and `checkHealth` (like `moveFocusToProjects`) is absent from all 20 locale directories. The `menu-icons` branch they were waiting on no longer exists. **Deferred follow-ups (noted in `docs/design-diagnostics-menu.md`):** an async pass to surface the network-bearing checks (API-key validation, reachability, Ollama probe) the endpoint currently skips; and native-only checks (Keychain access, sandbox entitlements, bundle integrity) this window is the future home for. Minor: the `fix` text is CLI-flavoured (`get_fix()`) — accepted for v1.

**26 Jul 2026 — dead-code / vestigial audit (catalogue only, nothing deleted).** Five parallel agents swept Python · frontend · theme · desktop · cross-cutting for deletable tails after the old-toast find. Highest-confidence, zero-risk deletion sweep queued: **(1) old-toast lineage** — `AutoCodeToast.tsx` + `AutoCodeReportModal.tsx` (both never mounted, superseded by ActivityChip / ThresholdReviewModal) + `atoms/autocode-toast.css` (never served) + 6 `autocode.report.*` locale keys ×20 — _`AutoCodeToast.tsx` + its test deleted 12 Sep 2026; four of its sixteen cases were coverage nothing else had (three of them `autocodeFailure()`'s only assertions anywhere) and were ported to `ActivityChipStack.test.tsx` first. **Still open:** `AutoCodeReportModal.tsx` (re-verified 12 Sep — still rendered only by its own test), `atoms/autocode-toast.css` (confirmed the sole atom on disk absent from `_CSS_FILES`, so never served — now with no possible consumer), and 6 orphaned `autocode.toast.*` keys ×21 locales (only `donePartial` survives, reused by the chip)._; **(2) frontend orphans** — `AboutDeveloper.tsx`, `useAppNavigate.ts`, `getColorTheme`, 4 dead `api.ts` fns; **(3) Python** — `output_paths.py` static-linking block (4 fns) + `_ensure_index_symlink`, zero-ref helpers in `text.py`/`timecodes.py`, unwired config `whisper_device`/`merge_speaker_gap_seconds`; **(4) repo cruft** — root PNGs (`overlay-mode*.png`, `push-*.png`), `interviews.png`, `bristlenose-todo-summary.md`, `scripts/populate-gh-project.py`, `scripts/compare-render.sh`; **(5) spike** — `scripts/spike_discussion_routing.py` + its 2 prompts (confirm the discussion-routing spike is abandoned first — new untracked files). **Two decisions, not mechanical:** desktop AppKit-vs-SwiftUI sidebar split (+ reconcile the `desktop/CLAUDE.md` contradiction: code defaults to SwiftUI, doc claims AppKit shipped); and the inert `focus-change`/`undo-state` bridge handlers (wire the missing JS emitters, or delete + the now-permanently-disabled desktop Quotes/Edit menu items). Docs half already trued this session (`0be5e712`). Also flagged but out of `--topic` scope: `CLAUDE.md:216` + `DEVELOPMENT.md:200` still call `design-react-migration.md` the "active migration plan" — it's complete; fix via `/true-the-docs --claude-pointers`.

**16 Jul 2026 — 🐟 stage-2.5 `.dmg` LIVE.** First notarised Developer-ID `.dmg` cut, shipped, and validated end-to-end: download from **bristlenose.app** → clean *"Apple checked it"* Gatekeeper (source=Notarized Developer ID) → launch → full analysis (IKEA project, 28 quotes / 3 themes). Signing spike solved + documented: a sandboxed app + Keychain-Sharing entitlement forces a provisioning profile, so forcing Developer ID at *archive* fails — the working flow is archive dev-signed → export Developer-ID with `-allowProvisioningUpdates` (Xcode auto-mints the profile, no portal). Mechanics in **`docs/design-dmg-build.md`**; `release.md` trued (bump-version.py flow + mandatory gates + desktop channels). Cert minted via Xcode ▸ Manage Certificates — **back it up as a `.p12`** (login-keychain password had diverged; the private key is the only unrecoverable piece). **Open follow-ups:** (a) website git commit is entangled with a concurrent docs-overhaul session — left for that session (CTA is live + on disk); ~~(b) **atomic scp upload** (temp name → `mv`) so a future cut never serves a partial file mid-upload (a naive re-download hit "disk not readable")~~ ✅ **shipped 4 Aug 2026** (`c018a3e9`) — `desktop/scripts/upload-dmg.sh`: stages to a dot-prefixed name **in the target dir** (`mv` is only atomic within a filesystem), verifies the sha256 of what landed, `chmod 644` before the rename (a new inode gets `0666 & ~umask`, so the old mode is NOT inherited and the download would 403), then swaps. Gated on `check-dmg-shippable.sh` as a precondition. Used for real on 0.24.0; ~~(c) **unified release doc** + the critiqued **`cut-a-release --patch|--minor` skill** (map all 5 channels — PyPI · Homebrew · Snap · `.dmg` · TestFlight; the doc is the honest prerequisite)~~ ✅ **shipped 8 Aug 2026** — the doc first, exactly as the note insisted: **`docs/release-channels.md`** maps all five channels, their triggers and their two different expiry clocks (`.dmg` 30 days from the *build*; TestFlight 90 from the *upload*). Then **`/bn-release`** (`docs/design-bn-release-skill.md`), which is thin-skill-over-fat-shell: the scripts own every irreversible act with their gates internally, so the skill cannot skip one, and its unique job is the prose. **The skill has never run** — acceptance criteria were written first, at `docs/testing/bn-release-acceptance.md`.

**14 Jul 2026 — 🐟 TF-1 DELIVERED.** First TestFlight build accepted by App Store Connect: **0.20.0 (2068)**, 223 MB model-less `.pkg` (large-v3-turbo downloads on first transcribe), "ready for internal testing", now in Apple processing. Cleared ASC validation after three nested-signing fixes (app-sandbox on sidecar/ffmpeg/ffprobe; `org.python.python` framework re-sign; `LSApplicationCategoryType`). Build/signing already merged into **local main** (`ed5ef885`) — `testflight-prep` branch is spent. Local main is ~26 commits ahead of `origin/main`, **all unpushed**: pushing main publishes the App Store signing config (sandbox flip, ASC fixes, entitlements) to the public repo — a launch-narrative call, not a routine push. First tester (Paul) invited. **Next TF work is no longer "upload" — it's add remaining testers + run cohort-feedback calls.** Whisper delivery decided = Background Assets (essential tier); brief at `docs/private/handoffs/background-assets.md` for the parallel branch. PII/Presidio stays post-TF.

**11 Jul 2026 — v0.20.0 shipped (incremental builds).** Curation survives re-analysis (freeze marked quotes, membership-based section identity, star-anchored theme names, dismissible "New" badge); desktop loose-file intake + incremental add (drop / File→Add Files ⇧⌘A) + run recovery; native feedback sheet; sessions-journey deep-links; Shoal adaptive count. Bumped 0.19.0→0.20.0 (feature release = minor bump; convention now in CLAUDE.md). Live on PyPI + brew. **Desktop half reaches the cohort only with the next bundled-sidecar build — not yet done.**

**7 Jul 2026 — Acceptance-testing tier (Phase 1).** All test docs under `docs/testing/` (hub + `coverage-inventory.md`); built format-coverage + invariant harness + lens smoke (`bc5a036a`, folded into 0.20.0). Open: real Teams/Meet `.docx` parity fixture; firing local/cloud provider cells (`run_matrix.py --run-local` free / `--run-cloud` = keys+spend); `launchd` nightly wrapper (Phase 3); the **media cell** (extract-audio + Whisper + handoff), still unbuilt. _Amended 22 Aug 2026: `transcribe:no-key` joined the free tier (`f28aaf7e`) — the first cell needing no credentials, so a keyless machine now exercises real pipeline code instead of only re-reading a committed fixture._

**Launch plan:** `docs/private/100days.md` — triaged by topic + MoSCoW priority. That's the source of truth for what ships. This file is a public capture inbox + session context — antechamber for untriaged items only; promote to the plan doc once triaged.

---

## Next session focus

_Rewritten 1 Aug 2026 — the previous block described the 14 Jul state (waiting on Apple processing, which cleared) and predated 0.21.0 / 0.22.0 / 0.23.0._

Sprint schedule (S1–S6) ended 30 Jun; the summer is deliberately unsprinted, S7–S14 scope in September. Active focus is still **run the cohort** — and on 22 Aug 2026 the blocker was *probed* rather than derived, which moved it again. **The premise this paragraph carried since 1 Aug — "the testers are on build 0.20.0 (2068)" — has no referent.** App Store Connect holds one beta group, "Cohort 1 from Spain", with **one tester: the maintainer**. Nobody from the 5–10 UR veterans is enrolled, so TF-2 and TF-3 were both being scoped for an audience that does not exist in ASC. **The gate is enrolment, not the build** — and enrolment was correctly waiting on the summer holidays, not dropped.

**The ordering constraint is the half that still binds.** `hasAccessToAllBuilds = true` on that group, so whoever is added receives the newest non-expired build automatically — which means a build has to *precede* the invitations, not race them. The live build is **2472 (0.25.3)**, uploaded 10 Aug, expiring **8 Nov** — 78 days of runway, so there was never any expiry pressure driving a rebuild. It does **not** carry the month's work: multiple windows, cloud import, the MCP Agents register, ingest refusals, Re-analyse, Welcome, Catalan. So the sequence is **build, then enrol, then call**: cut and upload **2501 / 0.26.0** (`build-all.sh` then `upload-testflight.sh`; 2501 is already written into the pbxproj), and only then send invitations. The full probe — including the reusable ASC REST recipe, since `altool --build-status` takes a build number as input and cannot enumerate — is in the TF-3 note in the maintainer's private planning tree.

Immediate ladder: ~~(1) finish + commit the in-flight **TagSidebar pending-tags** fix (uncommitted on main — tags typed since mount vanished from the sidebar tree)~~ ✅ **shipped 3 Aug 2026** — `mergePendingTags` folds store-only tags into the floor's default group, so the tree is the fetched codebook *plus* anything tagged since; optimistic rather than event-driven, so there's no fire-and-forget PUT to race and every write path is covered; ~~(2) **cut a release**~~ ✅ **shipped 4 Aug 2026** — **0.24.0** on PyPI (tagged at `923072e6` after a webkit-only e2e flake failed the run on the bump commit); Focus Mode, the appearance seam, the local-model catalogue and the Homebrew 6.0 tap-trust mitigation all reach users; (3) the **MCP extension's remaining one** (`icon.png` — and test whether a local install renders one at all before commissioning it, § Ideas below; the macOS-prompt pre-announcement shipped 3 Aug); ~~(4) **upload TF-2** — 2068 expires ~12 Oct regardless~~ ✅ **shipped 7 Aug 2026** — **0.25.1 (2450)** delivered and confirmed present in ASC (`BUILD-STATUS: VALID`, not just altool's exit code), expires 5 Nov. Uploaded by the new `desktop/scripts/upload-testflight.sh`, not Transporter. **Superseded twice over, corrected 22 Aug 2026:** 2450 was already one upload behind when this line was written — the live build is **2472 (0.25.3)**, uploaded 10 Aug, expiring 8 Nov — and TF-3 (**2501 / 0.26.0**) is the one now owed, before any invitation goes out; (5) **website rsync** (maintainer-only, SSH agent) — **the premise here was wrong and is retired**: the Mac connect flow was rewritten for the `.mcpb` extension on 31 Jul (`9dade66`, website repo) and has been live since the 16 Aug deploy; the pasted-config block that remains is the *command-line* fork, which is correct. What is actually owed is the deploy itself. bristlenose.app was last rsynced ~16 Aug, so six commits' worth of written work is unpublished: the 27-format list, twenty languages / twenty-two locales for Catalan, the cloud-import privacy disclosure (the one the OAuth consent screens link to), `terms.html`'s wording, the homepage search snippet, and a new **Recording permissions** page that is a live 404. `./deploy.sh` from the website repo ships all of it — it is all-or-nothing, and it ships the *working tree*, not a commit. Both documentation gaps behind it are now **closed** (22 Aug): `import-recordings.md` and `windows.md` are written and committed, and `recording-permissions.md` moved up to follow the import how-to. One thing the import page deliberately does **not** repeat: `CHANGELOG.md`'s 0.26.0 line says Bristlenose "fetches the recordings and transcripts" — it fetches the recording only (`CloudImportStore` calls `source.fetch` once per row, one file each; Teams transcripts need `OnlineMeetingTranscript.Read.All`, which we don't request). That clause wants correcting, but 0.26.0 is tagged and awaiting the publish approval, so it is the maintainer's call whether to touch it now — and the website changelog page renders live from that file, so the deploy publishes whichever wording is in `main` when it runs; (6) **add the remaining 4 Apple IDs + run the walks** per `docs/private/100days.md` §cohort-call-protocol.

In parallel, the **Background Assets branch** (whisper essential-tier delivery) can start slow off `docs/private/handoffs/background-assets.md`. Orthogonal small win, still open: **Opus 4.8 P2** — verified 1 Aug 2026, `bristlenose/pipeline_view/catalogue.py:149` still displays `"Opus 4"` and `bristlenose/llm/pricing.py` has **no Opus row at all**, so the catalogue recommends a model it can't cost. Overdue since ~18 Jun, TF-non-blocking.

**18 Aug 2026 — Settings ▸ General shipped; two things owed before it ships to testers.** The pane holds one row, **Default location for new projects**: configured (a security-scoped bookmark set here) → remembered (the parent of the last project made) → `~/Documents`. It fixed a real bug on the way — the `~/Documents` floor resolved to the app's *private container* Documents under App Sandbox, so a first-run panel would have created projects somewhere invisible in Finder and destroyed by `reset-sandbox-state.sh`. Owed: **(1)** the twenty non-en locales are **machine-seeded by me, not native-reviewed** — same status as the pl/ru/uk/da/sv/nb/tr/nl/fi/ca bulk, and the least-trusted strings are the Czech and Polish prepositions in `volumeOffline` (disk vs volume phrasing) and the German panel message, where the impersonal `Ort auswählen, an dem…` deliberately sidesteps the du/Sie question rather than guessing. `choosePrompt` needs no review — it is lifted from AppKit's own `Common.loctable`, so the button reads the word macOS already uses. **(2)** ~~the **desktop language smoke**~~ ✅ **run mechanically 22 Aug 2026, then parked — i18n is beta-shaped and the walk is not what still gates 0.26.0.** `choosePrompt` is now *proven* rather than asserted: **all 22 locales match AppKit's own `Choose` byte-for-byte**, including `ca` → `Selecciona` (imperative, as the Catalan convention wants) and `zh-Hant-HK` inheriting `選擇`, which is also Apple's own `zh_HK`. That row never needs a human again — it needs the re-run below. The other two rows (the label, the unset popup) were **not** seen rendered; the signed-`.app` pass was stopped deliberately, so the container fix stays visually unconfirmed.

    .venv/bin/python -c "import plistlib,json,pathlib;d=plistlib.load(open('/System/Library/Frameworks/AppKit.framework/Versions/C/Resources/Common.loctable','rb'));M={'en':'en','es':'es','ca':'ca','ja':'ja','fr':'fr','de':'de','ko':'ko','cs':'cs','it':'it','pl':'pl','ru':'ru','uk':'uk','da':'da','sv':'sv','nb':'no','tr':'tr','nl':'nl','fi':'fi','pt-BR':'pt','pt-PT':'pt_PT','zh-Hant':'zh_TW','zh-Hant-HK':'zh_HK'};[print(o,'MISMATCH',json.load(open(f'bristlenose/locales/{o}/settings.json'))['general']['choosePrompt'],d[a]['Choose']) for o,a in M.items() if pathlib.Path(f'bristlenose/locales/{o}/settings.json').exists() and 'choosePrompt' in json.load(open(f'bristlenose/locales/{o}/settings.json')).get('general',{}) and json.load(open(f'bristlenose/locales/{o}/settings.json'))['general']['choosePrompt']!=d[a]['Choose']] or print('all match')"

**What the walk surfaced is a different class from unreviewed translation — and is the reason to keep a smoke at all.** Machine-seeded-pending-review is an accepted standard (nine locales ship that way). These are *never enrolled in English*, so `check-locales.py` and `test_pipeline_diagnostic_locale_keys.py` were **green throughout** — the blind spot root `CLAUDE.md` §i18n documents in the second bullet, and precisely the recurrence `docs/design-cloud-import.md`:1320 predicted after the Accounts pane. Both **ship on every channel** and render English in all 21 non-en locales.

> **ADJUDICATED 22 Aug 2026 — settled, do not reopen.** English-only diagnostics is **fine for TestFlight**, and the Diagnostics surface is a **trim / reduce / possibly remove entirely** candidate for the Mac App Store build. So localising either of the two below would be translating a surface that may not ship — the right move is to decide its fate first. Recorded as an accepted carry, not a defect.

- **The whole System Health window.** `DoctorReportView.swift` has **zero** `i18n.t` call sites and six user-facing literals — `.help("Copy health report")` (:45), `.help("Re-run the checks")` (:53), `"Open a project first — the health checks run against its server."` (:78), `"Couldn't Load Health"` (:82), `"Try Again"` (:86), `"The server returned no health checks."` (:99). Not debug-gated: `BristlenoseApp.swift`:317 calls it "Shipping Health window … every channel".
- **Diagnostics ▸ Section 1**, the five items above the `exposesDebugTools` guard — `MenuCommands.swift`:134 `"Check Health"`, :140 `"Reveal .bristlenose/ in Finder"`, :143 `"Open Log in Console"`, :146 `"Copy Build Provenance"`, :149 `"Shoal Screensaver"`. The guard is at :151 and `#if DEBUG` at :187, so all five are ungated. The rest of that file has 102 `i18n.t` calls, and `docs/design-diagnostics-menu.md`:43 has the *Settings toggle that reveals this menu* localised in all 20 full locales — so the menu is translated and its contents are not. The same doc puts Section 1 in the **user** tier, "comprehensible, safe, reassuring to a non-technical researcher", so English-only is a gap, not a decision.
- ~~`WelcomeHomeView.swift`:290 `Text("Setup →")`~~ ✅ **fixed 22 Aug 2026** as `desktop.welcome.aiSetup`, and it was the **fourth** call site `a310bca6` replaced with English verbatim — the 21 Aug restoration caught three. `check-locales.py` could not report any of the four, because the keys were never *missing*. Values are Apple's own per-locale verb from `CCS_Localizable.loctable`, so nothing was machine-seeded. **English stays the noun "Setup"** — a "Set Up" rewrite was made and then reverted 22 Aug on the maintainer's call; don't re-propose it. The other 20 stay verbs deliberately: those languages take a verb for a link that performs the action, and Apple's noun forms (`ca` `Configuració`, `fi` `Käyttöönotto`) read as section titles rather than something you click. Part of speech follows each language's UI convention, not English's.
  **Caveat on the fix's value, recorded so nobody over-reads it:** the **bento Welcome is an experiment and is not committed to release**. An earlier note here called this string "the only live residue … which every locale sees on launch" — that overstated it, by assuming `WelcomeHomeView` was settled shipping surface. The *key* and its 21 translations survive any Welcome design; the *call site* goes if bento does.

**Verified clean, so don't re-check these:** all 22 locales are registered at all three Swift sites (`I18n.swift` `supportedLocales`, the `AppearanceSettingsView` picker, `I18nTests.swift`); `settings.general.*` is complete in all 21 full locales with `zh-Hant-HK` correctly inheriting; `de`/`it`/`nl` `inProvider` matching English is the real word, not drift; and the raw literals in `AppearanceSettingsView` (language names in their own language), `BootView` ("Bristlenose"), `MenuCommands` Section 2/3, `KeycapGalleryView`, `WelcomeDegradationLab` and `ProjectDiagnosticPopover`:290/294 are all correct or design-tool-only.

**The reframe, for whoever writes the next one.** A checklist naming one row in one pane could not have found either of the above, because it only looks where someone already thought to look. The smoke that earns its keep is: (a) the AppKit-corpus diff above, mechanical and total; (b) **`grep -L 'i18n\.t(' ` over every `.swift`/`.tsx` view added since the last tag** — both findings fall straight out of it, and it is the only check that sees a surface English never enrolled; (c) *then* eyes on whatever (b) flags. Rows (a) and (b) are scriptable and should run per release; the human pass is for what they surface.

**Raised, not scheduled: the pane makes the coordinated-reads work non-deferrable.** A configured default turns an occasional cloud-folder *choice* into the default at every project-creation door — and the drag-import door holds `FileManager.copyItem`, which blocks **indefinitely and uncancellably** on an iCloud/Dropbox dataless source (reproduced 29 Jul 2026; `docs/design-project-storage.md` §3). The pane does not cause that bug, it multiplies the rate at which it is reached.

---

**3 Sep 2026 — the `anthropic` pin is now code-ready, and the temperature slider is gone.** Two commits. `bb9e201d` fixed a **live** defect that had nothing to do with the SDK bump: sampling parameters are rejected with a 400 on every Claude model after 4.6, we sent `temperature=0.1` unconditionally, and `claude-opus-4-8` has been in the macOS picker — so picking it failed every analysis call, surfacing as the unactionable "The AI provider rejected the request." `fe145d32` then removed the slider itself (43 locale entries; the env var survives for CLI/CI). Reasoning promoted to `docs/design-decisions.md` §"Temperature is not a control we ship" — read that before proposing an `effort` slider, which is **not** a relabel of temperature.

What that leaves owed, in rough order:

1. ~~**Re-pre-mortem the SDK-major wave and float the ceiling.**~~ ✅ **done 5 Sep 2026** — both ceilings lifted (`anthropic>=1.0`, `mcp>=2.0`), Dependabot `versions:` rules retired, suite green on 1.4.0 / 2.1.1, three live Claude calls through the 1.4.0 SDK. The "wave" was one member: openai and google-genai were never ceilinged. Register rows graduated. _Original note:_ `anthropic` 1.x + `openai` 2→3 + `google-genai` 1→2 share the `httpx2` transport coupling, per Entry 6's "new cluster named". The Held register row is now `held — code-ready, wave-blocked`: no product decision is outstanding, only the wave. Nothing in `pyproject.toml` was touched.
2. **Two gates specified but not built** (spike §"How this gets caught"): (a) **preflight parity** — `preflight/api_key.py::_validate_anthropic` already makes a real one-token call with the real model but doesn't send the pipeline's optional params, which is exactly why preflight went green while the run died; ~3 lines to close. (b) **one autospec'd contract test** — `grep -rn autospec tests/` returns *nothing*, so no mock in the repo can see a signature change.
3. **`output_config.effort` at a tuned default, unexposed.** Reachable on the pinned `anthropic 0.125.0` already (`output_config` is in the signature) — no pin lift needed to start. Wants a measured pass against the stability corpus before a stop is picked; do **not** ship it as a control.
4. **`docs/platform-text-map.md` totals are drifted** — the header claims 22 sections / 638 keys; the table has 21 rows summing to 635. Pre-existing; I moved the total by my delta only rather than inventing a re-measurement. Wants one honest recount.
5. ~~**Aug 2026 quarterly dep review is overdue** (§Dependency maintenance below, unchecked).~~ ✅ done 3–5 Sep 2026 — §Dependency maintenance has carried it checked since, with Entry 7 of `docs/dependency-premortem-log.md` as the record. This line went on calling it overdue and unchecked; found 11 Sep 2026 by `/end-session`'s maintenance step, which reads the section rather than this summary of it. Next is **Nov 2026**.
6. **Two threads left open by Entry 5's score** (12 Sep, `/cassandra --score` on the 29 Jul Actions pre-mortem). **(a) `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` is still set in nine workflows** — up from seven; `mockup-register.yml:29` and `ratchet-tighten.yml:39` acquired it by copy-paste *without* the rationale comment, and `ci.yml`'s comment still cites a June 2026 switch three months past. It is now **measurably** inert rather than argued inert: the node20 annotation is absent from ci `34694795769`, codeql `34694795729` and snap `34694795721` **while the variable is still set** — so both halves are proven, it never silenced anything and the node24-native bumps are what did. Deleting it from all nine is workflow-only, so it reuses the existing tag. **(b) The Homebrew tap dispatch is unverified** — `peter-evans/repository-dispatch` moved to v4.0.1 on 4 Sep (`4daad56b`) and no release has run since 31 Aug, so the one path that fails *silently* has not yet met its bump; confirm the tap receives `update-formula` on the next release. Entry 5's Q3 (the 16 Sep node20 *removal* is a non-event here — our actions are all node24-native) is scored `untested` by design and resolves in four days. Ledger: Entry 5 § OUTCOME/SCORE.


## Ideas (captured, not triaged)

- **Focus Mode on Signals has one thing no test could draw: the pending skeleton.** Shipped 21 Sep 2026 (`4e3b0fb4`, `docs/design-focus-mode-signals.md` §6). `.signal-card-location-pending` sits where the headline goes, breathes 0.45→1 on its own animation, and now also takes the 0.4 wayfinding opacity — so on a cold elaboration cache, in Focus, a whole column of cards is breathing at 40%. Probably fine and possibly better; nobody has looked. One pass in the `.app` with a cold cache answers it. The mockup cannot: it draws settled cards only.
- **Fit-to-width shipped on `main` (`94dc52e4` + `5bdabf8d`, 20 Sep 2026) but its native half has not been seen running.** The web cascade was confirmed in the app (both panels give way in order, the quote grid holds its measure). The replacement native half — projects column collapsing on window resize from measured widths, no declared minimum — is unit-tested (`SidebarAutoCollapse`) and unbuilt: three Debug builds were refused by the sidecar freshness guard because another session was saving under `bristlenose/`. Next Cmd+R in a quiet tree; the four-step hand check is in `docs/design-sidebar-playground.md` § Fit to width. Two floors are still one constant: Codebook, Sessions and the dashboard have not been measured for their own `CONTENT_FLOOR_PX`.
- **Four analysis-lens items left by the 20 Sep truing pass, none blocking, ranked.** (a) **`docs/design-finding-weight.md` needs its frame inverted and must NOT be archived** — it opens "Design exploration. Not an implementation plan.", which has been false since 19 Mar: `classify_flag` *is* its waterfall and flags have rendered on the hero chip since 20 Sep (39 of 105 cards flagged, five of six words firing). Worse, its pseudo-code **differs in shape** from the shipped one — the doc gates `composite < MIN_SIGNAL → None` first, making `SMALL_SIGNAL` unreachable, while shipped the two thresholds swapped roles, so a positive card between 0.01 and 0.02 is `Success` in code and `None` in the doc. It is the only design record of a live vocabulary, so archiving would leave `classify_flag` with no spec. (b) **`confidence` is computed, serialised and read by nobody** — written at `analysis/signals.py`, on the wire at `routes/analysis.py`, typed in `utils/types.ts`, zero reads in `frontend/src`; `.confidence-badge` survives in `analysis.css` under "hidden from UI, preserved for future". Wire it or delete it; it currently reads as a feature. **Parked by the user 20 Sep**, captured so it is not re-derived. (c) **Top-N is three different numbers in three docs** — 5–8 in the private concentration note, 12 in `signals.py`/`generic_signals.py`, 10 in `design-signal-card.md`; `server/elaboration.py` records its own retirement of `DEFAULT_TOP_N` in favour of a char budget. One sweep against `elaboration.py`. (d) ~~**`analysis.intensityTitle` says "(0–3)"; intensity is 1–3**~~ — done 21 Sep 2026, rode along with the Signals-rename locale catch-up (`signals.intensityTitle` reads 1–3 in all 21 locales). Evidence and anchors in the truing plan note kept with the maintainer's private working files.

- ~~**The whole Codes menu dispatches into nothing, and has since 0.29.0 — needs a product call, not a gate.**~~ ✅ shipped 20 Sep 2026 (`7e2159b2`) — the product call was a **focus cursor**, not multi-select: one click focuses a group or a code, `CodebookFocusStore` holds the pair, the bridge mirrors it into eight `@Published` flags, and `CodesMenuContent` dims from those. The five inert commands are live and gated; Import/Remove Framework were replaced by one Install/Uninstall Codebook that mirrors the detail page's own button. **Merge Codes remains withdrawn** — it needs a source *and* a target, which is selection, not a cursor. Retained below as the record of how a menu shipped inert on nine channels with nothing red at any point. Its five remaining commands (`createCodeGroup`, `createCode`, `browseCodebooks`, `importFramework`, `removeFramework`) send `bn:codebook-*` CustomEvents that **nothing in `frontend/src` listens for**; `42d06638` put those listeners in v1's `CodebookPanel` and `baa1aa0e` deleted the panel with them, three commits before the 0.29.0 tag (`git describe --contains` → `v0.29.0~3`). So the release whose headline was the codebook lens shipped that lens's menu inert on all nine channels, and retiring the six warn-stubs on 12 Sep left a menu in which *every* item is inert — the stubs at least said so in the console. Nothing was ever red: a `CustomEvent` with no listener resolves normally, so the bridge succeeds and the Swift `catch` never fires. **The five are deliberately left ungated** — dimming a command with no implementation is a `.disabled` that will never go live. The call is re-home the listeners in the v2 navigator, or withdraw the menu with the `mergeCode` idiom. Evidence in `docs/design-desktop-menu-actions.md` § Codes menu and `docs/fix-the-menus.md` § B.

- **`theme/js/names.js` ships inert, and the surface it drove is gone (20 Sep 2026).** It binds through `.name-text`, `.role-text` and `.name-pencil` (`js/names.js:92,103,123,296`); nothing in the repo emits any of them and there is no participants-table template, so `initNames()` runs at boot (`js/main.js:54`) and attaches to nothing. `#export-names` (`templates/toolbar.html:14`) carries `style="display:none"` and is revealed only once edits exist, which they cannot — so the whole feature, pencil through YAML export, is unreachable. The React sessions grid replaced it with `.bn-name-pencil` + `.bn-speaker-editable-name`. **Deliberately not deleted**: it is frozen vanilla (data-integrity fixes only), and `tests/test_hidden_quotes.py:76` asserts its `_JS_FILES` position, so removal is a two-file change plus a test edit rather than a line deletion. `molecules/name-edit.css`, its stylesheet, WAS deleted (`bb70a8d5`). Banner and evidence in `docs/design-html-report.md` § Name editing.

- **The menu QA walk is four checks short, and check 9 is the one that matters.** 12 Sep: ⌘F wired and verified working twice in a row, ⌘E verified, zoom verified, Print verified dimmed. Unreported: ⌘F dimmed on Sessions/Codebooks/Analysis; ⌘J absent rather than dimmed; ⇧⌘E Export dimmed on Welcome; and **a failed-run status page with Find, Zoom and Export all dimmed** — the exact state the `canDispatch` predicate was introduced for, and the one `isReady` would have left lit. Everything in the walk is Swift-side, so a plain Cmd+R is enough. The state is reachable by opening a project whose last run failed.


- **`design-desktop-menu-actions.md`'s line anchors have rotted again, and its own banner predicted it (12 Sep 2026).** The 28 Jul banner asked for struct names because "line numbers here rot within days"; that re-anchoring was never done, and a spot-check today resolved **0 of 5** — `MenuCommands.swift:235-243` and `:267-270`, cited as the tab switch and the inspector gate, both land in the DEBUG menu (real sites `:930`, `:829`); `:530-533` (`playPause`) is really `:1381`; `:464-466` (`mergeCode`) is `:1272`. Today's truing pass used struct names for everything it wrote and left the old rows alone, so the doc is now mixed. The fix is mechanical — replace every `MenuCommands.swift:N` with the owning struct (`FileMenuContent`, `FindMenuContent`, `ViewMenuContent`, `CodesMenuContent`, `QuotesMenuContent`) — and it is worth doing in one pass rather than per-row, because a half-anchored doc reads as freshly checked.

- **`BridgeHandler.swift:437`'s comment counts warnings that are no longer there (12 Sep 2026).** It says "six inert warnings elsewhere in this file; don't add a seventh". Measured today: **five** `try? await … in: nil, in: .page` sites (`:511`, `:537`, `:555`, `:607`, `:626`), zero dead `catch` blocks — `switchToTab` and `menuAction` moved to the real `contentWorld:` overload and their catches are live. The rule the comment states is right and load-bearing; only the tally is stale. Left alone because another session had that file open. Same disease as the `_JS_FILES` count in the root notes: a number in prose is a number nothing recomputes — the honest repair is to drop the count, not to correct it to five.

- **CopyError Tiers 1–2 — the leftovers after Tier 0 (12 Sep 2026).** Tier 0 (`CopyError: LocalizedError`, `.underlying(Error)`, `.alreadyInFlight`; both ContentView sites read the same sentence) **landed 12 Sep 2026** (`fbd8f4f3`, Swift suite green in an isolated worktree). **Owed (user-facing): a CHANGELOG line at next release** — "a failed drag-in copy now says why, whichever gesture started it". Left: (1) the four `errorDescription` sentences are English literals — route through locale keys, 21 full locales; (2) `.noItemsAfterFiltering` at the loose-files site now toasts its sentence instead of the enum index, while the drop-onto-project site keeps it silent ("should not happen — we filtered above") — decide whether both should be silent; (3) Tier 2 — discriminate `.underlying`'s domain and code (`NSFileWriteOutOfSpaceError` mid-copy → the existing disk-space alert; File Provider `-2001`), and the toast-vs-alert call on the failure state, drawn before it is decided. `docs/design-copy-error-surfacing.md` §6.

- **iCloud percent probe — the one open measurement behind the cloud-wait label (4 Sep 2026).** Does `NSMetadataUbiquitousItemPercentDownloadedKey` (external-documents scope) or `Progress.addSubscriber(forFileURL:)` report anything on 26.x File-Provider-backed iCloud Drive, from the sandboxed test host, for a file outside our container? Needs an evicted file of tens of MB and real bandwidth — **ask before running**. Three outcomes, each a one-line edit to `docs/design-cloud-wait-label.md` §4; the recipe is its §5.

- **Test-target deployment floor — 26.1 is incidental, not an AI gate (3 Sep 2026).** `BristlenoseTests` sits at 26.1 while every app scheme is 15.0, and no test uses a 26 API. Spike: can it drop to 15.0, and does anything care? Handoff written (`verify-tests-deployment-floor`, in the private handoffs); `desktop/scripts/check-deployment-floors.sh` pins both values — change the pin and `docs/design-platform-policy.md` §Pillar 3 in the same commit.

- **`xcrun mcpbridge` — Xcode's own MCP bridge, wired nowhere (4 Sep 2026).** Would let the review agents build, test and read documentation through Xcode instead of `xcodebuild` shell-outs. Not in any `.claude/` config; the only mention in the tree is an aside in `docs/design-mcp-extension.md`. Optional; try when idle.

- **mypy long tail — 148 errors, none a runtime threat, held by the ratchet.** _Ceiling now 149, adopted 4 Sep 2026 from CI run 33881394121 — CI resolves mypy 2.3.1 and measures 149 where this Mac's 2.3.0 measures 148, which is why the metric is CI-authoritative and why the local run reports it but cannot lower it._ 4 Sep 2026: the two mechanical classes (bare generics, stale `# type: ignore`) were cleared, 238 → 148, all 4357 tests green. Every crash-shaped remainder was chased and is guarded at runtime — the `None`-into-non-optional assignments each sit above an explicit guard, `coverage.py`'s "4-tuple" list is unpacked as five by its only consumer, `cli.py:2321`'s `None` is a deliberate tri-state, the two non-exported `huggingface_hub` imports are inside `try/except ImportError`. What remains, by *what it tells you*: **(A, 44 errors, one edit)** `_terminus_kwargs() -> dict[str, object]` in `run_lifecycle.py:718` is spread into five terminus-event constructors, so mypy verifies nothing about the fields Swift decodes — a `TypedDict(total=False)` with `input_tokens: int`, `output_tokens: int`, `cost_usd_estimate: float | None`, `price_table_version: str`, `summary: PipelineSummary` (types read from `RunCost` and `RunHandle`) removes 30% of the remainder and checks the wire contract for the first time. **(B, ~12)** annotations that lie: `coverage.py:118/141` says 4-tuple, is 5; `render_html` says `list[FullTranscript]`, receives `list[PiiCleanTranscript]` at three sites; `miro_client` declares `dict` on three functions that return `dict | list`; `cli.py:2321` `bool` holding `None`. **(C, ~15)** `object` where a real type exists — `_estimator`, `settings`, the three LLM client attrs — every `"object" has no attribute` and `"object" not callable` is one of these. **(D, ~10)** invariance and third-party quirks: Presidio's two `RecognizerResult` classes, SQLAlchemy `Row` sort keys, `dict[str, None]` vs `dict[str, str | None]` — true pinheads. **(E, ~45)** `no-any-return`, `no-untyped-def`: absence, zero information. Do A on its own; B is cheap; C medium; leave D and E to the ratchet.

- **Branch protection on `main` requires a check nothing emits, and exempts the only person it applies to — `gaps.md` G9.** Measured 4 Sep 2026, **re-verified 12 Sep, unchanged**: the required contexts are `ci / test (3.10–3.13, ubuntu-latest)`, `ci / frontend-lint-type-test` and **`ci / lint`**. There has been no `lint` job since `5058bec0` split it into the fail-fast gates matrix on 3 Sep, so that context can never report again. `enforce_admins` is false, so every push prints `remote: - 6 of 6 required status checks are expected.` and lands anyway — the warning scrolls past above a successful push. Two consequences: nothing that is *not* the maintainer can push at all (found by trying — `ratchet-tighten.yml` was written to commit its own measurement and would have been red every time it worked), and **the gates matrix is not in the required list at all**, so `ruff`, `ratchet`, `inventory`, `gate-policy`, `gate-proofs` and `manpage` sit outside the rule meant to hold the branch. Not a defect with one right answer — it is a repo setting, and how much ceremony a solo trunk repo wants is a decision. The honest options: name the six `gates` cells and drop `ci / lint`, or drop required status checks entirely and let the local gates plus `/end-session` carry it. The present state is the one that is not defensible, because it claims a guarantee it cannot deliver.

- **`/end-session`'s verify phase runs no gate script, so a close-out can sign off `verify: ok` with a hard CI gate red.** Measured 12 Sep 2026, on a live example. The skill's verify phase is pytest, `ruff check .`, `npm run build` if frontend changed, and `test-swift.sh` if `desktop/` changed (`.claude/skills/end-session/SKILL.md:24-27`) — grep it for `check-ratchet`, `check-gate-policy`, `check-gate-proofs`, `gen-test-inventory`, `check-locales` and you get nothing. That morning a close-out recorded `"verify": "ok"`, `"tests": "passed"`, `"lint": "clean"` into `.claude/last-end-session.json` while `scripts/check-ratchet.py` exited **1** on the same tree: `pytest_skip_sites: 24 > ceiling 23`, from a corpus-gated `@pytest.mark.skipif` added the same morning. Six of the seven gates were green; the ratchet was not, and nothing in the ritual asked. **Why it matters more here than it would elsewhere:** CI does run the gates matrix, but `main` is pushed by hand and its branch protection is bypassed by `enforce_admins: false` (see the G9 item above), so the close-out is the last checkpoint that actually stands between a red gate and a push — and it is measuring the two things that were already hard to get wrong. Cheap fix: one loop over the `check-*` scripts in the verify phase, exactly as the `gates` matrix lists them, reported like the other suites. The counter-argument is runtime, and it is weak: all seven together take about four seconds. Note the shape — this is the same defect the ratchet work was about, one level up. A gate nobody is obliged to run is indistinguishable from no gate, and the ritual that exists to ask the questions does not ask these.

- **Three leftovers from the ratchet-tightening work (4 Sep 2026), none urgent, all cheap.** (a) **`mypy>=1.13` is a floor, not a pin** (`pyproject.toml:81`) — this is the root cause of the whole `authority: ci` apparatus, demonstrated twice now: 238 local / 239 CI on 3 Sep, then 148 local / 149 CI on 4 Sep when CI resolved mypy 2.3.1 against this Mac's 2.3.0. Pin it exactly and the metric becomes a property of the code, `--adopt` and the dispatch workflow stop being needed, and one whole mechanism can be deleted. It is a dependency-policy change (`docs/design-platform-policy.md`), so it belongs in a dep-review wave, not a drive-by. (b) **`scripts/test-check-ratchet.py` runs in no CI job** — `release-suites` names its suites in a literal list and now includes a Python one (`test-release-board.py`), so the precedent exists; the ratchet's write-path self-test currently runs only by hand and inside `ratchet-tighten.yml`, which means a regression in it sits undetected until someone dispatches. Same gap applies to `test-dep-drift.py`. (c) **`gate-proofs.json`'s `declared.check-ratchet` entry is dead weight** — adding the paired self-test reclassified the gate as `automated`, and `check-gate-proofs.py` has no stale-declared check, so the entry is annotated `SUPERSEDED` and read by nobody. Either prune it or teach the script to flag a `declared` entry for a gate that is now `automated` — the second is the one that stops it recurring.

- ~~**The Swift suite runs nowhere automatic.**~~ ✅ shipped 2 Sep 2026 (`afcac68f`, `bb0d3bd2`, `b34b0ca3`, `5e844972`). `desktop/scripts/test-swift.sh` is the single entry point, called from three altitudes: `/end-session` Phase 1 step 4, `build-all.sh` step 1c, and `mac-build.yml`. **Origin:** `TabLeftPanelTests` asserted `Tab.allCases.count == 6` after `baa1aa0e` left the enum at five — red for 10 commits, and **v0.29.0 shipped on all nine channels with it red**. Nothing could have reported that: `/end-session` ran pytest + ruff, and `mac-build.yml` piped `xcodebuild` to `tee` under a shell without `pipefail`, so it had reported success on every run since 20 May including ones carrying 16 compile errors. Fixing the pipe exposed two more layers (a `GeneratedBuildInfo` failure on every fresh checkout; the sidecar Ensure phase demanding `node_modules`) and a runner too old to compile the macOS 26 APIs now in use. CI is `macos-26`, and reports the same 1406 passed / 0 failed this Mac does — same Xcode 26.6/17F113, same script.

- ~~**`build-dmg.sh` has no Swift gate.** The `.dmg` is a separate entry point with its own signing path, so a red suite can still reach a notarised direct download — the one channel `mac-build.yml` and `build-all.sh` between them don't cover. Same three-line block as `build-all.sh` step 1c (skip on ad-hoc, honour `SKIP_SWIFT_TESTS`). Small; the only reason it didn't land with the rest is that it wasn't in the ask.~~ ✅ shipped 3 Sep 2026 (`8cae1527`) — step 1b, red proven against a failing stub. Left unstruck for a day by the same session that shipped it: the Scan B case in its purest form.

- **Bare-key shortcuts don't guard modifiers — ⌘S/⌘H/⌘M also fire the bare-key action.** 5 Aug 2026: `frontend/src/hooks/useKeyboardShortcuts.ts` has one handler per bare key, and only `z` (`:614`, added with Focus Mode) checks `!e.metaKey && !e.ctrlKey && !e.altKey`. `s` (`:594` star), `h` (`:585` hide), `m` (`:499`), `x` (`:577`), `j`/`k` (`:570`), `[` (`:462`), `]` (`:477`), `/` (`:508`) match on the key alone — so in browser serve mode ⌘S stars the focused quote *and* opens Save Page, and any future ⌘-chord landing on one of those letters silently doubles up. Caught while wiring `z` (⌘Z would have toggled Focus Mode); fixed there only, deliberately — widening the guard to eight more handlers mid-feature was out of scope. The fix is mechanical (same three-clause guard) but wants a pass over all of them at once plus a check of which chords the WKWebView actually receives, since macOS eats ⌘H/⌘M before the page sees keydown and the desktop half may already be immune.

- **Thinking-shimmer: native sidebar line needs an Xcode build-verify; help copy + tuner wiring pending.** 25 Jul 2026: the web half shipped + verified (`560a0cf4`) — activity chip label shimmers (`atoms/shimmer.css`, two-gate: `prefers-reduced-motion` + `data-analysis-animation`), spinner removed, CSS chip at 20% contrast, toggle bridged native→web. The native sidebar `.running` status-line shimmer (`SidebarShimmerText.swift` + `ProjectSidebarOutline` wiring, at 5%) **compiles clean** (verified via isolated `xcodebuild`, 25 Jul); the only remaining check is the **visual read** — does it shimmer and stay consistent light/dark on a running row — which needs a live run in the app. Follow-ups: (a) broaden the `showAnalysisAnimation` help copy (currently shoal-only) across the 20 locales once native lands; (b) the Debug▸Shimmer Tuner menu wiring (`MenuCommands`/`BristlenoseApp`/`ShimmerTunerView`) stays **uncommitted** — entangled with the Keycap Gallery debug window, commit with that batch. Spec: `docs/design-motion.md` §4.7.1.

- **Boot progress bar is silent to VoiceOver.** 25 Jul 2026: `BootView` progress phases were stripped to just the indeterminate bar (removed the "Bristlenose" wordmark / tagline / "Starting…" text that redundantly re-branded on every project switch in the embedded Mac context). With the visible text gone and no replacement label, the bare `ProgressView` announces nothing to VoiceOver — the app-wide convention is an explicit `.accessibilityLabel` (cf. `OllamaDownloadPill`/`StatusPill`). Deferred per the "bar UX comes later" steer; add a label (reuse the existing `desktop.boot.startingSidecar`/`loadingReport` strings) when the bar gets its next pass.

- **`configure azure` still punts endpoint+deployment to manual `.env`.** 21 Jul 2026: `configure <provider>` now file-persists the API *key* on a keyring-less box (`59e3770c`), and env-var reads accept the SDK-native names (`dd76725e`, incl. `AZURE_OPENAI_ENDPOINT`/`AZURE_OPENAI_DEPLOYMENT`). But `configure azure` only stores the key — it prints the endpoint/deployment as a "add these to .env yourself" hint (`cli.py` azure branch). To fully close the loop like the other providers, `configure azure` would prompt for endpoint+deployment and persist all three to the config `.env` (needs a generic "write arbitrary var" path on `FileCredentialStore`, or reuse the existing upsert). Low alpha priority (Azure is enterprise; `docs/design-cli-provider-selection.md`). The `ProviderSpec.config_fields` registry in `providers.py` already declares these fields — an interactive `configure` could iterate them generically instead of the current hardcoded per-provider branch.

- **The website is committed but NOT deployed — one manual rsync owed.** _Updated 1 Aug 2026 (0.23.0 release sweep): the "committed nowhere" half is closed_ — the website repo's working tree is clean and pushed (`3f706de` and the `connect-an-agent` series), and the README half landed in this repo too. `docs-src/cli.md` now carries `bristlenose pipeline`, `bristlenose use`, and the reworded `--llm`; `connect-an-agent.md` describes the shipped extension flow. **Still owed: the rsync deploy** (needs SSH agent — maintainer-only). Until it runs, `bristlenose.app/docs/` describes the pre-0.23.0 product: no `use` command, and a Mac connect flow (right-click → paste a config) that no longer exists. That last one is the drift direction the extension plan called worse than absence, so this deploy should ride with the build that ships the extension.

- **The MCP extension owes one small thing before the next TestFlight — and it may not be ours to fix.** 1 Aug 2026, recorded in `docs/design-mcp-extension.md`: the `.mcpb` has **no `icon.png`** (512×512), so Claude Desktop shows a generic tile. _3 Aug 2026 — research first, artwork second:_ the manifest `icon` key is optional PNG, 512×512 by convention (Figma's shipped extension is exactly that — full-colour brand mark, transparent, no plate; Anthropic's own Filesystem one is an 80×80 monochrome glyph). **But [mcpb#154](https://github.com/modelcontextprotocol/mcpb/issues/154) reports manifest icons never rendering for _locally installed_ extensions**, open with no maintainer response — and the correlation on this Mac supports it: both icon-bearing extensions are `ant.dir.*` (directory-installed), ours is `local.mcpb.*`. Since §7 Q4 decided against directory submission, local install is our only channel. **Decisive 5-minute test before commissioning artwork: drop any placeholder PNG in, rebuild, reinstall, see whether the tile changes.** If it doesn't, this stops being "we owe an icon" and becomes "blocked upstream", which is a different line and stops it recurring as a task. ~~(2) the pane's Claude Desktop hint should **pre-announce the one-time macOS "access data from other apps" prompt**~~ ✅ **shipped 3 Aug 2026** — `mcpAgents.claudeDesktopPromptNote` under the install row, each locale quoting macOS's own dialog wording *and* Allow-button label from `TCC.framework`'s loctable, so the sentence matches the dialog the researcher then sees. ~~(3) `AIConsentView.currentVersion` is still 1~~ ✅ **shipped 1 Aug 2026** (`09b348b1` connecting an agent is a new recipient and is disclosed; `6df94d4f` the sheet no longer contradicts itself about names) — `currentVersion = 2`, so the cohort re-consents on the build that ships the extension.

- **Three docs still name the retired `docs/manual.md`.** 31 Jul 2026: `manual.md` was archived (`d1c883ee`) and its live pointers corrected in `CLAUDE.md`, `docs/design-docs-system.md`, and the `user-documentation-review` agent (`95ff4bd4`). Left alone deliberately: `docs/design-docs-versioning.md:200`, `docs/design-prompt-injection-defence.md:250` (a future-work item), and `docs/how-to/INDEX-proposal.md` (:15, :21). These read as records of past reasoning rather than live routing, so they don't misdirect the way the docs-system table did — but a future `/true-the-docs` pass over that slice should sweep them.

- **Moderator identity: two more surfaces key on the bare speaker code, and neither is written down.** 27 Aug 2026, from a mechanism read of the `m1` collision. `assign_speaker_codes` resets its moderator/observer counters per session while threading `next_pnum` globally (`stages/s05b_identify_speakers.py:450`), and `compute_participant_stats` keys a flat dict by code (`people.py`), so the last session's `m1` wins. That much is already documented and now WARNs. What is **not** recorded anywhere: (1) `grounding.py:449` does `names.setdefault(sp.speaker_code, display)` — code-only and **first**-wins, so the MCP and chat-lens surfaces answer with one moderator's name for every session. This is a *different* defect from the anonymise one logged at `design-people.md:1659` (that one is "returns `{}` for everyone"); this is "which of N moderators wins". Three surfaces, three policies for one datum — `people.yaml` last-wins, `routes/dashboard.py:209` correct on `(session_id, code)`, `grounding.py` first-wins. (2) The **DOM** carries it too: `data-testid={`bn-name-${sp.speaker_code}`}` at `islands/SessionsTable.tsx:466` (and the pencil at `:501`), so `getByTestId("bn-name-m1")` fails with *multiple elements found* — two nodes asserting they are the same person. Under the settled renumber (`design-people.md` §E decision 1; roadmap 11c trued 25 Aug) a second moderator becomes `m2` and this resolves — but only once they have been *named* apart, so until then no test can address a row uniquely and a11y/regression specs pass against the ambiguity. **Sequencing, and it inverts the obvious order:** no failing test can be written for the header, the provenance chips or scope-at-rename today, because `people.yaml` is structurally incapable of holding two different `m1` names — the fixture does not exist. The renumber lands first; the `sessions.py` header dedup is **blocked, not deferred** (removing it yields "Moderated by Sarah, Sarah, Sarah" — it is a faithful render of collided data, not the defect). **Design already settled, don't re-derive:** mockup at `docs/mockups/people-provenance-paths.html` (ten benches; committed under `773707fb`, whose subject is unrelated — shared-index sweep). Copy is **"From the transcript" / "Heard in the session" / "You set this"** — "Researcher" is unusable as a provenance label (collides with an already-translated key meaning *a person* in 21 locales) and "conversation"/"the room" are further English words for the canonical **session**. Bench 8: a confirmed name must be a **recorded** state, not the absence of a marker, or human-verified and never-looked-at render identically — four tiers, not three. Bench 6: the subgrid sizes the name column from the widest entry in the study, so inline provenance chips do not survive 900px; Part 3's zero-geometry options are the live candidates. **One a11y residue:** rung 3 (≤480px) still exposes no name at all — needs `display:none` → visually-hidden in two `sessions-grid.css` blocks, not a delete; the jsdom test pins the attribute, not the announcement, because container queries do not run there.

- ~~**The report's left panel carries two measured CSS defects, both OS-independent, neither caught by any gate.**~~ ✅ **both fixed 20 Sep 2026** (`5851bbb6` the flank gutter, `1c563b29` the bottom gap it also hid, `d7710610` the scroll slip) — kept below as the record. Found 2 Sep 2026 while studying the AppKit↔WKWebView seam; re-verified at HEAD 12 Sep 2026, both unchanged then. They are unrelated to each other, and unrelated to the seam question that surfaced them — that one has a moving target and is parked behind an instrument (below); these two do not, and are cheap.

  **(a) ✅ FIXED 20 Sep 2026** — the embedded override in `report.css` now zeroes `padding-left`/`padding-right` too, and `.layout.embedded` gives `.center` (and the minimap slot) the 24px back wherever no panel stands between it and an edge (`sidebar.css` § "Desktop embedded"), so every text position is preserved and only the strip is gone. Measured on macOS 27 before the fix: the strip was pure `#ffffff` beside a `#f9f9f9` panel — the surface behind the transparent webview is white on 27, not the grey the seam mockup assumes. Original finding: 24px of body padding survived into the app and showed as a light gap on both flanks. `theme/templates/report.css:21` sets `padding: var(--bn-space-xl) var(--bn-space-lg)`; the embedded override at `:35-38` replaces **`background` and `padding-top` only**, so the horizontal `--bn-space-lg` (24px) reaches the WKWebView. Embedded `body` is `background: transparent` over a `drawsBackground=false` web view, so that strip shows the **window surface** through it — a third light grey between the native sidebar's material and the panel's `--bn-colour-inspector-bg`, and the same 24px again to the right of the tag sidebar. It reads as a failed join rather than as page background, which is why it was chased as a seam problem first and found last. The fix is not a token change but a change of which box owns the inset: `body` goes full-bleed under `html[data-embedded]`, and the 24px moves inward onto `.center`, which already carries `--bn-gutter-left`.

  **(b) ✅ FIXED 20 Sep 2026** — `.toc-sidebar` is now `display: flex; flex-direction: column` like its twin, so `.toc-sidebar-body` is the one scroller and the datum holds at every scroll position. The bottom gap at scroll end (the panel stopping one body padding short of the window) went the same day: body's bottom pad moved onto `.center`, inside the grid row the panels span. Original finding: `.toc-sidebar` was missing the `display: flex` its twin has, so the wrong box scrolled and the datum held only at scroll-top. `.tag-sidebar` (`theme/organisms/sidebar.css:519`) is `display: flex; flex-direction: column`; `.toc-sidebar` (`:190`) is not — though both are `position: sticky; height: 100vh; overflow-y: auto` with a `…-sidebar-body { flex: 1; overflow-y: auto }` (`:447`). On the tag side that works: the body fills the leftover height and scrolls inside itself under a pinned header, one scroller, and the `margin-top`/`padding-top` bleed that puts the panel's first row on the same datum as the content's `<h1>` never moves. On the TOC side `flex: 1` is **inert** (block parent), so the body sizes to its content, never overflows itself, and the **outer** panel scrolls instead — carrying the header, the bleed, and therefore the datum with it. The panel is aligned with the content at scroll-top and at no other offset. Second-order, same family: panel and document are separate scrollers and there is **no `overscroll-behavior` anywhere in `bristlenose/theme/`**, so a panel that bottoms out chains into the page under one continuous gesture.

  **Why nothing is red.** `e2e/tests/lens-datum.spec.ts` asserts the first `.section-heading` computes `margin-top: 0` — the contract, correctly chosen, but measured at scroll-top, which is the one position at which (b) is invisible. A datum that holds only at offset zero passes it. Same shape as the pipe-vs-TTY entry in root `CLAUDE.md`: the check is sound and is looking at the one state where the defect cannot appear.

  **The seam question these came from is parked deliberately.** Three macOS sidebar geometries are live at once (Sequoia flat, Tahoe inset plateau, Golden Gate edge-anchored again), so matching by hand is work bought twice; Apple's sanctioned answer is `backgroundExtensionEffect()` (macOS 26+, HIG ▸ Sidebars), whose documented use case is exactly a split-view detail column extending under the sidebar, and whose behaviour with a **WKWebView** — a hosted layer, not SwiftUI-rendered content — is the open question. `docs/design-test-environments.md` holds which machine can prove what; `docs/mockups/sidebar-seam-window-edge.html` holds the options side by side with the measured panel/content contrast ratios (the current tint is **1.052** against content in light mode — close to notional); **Diagnostics ▸ Seam Lab** (`SeamLabView.swift`, DEBUG-only, `b230eee8`) is the instrument. It **compiles but has never been run** — its view-tree probe and its CSS-overlay injection are both unexercised. One run on this Mac is the next step there, and is independent of (a) and (b).

---

## Task tracking

**GitHub Issues is the source of truth for actionable tasks:** https://github.com/cassiocassio/bristlenose/issues

**Launch plan:** `docs/private/100days.md` — triaged by topic and MoSCoW priority.

This file contains: session reminders, untriaged captures, dependency maintenance, and reference tables.

---

## Dependency maintenance

Bristlenose has ~30 direct + transitive deps across Python, ML, LLM SDKs, and NLP. CI runs `pip-audit` + `npm audit` on every push (informational, non-blocking). Dependabot opens weekly PRs for both ecosystems. CodeQL SAST runs on push + weekly. See `SECURITY.md` for remediation SLA.

### Quarterly dep review (next: May 2026, then Aug 2026, Nov 2026)

- [x] **May 2026** — Run `pip list --outdated`. Bump floor pins in `pyproject.toml` only if there's a security fix, a feature you need, or the floor is 2+ major versions behind _(prophecy 8 Jun 2026 via Cassandra Entries 1+2; execution 9 Jun 2026: security wave `5c96058` (presidio + cryptography 44→48, cleared 3 OSVs, floor bumped) + graduated-holds wave `e3c0a87` (starlette 1.x pair + WTForms 3.2 pair, dependabot config updated). Cassandra tally 4/4/0. Wave-3 greens deferred — see `docs/private/handoffs/dep-wave-3-greens.md`.)_
- [x] **Aug 2026** — Done 3–5 Sep 2026 (Entry 7 in `docs/dependency-premortem-log.md`): nine Dependabot PRs triaged, Actions pins moved (codeql 4.37.9 as one SHA across init/autobuild/analyze, setup-node 7, download-artifact 8, repository-dispatch 4, `actions/attest`), `snapcore/*` → `canonical/*` on node24 (snap edge rev 14), Held register re-grounded — four release-predicates could never fire. Python floor decided in a sibling session: `>=3.12` on 1 Nov 2026 (`docs/design-python-floor.md`)
- [ ] **Nov 2026** — Same. Start with `scripts/check-python-band.py --latest` and `scripts/check-providers-live.py`; execute the 1 Nov floor move if it has not happened; the 3.15 / presidio ceiling question is due ~Mar 2027, not this review

### Annual review (next: Feb 2027)

- [ ] **Feb 2027** — Full annual review:
  - Python floor is 3.12 from 1 Nov 2026 — re-run `scripts/check-python-band.py --latest`; the 3.15 ceiling waits on presidio
  - Check faster-whisper / ctranslate2 project health
  - Check spaCy major version
  - Check Pydantic major version
  - Rebuild snap; review `pip-audit` CI output

### Risk register

| Dependency | Risk | Escape hatch |
|---|---|---|
| faster-whisper / ctranslate2 | High — fragile chain, maintenance varies | `mlx-whisper` (macOS), `whisper.cpp` bindings |
| spaCy + thinc + presidio | Medium — spaCy 3.x pins thinc 8.x | Contained to PII stage; can pin 3.x indefinitely |
| anthropic / openai SDKs | Medium — anthropic 1.x removed `temperature` from `messages.create`; GPT-5+ rejects `max_tokens` and non-default temperature; `AsyncMock` cannot see either | `tests/test_llm_sampling_params.py` introspects the installed signature; sunset lists in `client.py` fail closed |
| Pydantic | Low — stable at 2.x | Large migration but not urgent |
| Python itself | Decided — floor moves to 3.12 on 1 Nov 2026; band 3.12–3.14 (`docs/design-python-floor.md`) | `scripts/check-python-band.py` measures the band from wheel tags; ceiling waits on presidio `<3.15` |
| protobuf (transitive) | Low — CVE-2026-0994 (DoS); we don't parse untrusted protobuf | Resolves when patched |

---

## Key files to know

| File | What it does |
|------|-------------|
| `pyproject.toml` | Package metadata, deps, tool config (version is dynamic — from `__init__.py`) |
| `bristlenose/__init__.py` | **Single source of truth for version** (`__version__`) |
| `bristlenose/cli.py` | Typer CLI entry point |
| `bristlenose/config.py` | Pydantic settings (env vars, .env, bristlenose.toml) |
| `bristlenose/pipeline.py` | Pipeline orchestrator |
| `bristlenose/people.py` | People file: load, compute stats, merge, write, display name map |
| `bristlenose/stages/s12_render/` | HTML report renderer package |
| `bristlenose/theme/` | Atomic CSS design system |
| `bristlenose/theme/js/` | Report JavaScript modules (frozen — static render path only) |
| `bristlenose/llm/prompts/` | LLM prompt templates |
| `bristlenose/doctor.py` | Doctor check logic |
| `frontend/` | Vite + React + TypeScript SPA |
| `.github/workflows/` | CI (ci.yml), release (release.yml), snap (snap.yml) |
| `snap/snapcraft.yaml` | Snap recipe |

## Key URLs

- **Repo:** https://github.com/cassiocassio/bristlenose
- **Issues:** https://github.com/cassiocassio/bristlenose/issues
- **PyPI:** https://pypi.org/project/bristlenose/
- **Homebrew tap:** https://github.com/cassiocassio/homebrew-bristlenose
- **CI runs:** https://github.com/cassiocassio/bristlenose/actions

---

## Design docs

| Document | Covers |
|----------|--------|
| `docs/archive/design-reactive-ui.md` | Framework comparison, risk assessment (partially superseded by React migration) |
| `docs/design-react-migration.md` | **React migration plan** (Steps 1–10, all complete) |
| `docs/design-react-component-library.md` | 16-primitive component library (complete) |
| `docs/design-llm-providers.md` | Provider roadmap |
| `docs/design-performance.md` | Performance audit |
| `docs/design-export-sharing.md` | Export and sharing phases 0–5 (**superseded** — see 4 feature docs below) |
| `docs/design-export-slides.md` | Export dropdown (scope→format), per-quote copy icon, PowerPoint quote slides |
| `docs/design-export-quotes.md` | CSV + XLS spreadsheet export (11-column table) |
| `docs/design-export-clips.md` | Video clip extraction via FFmpeg |
| `docs/design-export-html.md` | Self-contained HTML export + cross-cutting export concerns |
| `docs/design-miro-bridge.md` | Miro API integration (OAuth, board creation, layout — post-beta) |
| `docs/design-html-report.md` | HTML report, people file, transcript pages |
| `docs/design-discussion-lens.md` | Discussion lens — project quotes onto the researcher's guide by territory; macOS-only. Design + routing spike; feature unbuilt |
| `docs/design-responsive-layout.md` | Responsive layout, density setting, breakpoints |
| `docs/design-doctor-and-snap.md` | Doctor command, snap packaging |
| `docs/design-serve-doctor.md` | Serve-mode doctor checks, Vite auto-discovery |
| `docs/design-research-methodology.md` | Quote selection, sentiment taxonomy, clustering rationale |
| `docs/design-pipeline-resilience.md` | Manifest, event sourcing, resume, provenance |
| `docs/design-logging.md` | Persistent log file, two-knob system |
| `docs/design-test-strategy.md` | Gap audit, Playwright plan, `data-testid` convention |
| `docs/design-desktop-app.md` | macOS app, SwiftUI, PyInstaller sidecar |
| `docs/design-session-management.md` | Re-import, enable/disable, quarantine |
| `docs/archive/design-codebook-island.md` | Migration audit, API design, drag-drop (v1 lens, archived 20 Sep 2026) |
| `docs/design-signal-elaboration.md` | Interpretive names, pattern types |
| `docs/design-transcript-editing.md` | Section strike, text correction, prior art |
| `docs/design-speaker-splitting.md` | LLM splitting for single-speaker transcripts |
| `docs/design-speaker-role-detection.md` | Generalised role detection (oral history, journalism, etc.) |
| `docs/design-speaker-editing.md` | Four transcript editing operations (name, reassign, split, merge) |
| `docs/design-transcript-speaker-editing-roadmap.md` | 11-layer work breakdown for transcript + speaker editing |
| `docs/design-sidebar.md` | Dual-sidebar layout (TOC left, Tags right) |
| `docs/design-windows-ci.md` | Windows CI strategy, compatibility audit, phased plan |

